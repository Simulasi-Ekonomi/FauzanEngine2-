# FauzanEngine Runtime TODO

- [ ] Verify the canonical FauzanEngine source tree, active CMake targets, toolchain versions, and executable entry point on a clean build.
- [ ] Establish one canonical runtime lifecycle: Init, fixed/update tick, render, shutdown, and deterministic error reporting.
- [x] Add canonical `NeoRuntime` state machine for validated Init, fixed Tick, Farm ownership, Shutdown, and explicit invalid-state errors; `runtime_smoke` passes in Release and AddressSanitizer. Renderer attachment remains open.
- [x] Replace legacy no-op `EngineLoop` lifecycle with a smoke-tested explicit `NOT_IMPLEMENTED` failure so it cannot silently impersonate a working renderer loop; use `NeoRuntime` instead.
- [x] Update `SceneWorld` hierarchy composition so a parent's uniform scale and fixed-order Euler-radian rotation transform each child local position before translation; it now rejects non-finite values or non-positive local scale on mutation/deserialization without replacing prior state. Release/AddressSanitizer smoke proves rotated child and grandchild positions, serialization round trip, and atomic invalid-transform rejection. Quaternion/matrix composition, non-uniform-scale rotation semantics, constraints, and production scene graph features remain open.
- [x] Add `KinematicMotionController`, a bounded SceneWorld-local planar controller with finite positive speed, bounded step duration, normalized diagonal input, optional default-off planar heading yaw, and atomic input rejection. When enabled, heading writes local `ry = atan2(normalizedX, normalizedZ)` for nonzero movement only, retaining yaw while idle; Release/AddressSanitizer smoke proves a 3-4 input moves exactly 0.6/0.8 units at speed 10 for 0.1 seconds, right/diagonal yaw, zero-input yaw stability, missing entity, invalid duration, and non-finite-input atomic preservation. Collision, gravity, jump, slope, path following, root motion, prediction, and physics coupling remain open.
- [x] Add `InputMotionBridge`, a bounded four-action bridge from processed `InputState` snapshots to `KinematicMotionController`. Release/AddressSanitizer smoke proves keyboard-style forward-plus-right maps to normalized 0.707107/0.707107 SceneWorld movement, idle stability, duplicate binding rejection, and missing-action preservation. Runtime tick wiring, device profiles, remap persistence, collision, camera-relative input, multiplayer input authority, and player client ownership remain open.
- [x] Add optional `NeoRuntime` ownership and fixed-tick execution for one keyboard-style `InputMotionBridge` entity. The default-off `inputMotionFaceMovementDirection` propagates controller heading yaw only when selected. Release/AddressSanitizer smoke proves config rejection, 60-units/second diagonal movement to 0.707107/0.707107 on one 1/60-second tick, default yaw preservation, opt-in diagonal yaw of π/4, paused-frame preservation, and shutdown cleanup. Device profiles, remap persistence, camera-relative input, collision, player identity selection, multiplayer input authority, prediction, and player client ownership remain open.
- [x] Add `GridRouteFollower`, a bounded continuous follower for a precomputed four-neighbor `GridCell` route over `KinematicMotionController`. It revalidates the current and every target cell against `GridNavigation` before mutation, and can manually replan from the last reached cell to the preserved final goal when the target is newly blocked. Release/AddressSanitizer smoke proves exact arrival at `(1,1)` through four bounded chunks, non-adjacent route rejection, start-transform mismatch rejection, dynamic obstacle rejection with state preservation, deterministic detour replan, and deterministic state. Automatic replanning policy, collision/avoidance, runtime ownership, actor behavior coupling, jump/slope, and multiplayer prediction remain open.
- [x] Add opt-in `NeoRuntime` ownership for exactly one separate static-route entity: it validates a 2–512-cell route and bounded GridNavigation before mutation, creates the entity at route start, advances only while unpaused, and fails runtime tick on a newly blocked target without automatic replan. The default-off `routeMotionFaceMovementDirection` propagates yaw heading only when selected. `ReplanRouteMotion()` is a separate caller-triggered API: it is rejected before route start or in an invalid runtime state, atomically delegates to `GridRouteFollower::Replan`, and cannot recover a runtime already failed by a tick. Release/AddressSanitizer smoke proves default-off absence, invalid route rejection, fixed-tick half-step, opt-in rightward yaw of π/2, pause preservation, resume-to-exact arrival, post-goal stability, manual replan detour around a newly blocked intermediate cell, blocked-target transform preservation, and shutdown cleanup. Runtime policy still has no automatic replan, dynamic obstacle handling beyond explicit caller action, avoidance, collision, player/NPC behavior coupling, prediction, or multiplayer authority.
- [ ] Consolidate ECS ownership, scene serialization, transform writeback, entity lifecycle, and component access into one active implementation.
- [ ] Replace renderer stubs required by the selected platform with a working render loop, camera, mesh, material, lighting, and resource lifetime path.
- [x] Add canonical renderer capability probe that reports explicit `NotImplemented` when no validated backend is linked; Release and AddressSanitizer smoke tests prevent terminal/runtime paths from falsely claiming graphical readiness.
- [x] Add bounded `RenderCamera` orthographic/perspective projection, 2,048-capacity sorted `SpriteBatch`, and camera-aware Farm terrain-plus-actor graphical test slice; dedicated camera/sprite/Farm smokes pass in Release and AddressSanitizer. Mesh/material/light/render-loop readiness remains open.
- [x] Add bounded CPU `MeshRenderer` with perspective projection, finite translation/uniform-scale/Euler-radian rotation validation, finite transform/index/light validation, ambient-plus-directional lighting, rotated normals, depth ordering, optional front-winding back-face culling, and Sutherland–Hodgman clipping only against the perspective near plane before projection; dedicated Release/AddressSanitizer smoke proves near geometry wins, culled reverse winding, two-sided opt-out, deterministic near-plane intersection raster, fully-behind discard, and invalid index/light rejection. Asset import, GPU resource lifetime, side/far-plane clipping, animation, and presentation remain open.
- [x] Bind bounded mesh material sampling only to content-hashed `TextureStagingStore` resources; texture staging now accepts strict P6 PPM plus bounded uncompressed BMP `BITMAPINFOHEADER` 24-bit BGR and 32-bit BGRA resources. Raster texture UV and per-vertex lighting now use validated reciprocal-depth perspective-correct interpolation for perspective meshes. Release/AddressSanitizer smoke proves staged PPM, BMP RGBA conversion, an exact blue BMP texel sampled by CPU mesh material, reciprocal-depth-distorted texel and lighting probes, distinct ambient-light outputs, and malformed texture rejection. Farm sprite textures, broad format support, authoring, GPU mapping, and presentation remain open.
- [x] Add bounded 5×7 bitmap text primitive for uppercase letters, digits, and spaces, including an explicit validated clip-depth draw path for compositing; Release/AddressSanitizer smoke proves deterministic glyph raster and rejects unsupported characters, overflow, invalid depth, and outside-surface text. UI layout, localization, IME, accessibility, and dynamic font assets remain open.
- [x] Add bounded `SceneMeshAdapter` from propagated SceneWorld transforms to the CPU MeshRenderer, permitting finite uniform-scale Euler-radian rotation and conservative local-origin bounding-sphere frustum culling against the current axis-aligned camera; Release/AddressSanitizer smoke proves parent-child world transform, rotation changes the frame, off-frustum culling, deterministic frame hash, missing-entity rejection, and non-uniform-scale rejection. Rotated child position composition, mesh import/asset mapping, clipping/back-face culling, GPU scene graph, animation, streaming, and playable 3D presentation remain open.
- [x] Add bounded `UiInputRouter` with deterministic overlap hit-test, parent validation, pointer capture/release, and removal safeguards; Release/AddressSanitizer smoke passes. Rendered UI, layout, focus navigation, UI animation, localization, touch-device evidence, and accessibility remain open.
- [x] Add bounded `UiCanvasRenderer` that draws styled visible router widgets plus one uppercase/digit/space bitmap label per widget through CPU SpriteBatch and depth-aligned bitmap raster; Release/AddressSanitizer smoke proves routed panel layering, parent-label occlusion by a higher child panel, child label above its own panel, duplicate/invalid/out-of-widget label rejection, and deterministic output. `UiInputRouter` now additionally supports insertion-order Tab/Shift-Tab traversal among visible interactive focusable widgets, explicit focus, activation, focus clearing, and invalid-key/target rejection. Player screens, layout, localization/IME, touch-device evidence, accessibility semantics, dynamic text/font assets, and UI animation remain open.
- [x] Add bounded scalar `AnimationTimeline` with strict keyframe validation, interpolation, clamp, and loop playback; Release/AddressSanitizer smoke passes. Skeletal animation, blend trees, state machines, root motion, transform binding, and animation asset pipeline remain open.
- [x] Bind bounded animation timeline tracks to SceneWorld local position/rotation/scale channels with entity and finite-value validation; Release/AddressSanitizer smoke proves transform writeback and missing-entity rejection. Skeletal animation, blend trees, state machines, root motion, asset import, and NeoRuntime tick binding remain open.
- [x] Add versioned bounded runtime settings and opaque save-envelope codec with atomic deserialize behavior, checksum/trailing-byte rejection, and explicit credential-marker rejection; Release/AddressSanitizer smoke passes. Filesystem/database durability, encryption/key management, cloud sync, backup/recovery, and multiplayer reconciliation remain open.
- [x] Add bounded local `AtomicSaveFile` primitive using safe slots and write-temp/flush/rename for the versioned codec; Release/AddressSanitizer smoke proves isolated write/read, missing-slot, and traversal-style slot rejection. Locking, crash journal, encryption/key management, database/cloud backup, authorization, and Android storage lifecycle remain open.
- [x] Replace the legacy Android native CMake root with an explicit canonical source subset and cross-compile `libneo_core.so` for arm64-v8a using NDK r29, without Gradle packaging. Full JNI lifecycle, device renderer/input/audio/storage, APK/AAB, signing, install, Play compliance, and release operations remain open.
- [x] Add fail-closed `AndroidLifecycleGate` and export a minimal JNI lifecycle event entry from the canonical Android subset; host Release/AddressSanitizer smoke and arm64 cross-compile pass. Java/activity/surface lifecycle, device execution, renderer/input/audio/storage, APK/AAB, signing, install, Play compliance, and release operations remain open.
- [x] Add versioned, checksummed `CommodityCatalog` with 50 bounded game-data commodity definitions across six typed categories; Release/AddressSanitizer smoke proves duplicate/invalid-ID, checksum, and atomic-replace boundaries. Market/payment/loan/government/payroll/accounting and multiplayer-economy authority remain open.
- [x] Add bounded `CommodityEconomyLedger` with typed harvest/craft/quest/shop origins, catalog-price integer transactions, inventory/coin/overflow validation, idempotent command IDs, and a catalog-validated versioned snapshot; Release/AddressSanitizer smoke passes. External receipts/top-up/payment/agent paths, durable/multiplayer authority, bank/loan, payroll, government policy, accounting, and fraud adjudication remain open.
- [x] Add bounded `GameLoanBook` as an internal progression rule with collateral threshold, fixed integer term schedule, maximum 100 terms, and validation; Release/AddressSanitizer smoke passes. No real payment/credit/user finance/top-up path exists; durable/multiplayer authority, payroll, government policy, accounting, and fraud adjudication remain open.
- [x] Audit canonical CMake scope against remaining legacy stub markers; documented renderer, AI, editor/ECS, streaming, and economy placeholders as non-active blockers rather than production implementations.
- [ ] Build a minimum asset pipeline for mesh, texture, material, prefab, scene import, dependency tracking, and hot-reload diagnostics. A bounded OBJ-only in-memory mesh adapter now exists, but it is not a general asset pipeline.
- [x] Add canonical bounded `AssetRegistry` with safe IDs, dependency validation, duplicate rejection, and explicit readiness; Release/AddressSanitizer smoke pass. Byte import, hashing, hot reload, and renderer upload remain open.
- [x] Integrate AssetRegistry ownership into NeoRuntime initialization and shutdown; extended Release/AddressSanitizer lifecycle smoke validates asset readiness and cleanup.
- [x] Add versioned, checksummed `AssetManifestSnapshot` for bounded AssetRegistry metadata and dependency graph; Release/AddressSanitizer smoke proves round trip, registry matching, and checksum rollback. Importers, disk cache, hot reload, GPU upload lifetime, and production authoring remain open.
- [x] Add read-only `AssetReloadDiagnostics` that produces deterministic transitive dependency impact plans; a Release/AddressSanitizer-identified vector invalidation was fixed and both suites now pass. File watching, reload execution, cache invalidation, GPU rebind, importers, and production content pipeline remain open.
- [x] Add `ObjMeshImporter`, a bounded in-memory Wavefront OBJ adapter over MIT TinyObjLoader pinned at commit `45636bdcef1a4fec140346b90c0b50bf0bc3e23b` (SHA-256 documented in `ThirdParty`). It accepts at most 1 MiB source, triangulates faces, deduplicates position/normal/UV tuples to canonical CPU `MeshVertex` data, and limits output to existing 2,048 vertices/6,144 indices while retaining caller output on failure. Explicit finite normals are mandatory by default; a separate opt-in mode emits flat face normals only for nondegenerate triangles. Release/AddressSanitizer smoke proves a UV/normal quad triangulates to four vertices/six indices, missing-normal rejection, opt-in +Z flat normal, degenerate flat-normal rejection, malformed reference, line primitive, oversized source, and 2,049-vertex capacity overflow atomically. Filesystem import, MTL/material import, smoothing-group/angle-weighted normal generation, generic mesh formats, AssetRegistry byte ownership, GPU upload, and production asset pipeline remain open.
- [x] Add `MeshStagingStore`, which accepts only a ready `AssetRegistry` Mesh asset and stages OBJ bytes through `ObjMeshImporter` into a CPU resource retaining the registry content hash. Its `generateFlatNormals` option is default-off and caller-selected; the staged resource records whether it generated flat normals. It bounds resources to 128 and aggregate staged data to 2,048 vertices/6,144 indices, with duplicate, wrong-kind, unready, invalid-OBJ, and capacity rejection preserving existing resources/counters. Release/AddressSanitizer smoke proves staged hash linkage, default missing-normal rejection, explicit flat-normal staging, and atomic rejection. Filesystem import, MTL/material import, smoothing-group/angle-weighted normal generation, generic mesh formats, AssetRegistry reload/cache coordination, GPU upload, and production asset pipeline remain open.
- [x] Add `SceneMeshAdapter::AddStaged`, which copies a nonempty, hash-identified `CpuMeshResource` into the existing bounded scene mesh adapter after validation. Release/AddressSanitizer smoke proves a ready AssetRegistry OBJ mesh stages and produces non-background CPU scene pixels through `AddStaged`, retains the existing deterministic scene hash `201679423701238659` for the prior direct path, and rejects an assetless staged resource. It is a copy-on-register bridge only: no live asset reference, hash refresh/reload, material import, registry lifecycle ownership, GPU upload, or production scene renderer exists.
- [x] Add `MtlMaterialImporter`, a bounded in-memory named-MTL adapter over the same pinned MIT TinyObjLoader parser. It maps finite `[0,1]` diffuse `Kd` and dissolve `d` into CPU `MeshMaterial::rgba`, caps source at 1 MiB, and retains caller material on missing/duplicate name, invalid value, parse, or size failure. TinyObjLoader implementation now resides in one translation unit shared by OBJ and MTL adapters. Release/AddressSanitizer smoke proves diffuse `0.2/0.4/0.6`, alpha `0.5`, atomic missing/duplicate/out-of-range rejection, and source cap. Filesystem `mtllib` resolution, texture maps, PBR, material staging/registry ownership, mesh-to-material assignment, shaders, GPU binding, reload, and production material pipeline remain open.
- [x] Add `MaterialStagingStore`, which accepts only ready `AssetRegistry` Material assets and stages selected MTL names into CPU material resources retaining both asset ID/content hash and initialized CPU light defaults. It caps resources at 128 and rejects missing/wrong-kind/unready assets, duplicate `(asset,name)`, missing MTL name, and capacity overflow without mutation. Release/AddressSanitizer smoke proves hash/RGBA linkage, selection failure, and capacity atomics. Filesystem `mtllib` resolution, texture/PBR maps, mesh-to-material assignment, material reload/cache coordination, shader/GPU binding, and production material pipeline remain open.
- [x] Add `SceneMeshAdapter::AddStaged` overload for a validated pair of staged mesh and material resources. It fails closed on an assetless/named-less/hashless material, then copies both resource values into the existing bounded CPU scene instance. Release/AddressSanitizer smoke proves the staged MTL green diffuse color reaches the CPU scene raster with green dominance and rejects invalid staged material. This remains copy-on-register: no live mesh-material binding, dependency validation, hash refresh/reload, texture/PBR map, shader/GPU binding, or production renderer exists.
- [x] Replace append-invalidating resource vectors in `TextureStagingStore`, `MeshStagingStore`, and `MaterialStagingStore` with append-stable bounded deques. Release/AddressSanitizer smoke retains a `Find` pointer across PPM→BMP staging, valid mesh→flat-normal mesh staging, and one material→127 additional material resources, verifying the original resource remains readable. `AssetRegistry::Find` remains a separate non-stable metadata view; callers requiring its hash across registry mutation must snapshot the value.
- [x] Replace append-invalidating AssetRegistry metadata/byte vectors with paired append-stable deques. Release/AddressSanitizer smoke retains both `Find("farm-tile")` and `Data("farm-tile")` across 512 subsequent asset imports, verifying ID/hash and byte values remain readable. This is lifetime hardening only: no content update/replacement, reload, cache invalidation, dependency executor, or production asset pipeline exists.
- [x] Add `AssetRegistry::ReplaceBytes` for an existing ready asset. It validates per-asset and aggregate byte limits before mutation, then atomically replaces bytes and updates content hash/byte size while preserving metadata-view object identity. Release/AddressSanitizer smoke proves replacement to `{9,8}`, changed hash/size, missing-ID and oversize rejection with preserved replacement, then pointer-stable behavior across 512 imports. It does not reload dependent staging resources, invalidate caches, execute dependency updates, or bind GPU resources.
- [x] Add read-only `IsCurrent` checks to texture, mesh, and material staging stores. Each verifies that its bounded resource still corresponds to a ready registry asset of the expected kind and matching content hash; it has no side effects and fails closed on missing/mismatched data. Release/AddressSanitizer smoke proves every staged resource begins current and becomes stale after `ReplaceBytes`, without restaging or mutating the store. Automatic reload, dependency traversal, cache invalidation, material reassignment, and GPU update remain open.
- [ ] Complete canonical XPBD V5 correctness gaps, including constraints, static collider treatment, deterministic timestep, rotation writeback, thread-safe buffers, and explicit unsupported-feature status.
- [ ] Create reproducible XPBD unit, integration, stress, and benchmark scenes with declared hardware, awake entities, contacts, constraints, iterations, memory, P50, P95, and correctness assertions.
- [ ] Build the Farm Simulation Core vertical slice: grid/terrain, crop lifecycle, harvest, inventory, market transaction, animal pen, quest, UI, persistence, and playable loop.
- [ ] Connect game runtime events to a server-authoritative telemetry, economy ledger, fraud evidence, top-up validation, ban, and release data contract.
- [ ] Make Aries and Coba use typed command contracts with dry-run, approval, build/test evidence, rollback, and no direct runtime or ledger authority.
- [x] Add canonical `AgentCommandGateway` and `agent_gateway_smoke`: Coba audit and Aries game-template plans are typed and dry-run by default; runtime/economy mutation is rejected; non-dry plans require build/test evidence and issue once only to an external supervised executor. Release and AddressSanitizer pass.
- [x] Add canonical `TrustSafetySystem` with duplicate-receipt, ledger-mismatch, and impossible-inventory signals; permanent threshold ban, audit ledger, deterministic replay, Release, and AddressSanitizer smoke pass. Server reconciliation and appeal governance remain open.
- [ ] Replace control-panel demonstration data with authenticated queries to real engine/game services only after those services emit validated data.
- [ ] Produce evidence artifacts: clean build log, executable smoke test, playable vertical-slice recording, XPBD benchmark report, test report, and integration contract report.
- [x] Re-run XPBD 100K collision-heavy stress under AddressSanitizer after the 262,144-contact capacity increase, with no memory error and declared contact count.
- [ ] Replace the current allocation-heavy 100K contact path with a bounded, deterministic contact-cache strategy that preserves all declared collision pairs or reports a fail-closed capacity error.
- [x] Align manifold lookup probe depth with the 64-slot insert probe depth; regression and determinism pass while collision-heavy contact coverage remains at approximately 202,387 contacts/frame.
- [x] Reduce the 100K 10%-awake collision-heavy benchmark below the mandatory <10 ms average without lowering declared contact coverage: retained inverse-distance staging plus the six-worker acceptance configuration measured 9.861 ms mean, 9.737 ms median, 6/10 runs <10 ms, and 201,123.9 contacts/frame.
- [ ] Add XPBD benchmark reporting for hardware, memory, P50, P95, P99, awake ratio, constraints, iteration count, and contact count; the current executable reports only a subset.
- [x] Add a multi-worker XPBD determinism regression that compares serialized state from two identical replays.
- [x] Build FarmSystem runtime primitives: bounded grid tiles, crop lifecycle, watering and harvest, hardened inventory, verified serial receipts, anti-duplicate sale, animal products, quest progress, telemetry snapshot, and signed local save/load.
- [x] Add the `farm_smoke` executable covering a 1,000-tile farm, verified ledger transactions, duplicate-sale rejection, animal product flow, quest completion, and persistence round trip.
- [x] Restore the missing canonical FarmSystem source and `farm_smoke` target after source-tree audit; Release and AddressSanitizer smoke runs now verify the 1,000-tile crop/economy/anti-duplicate/animal/quest/telemetry/persistence loop.
- [ ] Add an interactive Farm presentation loop and engine UI binding on top of the validated headless FarmSystem runtime.
- [x] Add a `farm_demo` terminal player loop and scripted test mode that maps player actions to FarmSystem only, including duplicate-sale rejection; graphical presentation and engine UI binding remain open.
- [x] Add the first executable game-template runtime: deterministic Sudoku with immutable clues, placement conflict checks, completion verification, bounded persistence, and Release/AddressSanitizer `sudoku_smoke` coverage.
- [x] Add the second executable game-template runtime: bounded deterministic Tower Defense with lane placement, waves, combat, rewards, base damage, and Release/AddressSanitizer `tower_defense_smoke` coverage.
- [x] Add the third executable game-template runtime: deterministic Match-Three with adjacent-swap validation, board-wide match resolution, deterministic refill, score accounting, and Release/AddressSanitizer `match_three_smoke` coverage.
- [ ] Expand the validated template pattern into the requested catalog of at least 30 production-capable game templates; Sudoku, Tower Defense, and Match-Three are currently implemented as executable game-state templates.
- [x] Add `GameTemplateRegistry` with 30 typed game concepts and explicit `CatalogOnly`/`ExecutableRuntime` readiness; only smoke-tested Sudoku is executable, so the registry fail-closes plans for all unimplemented templates.
- [x] Add fail-closed Android APK/AAB preflight plus environment-only release signing configuration; sandbox validation blocks correctly because Android SDK/NDK/CMake 3.22.1, Java 17, and release signing values are unavailable.
- [ ] Replace Android JNI legacy source glob with a tested Android-capable subset of the canonical runtime, provision SDK/NDK/Java 17, and produce a signed debug APK then user-signed release AAB before claiming Play Store readiness.
- [x] Add FarmTelemetryAdapter that serializes bounded FarmSystem snapshots/events to the signed control-plane ingest envelope without HTTP or secrets in the runtime loop.
- [x] Restore and validate the canonical `FarmTelemetryAdapter` source plus `farm_telemetry_smoke`: Release/AddressSanitizer output passes, and the control-plane Zod contract test accepts the real bounded envelope while rejecting malformed event types and negative economy balances.
- [x] Integrate TrustSafety into Farm economy via explicit injection: duplicate accepted receipts and impossible-inventory sales create deterministic audit signals; permanent-ban state blocks later economic mutation. Release/AddressSanitizer `farm_trust_smoke` passes; server reconciliation remains open.
- [x] Add persistent bounded `TelemetryOutbox` with envelope deduplication and host acknowledgement; Release/AddressSanitizer smoke pass. A token-holding trusted host forwarder is still required and no smoke data is posted to the dashboard.
- [x] Add a trusted authority forwarder on the FastAPI backend that holds the control-plane token server-side, validates Farm envelopes, and forwards to the control plane with mock-transport tests.
- [x] Replace FastAPI scene actor placeholder routes with explicit NOT_IMPLEMENTED responses until authority-backed persistence exists.
- [x] Replace 3D generation placeholder response with an explicit NOT_IMPLEMENTED response until a queued, traceable production job is implemented.
- [x] Replace massive-world placeholder response with an explicit NOT_IMPLEMENTED response until a real C++/server pipeline exists.
- [x] Replace explicit GPU ECS compute-dispatch stub with a capability-gated implementation and smoke-tested explicit NOT_IMPLEMENTED runtime status.
- [x] Replace VulkanRHI fake instance/device lifecycle success with smoke-tested explicit NOT_IMPLEMENTED runtime status until a validated backend is supplied.
- [x] Implement deterministic FrameGraph dependency ordering with smoke-tested resource reordering and dependency-cycle rejection.
- [x] Remove the generated AI implementation TODO path from OpenCodeIntegration and replace it with a smoke-tested typed, fail-closed agent contract.
- [x] Add benchmark phase timing, broadphase candidate/grid metrics, manifold-cache metrics, max color-batch metrics, configurable frame/worker counts, and an explicit no-phase-instrumentation mode to `bench_100k`.
- [x] Remove dead union-find island work, build graph colors directly from the contact stream, parallelize deterministic grid candidate gathering, parallelize deterministic delta merging, and split large independent color batches; regression, determinism, and AddressSanitizer pass.
- [x] Add a dense-grid canonical emit fast path that skips fallback-only index bounds and normalization guards after gather validation; release regression, determinism, and AddressSanitizer pass while retaining about 202K collision-heavy contacts/frame.
- [x] Re-verify the 100K 10%-awake no-overlap control below 10 ms after retained collision-heavy optimizations: latest 5.160 ms average, 5.408 ms P95, 0 contacts/frame, 8-worker sandbox configuration with phase timing disabled (earlier run: 4.781 ms average).
- [x] Verify the retained 100K 10%-awake collision-heavy workload at the mandatory <10 ms target without lowering coverage: six-worker acceptance validation measured 9.861 ms mean across 10 independent runs at 201,123.9 contacts/frame; the eight-worker comparison configuration remains host-variable and is documented separately.
- [ ] Design and validate a production-grade manifold-cache strategy that materially reduces the remaining collision-heavy cache/create cost without changing warm-start contact coverage; previous enlarged, sharded, and reset-cache trials were rejected for coverage or time regressions.
- [x] Evaluate a deterministic physically reordered contact stream after graph coloring to remove repeated solver indirection; rejected because it preserved regression, determinism, and about 202K contacts/frame but raised a collision-heavy run to 58.975 ms and peak RSS to 134.85 MiB.
- [x] Evaluate deterministic parallel staging of dense-grid contact payloads into fixed contact ranges, followed by serial manifold association; retained after Release regression/determinism, later 8-worker averages of 37.117/36.170/36.471 ms at 202.044 contacts/frame, phase emit reduction, and clean AddressSanitizer regression/determinism plus 201.942 contacts/frame benchmark coverage.
- [x] Evaluate an atomic direct-slot manifold associator over staged dense-grid contacts, with a deterministic capacity fallback; rejected because it reduced a 10-frame run to 199.963 contacts/frame and worsened time to 57.989 ms despite release regression and determinism passing. Source restored to serial associator.
- [x] Recover the complete engine tree after sandbox reset from the user-provided archive, reapply the retained XPBD staging source, restore the headless CMake target, SIMD compatibility layer, raw job adapter, and lifecycle-safe ECS; revalidate Release and AddressSanitizer before resuming optimization.
- [x] Evaluate collision-heavy throughput with two XPBD contact iterations behind a temporary experiment gate; rejected because it reduced coverage to 178.682 contacts/frame (below the ~200K gate), despite regression/determinism passing and runtime falling to 12.886 ms. Default remains three iterations.
- [x] Evaluate a serial direct-slot manifold cache sized to the stamp table, retaining deterministic lookup order while delaying full-cache eviction; rejected because it reduced coverage to 195.463 contacts/frame, introduced 503 rejections/frame, raised runtime to 21.590 ms, and raised RSS to 117.00 MiB despite small-test regression/determinism passing.
- [x] Evaluate applying velocity restitution once per contact step rather than on every position-solver iteration; rejected because coverage collapsed to 154.689 contacts/frame despite small-test regression/determinism passing and runtime falling to 15.792 ms. Default retains restitution on all three iterations.
- [x] Evaluate conservative per-cell AABB and maximum-radius rejection before dense-grid neighbor traversal; rejected because candidate pairs fell to about 384K/frame but retained coverage still cost 19.548/20.068/20.860 ms and phase broadphase rose to 9.097 ms, so metadata overhead outweighed traversal savings.
- [x] Evaluate native horizontal-any mask for the SIMD solver validity check; retained after identical 202.416 contacts/frame, Release regression/determinism, clean AddressSanitizer regression/determinism plus 100K stress, and repeated Release averages of 18.720/19.084/18.611 ms.
- [x] Evaluate awake/static split lists in dense-grid gather to eliminate static-static pair traversal; rejected because coverage remained 202.416 contacts/frame but runtime rose to 20.447 ms, so list construction and ownership branches outweighed skipped static-static checks.
- [x] Evaluate deterministic coalescing of repeated Jacobi contact iterations by scaling their deferred deltas in one solver pass; retained after 202.414 contacts/frame, Release regression/determinism, clean AddressSanitizer regression/determinism plus 100K stress, Release averages of 15.841/16.314/17.252 ms, and solver phase reduction from about 5.874 to 2.246 ms.
- [x] Evaluate exact contact-pair topology caching for graph colors and sorted indices; rejected because the contact sequence changed often enough that compare/rebuild raised graph phase to 2.219 ms and retained full-frame averages at 15.496/15.898/16.032 ms.
- [x] Evaluate colorless Jacobi contact dispatch with sequential contact indices and worker-local deferred deltas; rejected because coverage remained 202.408 contacts/frame but one 202K-contact batch raised runtime to 17.558 ms and RSS to 146.51 MiB.
- [x] Evaluate colorless Jacobi dispatch with deterministic 4K micro-batches; rejected because coverage remained 202.408 contacts/frame but runtime was 17.010 ms and RSS 146.75 MiB, so micro-batching did not recover graph-colored locality or memory behavior.
- [x] Evaluate bit-scan first-free color selection in the contact graph; retained after identical 202.414 contacts/frame, Release regression/determinism, clean AddressSanitizer regression/determinism plus 100K stress, Release averages of 14.204/14.401/14.698 ms, and graph phase reduction to 1.515 ms.
- [x] Evaluate manifold capacity equal to the existing stamp table to reduce full-cache resets while retaining the serial compact-index lookup; rejected because stamp-table rejection reached 73.643/frame, coverage dropped to 191.600 contacts/frame, time rose to 23.428 ms, and RSS rose to 116.67 MiB despite small-test regression/determinism passing.
- [x] Evaluate a separate Release `-ffast-math` toolchain build; rejected because coverage fell to 201.848 contacts/frame and runtime rose to 16.828 ms despite small-test regression/determinism passing, so the portable default remains unchanged.
- [x] Evaluate a separate Release link-time optimization build; rejected because coverage remained 202.414 contacts/frame but runtime was 15.804 ms, slower than the retained bit-scan/Jacobi baseline, despite small-test regression/determinism passing.
- [x] Measure the non-production upper bound of skipping staged manifold association, then restore it; diagnostic reached 10.520 ms with about 203.454 contacts/frame and emit 2.610 ms, proving serial association/cache is the dominant remaining cost but not retaining the unsafe no-warm-start path.
- [x] Evaluate a deterministic hash-sharded manifold associator: retained after warm-start hits, 201.434 contacts/frame, Release regression/determinism, clean AddressSanitizer regression/determinism plus 100K stress, 11.339/11.433/13.054 ms Release runs, and 1.838 ms no-overlap control. Target <10 ms remains open.
- [x] Evaluate manifold-stamp prefetch during parallel payload staging; rejected because coverage stayed 202.414 contacts/frame but runtime rose to 15.717 ms, so speculative prefetch did not improve the serial associator.
- [x] Evaluate deterministic deferred manifold admission: rejected because coverage remained 202.008 contacts/frame but runtime rose to 14.529 ms and RSS to 126.20 MiB; immediate shard admission restored.
- [x] Evaluate 4-shard manifold associator against retained 8-shard baseline; rejected because coverage remained 201.434 contacts/frame but runtime rose to 13.950 ms with P95 35.478 ms, so eight shards remain retained.
- [x] Evaluate 16-shard manifold associator against retained 8-shard baseline; rejected because it kept 201.434 contacts/frame but measured 12.679 ms, not better than the retained eight-shard results, so eight shards were restored.
- [x] Evaluate per-worker/per-shard contact routing during payload staging; rejected because coverage remained 201.434 contacts/frame but runtime was 12.762 ms, not better than retained serial routing, so the simpler serial routing pass was restored.
- [x] Evaluate fused per-shard payload staging and manifold association; rejected because coverage remained 201.434 contacts/frame but runtime rose to 19.013 ms, so the separate parallel stage-job barrier was restored.
- [x] Evaluate dense-shard-only manifold capacity equal to the stamp table, preserving the smaller fallback cache; rejected because coverage was 202.420 contacts/frame but runtime rose to 15.658 ms, RSS to 123.48 MiB, and rejections to 7.5/frame despite higher cache hits.
- [x] Measure deterministic sampled manifold admission as a non-default diagnostic; rejected because it reduced warm-start admission and still measured 13.149 ms at 202.238 contacts/frame, not better than the retained eight-shard full-admission path.
- [x] Evaluate persistent direct-slot manifold storage per dense shard, with collision-safe fallback and no mid-frame eviction; rejected because it measured 16.292 ms, 66.384 rejection/frame, 123.19 MiB RSS, and did not improve coverage or timing. Compact shard cache restored.
- [x] Evaluate a 1.5× dense-shard manifold capacity as a middle point between retained compact capacity and rejected full capacity; rejected because it measured 14.084 ms, 121.17 MiB RSS, and did not improve timing versus the retained compact capacity.
- [x] Evaluate packed precomputed dense-manifold shard routing in parallel gather, so the serial routing pass only appends prehashed contact indices without enlarging `GridContactCandidate`; rejected because it preserved coverage and validation but measured 14.395 ms, so baseline routing was restored.
- [x] Evaluate occupancy early-exit for dense-grid linked-list traversal, preserving candidate ordering while avoiding same-cell singleton and empty-neighbor loop setup; retained after Release regression/determinism, identical 201.434 contacts/frame, three Release averages of 13.525/14.892/14.294 ms, and clean AddressSanitizer regression/determinism plus 100K stress.
- [x] Evaluate paired dense-shard associator tasks, keeping all eight shard-local serial orders but dispatching four deterministic jobs; rejected because it retained coverage and validation but measured 14.316 ms, not better than the retained eight-job path.
- [x] Evaluate a dense-manifold-only 32-bit mixed hash for shard/slot selection, isolated from global pair deduplication; rejected because it preserved coverage and determinism but measured 15.213 ms, not better than the retained multiplicative hash.
- [x] Evaluate canonical dense-grid pair-key construction in routing and shard association, removing redundant swap branches after gather normalization; rejected because it preserved coverage and determinism but measured 15.102 ms, not better than the retained `MakeKey` path.
- [x] Evaluate epoch-based touched-cell reset for dense-grid scratch state, eliminating full-grid clearing while preserving deterministic active-cell and contact order; rejected because the initial version exposed stale-neighbor coverage loss (190.074 contacts/frame), and the corrected version restored coverage/determinism but measured 15.092 ms with added complexity.
- [x] Evaluate removal of redundant dense-grid next-link clearing, because every participating collider overwrites its link during grid insert; retained after identical 201.434 contacts/frame, Release regression/determinism, three Release averages of 13.957/14.376/14.340 ms, and clean AddressSanitizer regression/determinism plus 100K stress.
- [x] Evaluate a contiguous per-cell dense-grid storage layout to replace linked-list traversal in gather; rejected because it preserved Release regression/determinism and about 201.434 contacts/frame but the three-run range was 13.433/14.540/15.462 ms with higher 120.40–121.44 MiB RSS, so linked-list locality remained preferable.
- [x] Evaluate staging the dense-manifold shard ID in the pre-association `manifoldIndex` slot, moving hash selection into existing parallel payload jobs without enlarging candidates; rejected because it preserved coverage and validation but measured 15.484 ms, not better than retained routing.
- [x] Evaluate a direct-mapped exact-key dense-shard manifold cache that overwrites only warm-start state on hash collision, never rejects contacts, and removes 64-slot probe traversal; rejected because it preserved regression/determinism but collision-heavy coverage fell to 183.706 kontak/frame with 14.137 ms and 122.08 MiB RSS.
- [x] Evaluate direct-mapped dense-shard admission without overwrite: hash collisions remain uncached but all contacts still solve, preserving selected warm-start entries; rejected because regression/determinism passed but collision-heavy coverage fell to 193.609 kontak/frame, with 87.099 cache rejections/frame, 14.192 ms, and 122.79 MiB RSS.
- [x] Evaluate portable SIMD-assisted scalar inverse-square-root with one Newton–Raphson refinement for dense-grid contact normals; rejected because it preserved regression, determinism, and 201.434 contacts/frame but measured 14.720 ms, not better than the retained exact root path.
- [x] Evaluate cacheline isolation for per-shard manifold statistics to eliminate potential false sharing in associator hot counters; rejected because it preserved coverage and validation but measured 14.612 ms with P95 27.427 ms, not better than the retained layout.
- [x] Evaluate gating all dense-manifold cache counters behind profiling mode, preserving detailed metrics in instrumented runs while eliminating per-contact counter writes from non-instrumented headline benchmarks; retained after identical 201.434 contacts/frame, Release regression/determinism, headline averages of 12.636/12.493/12.942 ms, preserved profiling metrics, and clean AddressSanitizer regression/determinism plus 100K stress.
- [x] Re-evaluate compact dense-shard manifold capacity 1,5× after cache-metrics gating changed the headline hot path; retained after 201.173 contacts/frame, Release regression/determinism, three headline averages of 12.525/12.392/12.830 ms, preserved profiling metrics, and clean AddressSanitizer regression/determinism plus 100K stress. The retained memory cost is about 1,3–2,2 MiB RSS.
- [x] Re-evaluate four deterministic paired dense-shard associator tasks after cache-metrics gating changed the dispatch/counter balance; rejected because regression/determinism and coverage held but headline runtime was 13.197 ms, not better than retained eight-shard dispatch.
- [x] Re-evaluate packed precomputed dense-manifold shard routing after cache-metrics gating, moving shard hash to parallel gather without enlarging `GridContactCandidate`; rejected because regression/determinism and coverage held but runtime was 14.504 ms, not better than retained routing.
- [x] Evaluate tail-only sentinel initialization in `SolveColorBatch`, avoiding redundant invalid-index writes for full SIMD lanes; rejected because regression/determinism and coverage held but three headline runs measured 11.967/13.262/13.674 ms, not better than the retained configuration.
- [x] Evaluate gating dense-grid broadphase counters behind profiling mode, preserving candidate/occupancy evidence when instrumented while removing counter writes from headline benchmarks; rejected because regression/determinism held but the branch in the candidate hot path measured 13.681 ms headline and 15.333 ms profiling, not better than retained unconditional counters.
- [x] Evaluate homogeneous default collision-layer fast path in broadphase, skipping per-pair layer loads only until a custom layer is explicitly configured; rejected because regression/determinism and coverage held but runtime was 13.587 ms, not better than direct layer checks.
- [x] Evaluate compact dense-shard capacity 1,25× as a memory-lower middle point after 1,5× retention; rejected because regression/determinism and coverage held but three headline runs measured 12.058/13.585/13.465 ms, not better than 1,5× retained.
- [x] Re-evaluate deterministic 1/8 dense-manifold cache admission after headline counter gating; rejected because regression/determinism passed and time reached 11.441 ms, but collision-heavy coverage fell to 177.446 kontak/frame, below the mandatory gate.
- [x] Evaluate dense-manifold probe limit 16 versus retained 64; rejected because regression/determinism and coverage held but runtime was 13.374 ms, not better than probe 64 retained.
- [x] Evaluate GCC Profile-Guided Optimization trained on deterministic collision-heavy XPBD; rejected because regression/determinism passed but three PGO runs measured 13.400/14.355/14.509 ms, not better than retained Release.
- [x] Evaluate a host-specific GCC `-march=native` build for the XPBD benchmark; rejected because regression/determinism passed but three native runs measured 12.866/12.534/13.852 ms, not better than portable Release retained.
- [x] Evaluate compile-time specialization of dense-shard associator for headline versus profiling metrics, eliminating runtime counter branches only in non-instrumented mode; rejected because regression/determinism and profiling metrics held but headline measured 13.295 ms and profiling 16.274 ms, not better than retained runtime-gated associator.
- [x] Evaluate a four-job cap for dense-grid gather/staging under an eight-worker scheduler; rejected because regression/determinism and coverage held but runtime was 12.666 ms, not better than retained task partitioning.
- [x] Re-evaluate deterministic per-worker/per-shard contact routing after headline counter gating; rejected because regression/determinism and coverage held but three runs measured 11.963/12.697/13.220 ms, not consistently better than retained serial routing.
- [x] Evaluate compile-time specialization of dense-grid gather profiling versus headline mode, removing candidate-counter work without a runtime branch in `appendIfContact`; rejected because regression/determinism and profiling evidence held but headline measured 13.600 ms and profiling 16.442 ms, not better than retained gather.
- [x] Evaluate dense-grid cell size at 1,25× maximum contact diameter while retaining a complete one-cell neighbor stencil; rejected because regression/determinism and coverage held but three runs measured 12.242/13.242/13.234 ms, not better than diameter-sized cells, while candidates rose to about 753.990/frame.
- [x] Evaluate reuse of routing hash in the temporary pre-association manifold slot, avoiding a second `HashIndex` call per staged contact; rejected because regression/determinism and coverage held but temporary ContactBlock traffic measured 14.500 ms, not better than retained recomputation.
- [x] Evaluate half-sized dense-shard stamp tables with retained 1,5× manifold capacity; rejected because regression/determinism and coverage held but runtime was 14.119 ms, not better than retained stamp tables.
- [x] Evaluate cached `(x,z)` coordinates for active dense-grid cells to remove gather-time cell division/modulo; rejected because the initial world-coordinate version lost coverage (27.779 kontak/frame), and the corrected local-coordinate version restored validation/coverage but three runs measured 12.742/13.690/12.950 ms with higher RSS, not better than retained compact indices.
- [x] Evaluate direct canonical pair-key construction only inside dense-shard associator, where staged grid indices are already canonical; rejected because regression/determinism and coverage held but runtime was 12.674 ms, not better than retained `MakeKey`.
- [x] Evaluate immovable-aware contact graph coloring that excludes endpoints with zero inverse mass and inertia from color masks; rejected because regression/determinism and coverage held but benchmark colors remained 11,7/frame and runtime was 12.597 ms, not better than retained coloring.
- [x] Evaluate compact `GridContactCandidate` layout that stores only fields consumed by normal staging, recomputing fallback-only data locally; rejected because regression/determinism and coverage held but three runs measured 12.565/12.439/12.773 ms, not consistently better than retained payload layout.
- [x] Evaluate condition-variable wake-up for JobSystem worker idle waits, replacing unconsumed wake flags and 50 µs polling sleep; rejected because liveness, regression, and determinism passed but three runs measured 13.217/13.439/12.994 ms, not better than retained polling.
- [x] Evaluate a 1 µs `WaitForAll` backoff instead of host-thread yield spinning; retained after liveness, Release regression/determinism, and AddressSanitizer regression/determinism plus 100K stress passed with identical 201.173 contacts/frame. Three Release averages improved to 9.982/10.932/10.031 ms; P95 remains host-variable and the <10 ms average gate still needs additional improvement.
- [x] Evaluate a 2 µs `WaitForAll` backoff as a scheduler tuning point after retained 1 µs backoff; rejected because regression/determinism and coverage held but three runs measured 12.356/12.033/12.365 ms, slower than 1 µs retained.
- [x] Evaluate atomic consumption of worker `shouldWake` before idle sleep to avoid a lost wake window in JobSystem polling; rejected because liveness, regression, determinism, and coverage held but three runs measured 10.532/11.641/13.049 ms, not better than retained scheduler.
- [x] Evaluate reducing JobSystem idle worker polling sleep from 50 µs to 10 µs alongside retained 1 µs waiter backoff; rejected because regression/determinism and coverage held but three runs measured 10.883/11.429/11.165 ms, slower than 50 µs retained.
- [x] Evaluate 8.192-contact solver color tasks versus retained 4.096-contact tasks; rejected because regression/determinism and coverage held but three runs measured 13.497/13.577/14.141 ms, much slower than retained task granularity.
- [x] Evaluate 2.048-contact solver color tasks versus retained 4.096-contact tasks; rejected because regression/determinism and coverage held but three runs measured 12.353/12.216/12.118 ms, slower than retained task granularity.
- [x] Evaluate a 500 ns `WaitForAll` backoff versus retained 1 µs backoff; rejected because regression/determinism and coverage held but three runs measured 12.506/12.135/12.576 ms, slower than 1 µs retained.
- [x] Evaluate sparse reset of graph entity-color masks via a touched-entity list; rejected because graph correctness, regression, determinism, and coverage held but three runs measured 11.904/12.433/12.121 ms, not better than the retained full clear.
- [x] Re-evaluate 2× compact dense-shard manifold capacity after headline metric gating and scheduler backoff; rejected because regression/determinism passed but three runs measured 13.373/13.769/13.528 ms with 123.19–123.71 MiB RSS, not better than retained 1,5× capacity.
- [x] Evaluate a uniform-collider-radius broadphase fast path that detects homogeneous radii during setup and avoids per-candidate radius loads; rejected because regression/determinism and coverage held but three runs measured 13.358/12.428/12.758 ms, not better than direct radius loads.
- [x] Evaluate removal of redundant graph-color buffer clearing, because every contact color is overwritten before scatter; rejected because regression/determinism and coverage held but three headline runs measured 12.051/13.071/13.344 ms, not better than the retained configuration.
- [x] Evaluate canonical-endpoint shard routing for staged dense-grid contacts, replacing the serial routing hash with a low-bit shard selection while retaining collision-safe hashed slots within each shard; rejected because regression/determinism passed but three runs reached only 200.748 kontak/frame and 11.508/11.291/11.313 ms, so neither coverage nor timing improved over hash-balanced routing.
- [x] Evaluate a worker pre-sleep wake recheck that skips the 50 µs idle sleep when a producer has already marked work pending; rejected because regression/determinism and coverage held but three runs measured 13.398/12.943/13.122 ms, so atomic rechecks did not beat polling retained.
- [x] Evaluate portable Linux/Android worker CPU affinity that maps scheduler workers cyclically to available logical CPUs; rejected because regression/determinism and coverage held but three runs measured 12.110/12.762/13.001 ms, so pinning did not beat portable scheduler placement.
- [x] Evaluate dense-grid neighbor traversal that tests the head sentinel before canonical cell ordering, eliminating a redundant active-cell occupancy load while preserving the exact emitted pair stream; rejected because regression/determinism passed but stress coverage fell to 177.065 kontak/frame, revealing that active-cell occupancy has non-redundant deduplication semantics.
- [x] Evaluate short-circuit ordering of dense-grid neighbor deduplication so the cell-order comparison precedes the required occupancy lookup, preserving semantics while avoiding occupancy reads for forward neighbors; rejected because regression/determinism and coverage held but three runs measured 11.305/11.971/12.646 ms, not consistently better than retained ordering.
- [x] Evaluate awake-first traversal when an active dense-grid cell meets an occupied static-only neighbor, skipping provably inactive pair calls without allocating awake/static split lists or changing live-contact order; rejected because regression/determinism and coverage held but three runs measured 12.303/12.653/13.243 ms, so added branching did not beat the compact stencil.
- [x] Evaluate cooperative `WaitForAll` work-stealing by the caller thread during scheduler barriers; rejected because timeout liveness, regression, determinism, and coverage held but three runs measured 11.102/12.566/12.236 ms, not consistently better than retained backoff.
- [x] Evaluate dense-shard contact references that carry staged contact index plus canonical endpoints, reducing random ContactBlock reads in manifold association while retaining deterministic shard order and exact payload writeback; rejected because regression/determinism passed but two runs measured 11.897/12.697 ms, RSS rose to 123.29 MiB, and the third run exceeded the timeout guard.
- [x] Evaluate a separate Release toolchain with `-fno-math-errno` only, retaining strict floating-point semantics while testing whether square-root calls in dense-grid narrowphase compile more efficiently; rejected because regression/determinism passed but the repeated collision-heavy harness did not complete within the timeout guard, so no stable improvement was demonstrated.
- [x] Evaluate C++20 `atomic::wait`/`notify_one` worker wake-up with a post-idle queue recheck, replacing 50 µs polling without mutex condition-variable overhead; rejected because timeout liveness, regression, determinism, and coverage held but three runs measured 12.910/12.194/12.850 ms, slower than polling retained.
- [x] Evaluate one-contact look-ahead stamp prefetch inside the serial dense-shard associator, keeping the hint close to the actual random lookup rather than issuing it in the earlier payload-staging phase; rejected because regression/determinism and coverage held but three runs measured 12.146/12.279/13.483 ms, not better than retained associator.
- [x] Evaluate deterministic 2× worker-count task partitioning for dense-grid gather and payload staging, preserving contiguous active-cell/task order while reducing potential per-worker traversal imbalance; rejected because regression/determinism and coverage held but three runs measured 13.469/12.371/12.189 ms with higher tail latency, not better than one task per worker retained.
- [x] Evaluate deferred dense-shard manifold writeback to shard-local result buffers followed by deterministic parallel scatter to staged ContactBlock ranges, isolating associator writes from cross-shard cache-line contention while preserving lookup order; rejected because regression/determinism and coverage held but three runs measured 12.691/12.774/12.537 ms with higher RSS, so scatter/barrier overhead exceeded any false-sharing benefit.
- [x] Evaluate dense-shard actual-capacity retention by resetting cache only when it is already full rather than when all incoming contacts could theoretically be new; rejected because regression/determinism passed but profiling reached 168.915 rejection/frame and three runs measured 12.981/13.116/12.868 ms, so bounded preflight reset remains preferable.
- [x] Evaluate late canonicalization in dense-grid narrowphase: reject noncontacts using order-invariant radius/distance first, then normalize endpoint indices and normal only for emitted contacts while preserving output stream semantics; rejected because regression/determinism and coverage held but three runs measured 12.937/12.723/12.808 ms, slower than canonical-first retained.
- [x] Evaluate fixed-span dense-shard contact routing that writes into pre-sized per-shard storage using explicit counts, with an allocation-safe overflow fallback, to reduce serial vector append bookkeeping without changing shard order; rejected because regression/determinism and coverage held but three runs measured 13.464/12.977/13.495 ms, so standard reserved append remains faster.
- [x] Evaluate a 1,75× dense-shard manifold capacity between retained 1,5× and rejected 2×, testing whether near-two-frame cache retention offsets a smaller locality/memory cost than full 2× capacity; rejected because regression/determinism passed but three runs measured 14.306/14.347/14.207 ms with coverage shifting to 202.420 kontak/frame, not better than 1,5× retained.
- [x] Evaluate per-contact canonical key staging during parallel payload writes, allowing the serial associator to decode endpoints from compact key storage instead of loading them from sparse ContactBlock lanes before manifold lookup; rejected because regression/determinism and coverage held but three runs measured 12.990/13.118/13.055 ms with higher RSS, so added key traffic did not beat direct ContactBlock loads.
- [x] Evaluate local dense-shard manifold admission counts with one end-of-job commit, eliminating repeated parallel writes to adjacent `m_DenseShardCounts` cache-line slots while preserving each shard's serial capacity guard; retained after Release regression/determinism, clean AddressSanitizer regression/determinism + 100K stress, identical 201.173 kontak/frame, no-overlap 1.828 ms, and profiling broadphase/emit reduction to 6.217/3.855 ms. Six headline runs remain about 11.171–11.717 ms, so target <10 ms stays open.
- [x] Evaluate deferred dense-shard manifold writeback with a single serial contiguous scatter after the existing associator barrier, avoiding random ContactBlock writes across shards without adding a second scheduler wave; rejected because regression/determinism and coverage held but three runs measured 12.713/11.820/11.822 ms with higher RSS, so serial scatter did not beat direct local-shard writeback.
- [x] Evaluate a separate Release toolchain with `-funroll-loops`, preserving strict floating-point semantics while testing code generation for bounded manifold probe and SIMD solver loops; rejected because regression/determinism passed but three runs measured 11.899/11.712/12.008 ms, not better than Release retained.
- [x] Evaluate a 2× dense-shard stamp table with retained 1,5× manifold capacity, testing whether fewer hash collisions/probes outweigh extra reset memory traffic and RSS; rejected because regression/determinism and coverage held but three runs measured 11.684/12.487/11.835 ms with higher RSS, not better than retained stamp size.
- [x] Evaluate removal of the unused global `activeJobs_` scheduler counter, eliminating two shared atomic updates per executed task while retaining `totalJobs_` completion accounting and queue semantics; rejected because regression/determinism and coverage held but three runs measured 11.340/12.089/12.440 ms, not better than retained scheduler lifecycle accounting.
- [x] Evaluate removal of unused `shouldWake` atomic stores/field in the polling JobSystem, retaining worker loop timeout and shutdown join semantics while eliminating enqueue and idle flag traffic; retained after Release regression/determinism, clean AddressSanitizer regression/determinism + 100K stress, identical 201.173 kontak/frame, no-overlap 1.778 ms, and six headline runs including 9.913/9.986 ms. Variasi 10.338–11.339 ms berarti target <10 ms konsisten tetap terbuka.
- [x] Evaluate a 25 µs worker idle polling interval after removing unused wake stores, testing the midpoint between rejected 10 µs and retained 50 µs intervals for lower scheduler latency without excessive contention; rejected because regression/determinism and coverage held but three runs measured 9.893/11.911/11.593 ms, so 50 µs remains the more stable retained interval.
- [ ] Define the production-backend readiness gate: validated render backend, asset byte import/upload, scene/entity ownership, camera/input/audio paths, executable Farm graphical vertical slice, platform packaging evidence, and no application/website work before this gate passes.
- [ ] Consolidate canonical scene/entity/component ownership with deterministic transforms, lifecycle, serialization, prefabs, and runtime scene loading; replace legacy ECS/editor ambiguity with one smoke-tested implementation.
- [ ] Implement a validated renderer backend with explicit window/surface lifecycle, command submission, camera, mesh, material, texture, lighting, resource lifetime, deterministic capability reporting, and headless fail-closed fallback.
- [ ] Implement asset byte import and content hashing for mesh, texture, material, audio, prefab, and scene resources; connect AssetRegistry metadata to actual bytes and renderer upload readiness.
- [ ] Implement runtime-facing input abstraction, audio mixer/resource lifetime, game UI presentation layer, and platform-independent executable bootstrap without creating a separate application or website.
- [ ] Build a graphical Farm vertical slice solely inside the executable engine runtime, connecting actual crop/animal/economy/trust state to presentation and input while preserving the existing headless determinism tests.
- [ ] Extend NeoRuntime beyond Farm to select, initialize, tick, persist, and fail-close executable game templates; create the remaining 27 executable game-template backends using the same validation contract.
- [ ] Replace Android preflight-only status with a canonical JNI bridge, SDK/NDK/Java 17 toolchain, debug APK, signing-safe release AAB, device/emulator evidence, and Play Store submission artifact validation.
- [ ] Keep application, website, dashboard, and control-surface development deferred until the production-backend readiness gate passes; do not represent any graphical client as ready before executable evidence exists.
- [x] Add a deterministic headless `SoftwareRenderer` that allocates a bounded framebuffer, rasterizes a triangle, writes a non-empty PPM frame artifact, and produces a stable hash; Release and AddressSanitizer `software_renderer_smoke` pass. Native GPU/window/Android backends remain open.
- [x] Add bounded canonical `SceneWorld` with generation-safe entity handles, hierarchy cycle rejection, transforms, deterministic serialization, and Release/AddressSanitizer `scene_world_smoke` coverage. Prefabs/component registry/runtime integration remain open.
- [x] Integrate `SceneWorld` ownership and teardown into `NeoRuntime`; extended Release/AddressSanitizer `runtime_smoke` verifies Farm, AssetRegistry, and SceneWorld lifecycle ownership together.
- [x] Extend `AssetRegistry` with bounded in-memory byte import, deterministic FNV content hash, byte-size metadata, bounded aggregate storage, and byte retrieval; Release/AddressSanitizer `asset_registry_smoke` pass. File-format decoders and renderer GPU upload remain open.
- [x] Add bounded canonical `InputState` with deterministic per-frame pressed/just-pressed/just-released transitions and a bounded event queue; Release/AddressSanitizer `input_state_smoke` pass. Platform window, Android touch, controller, and remapping integration remain open.
- [x] Add bounded canonical PCM `AudioMixer` with stable mixing, saturation, voice completion, and Release/AddressSanitizer `audio_mixer_smoke` coverage. Decoder, spatialization, platform audio output, and asset-to-voice integration remain open.
- [x] Replace the renderer's blanket `NotImplemented` capability report with an explicit `ReadyHeadless` software-raster capability; `CanRender()` is true while `CanPresent()` remains false, verified in Release/AddressSanitizer. GPU/window/mobile presentation remains an open readiness blocker.
- [x] Provision Vulkan/SDL development dependencies and add a canonical Vulkan bootstrap that creates an instance, enumerates one sandbox physical device, creates/releases one graphics logical device, and records/submits an empty command buffer with a fence in Release. AddressSanitizer finds no engine error when driver leak detection is disabled; the Mesa/Vulkan loader retains a 128-byte external allocation with leak detection enabled. Surface, swapchain, shader pipeline, and presentation remain open.
- [x] Add minimal vertex/fragment GLSL sources, compile and validate their SPIR-V artifacts, and create/destroy both shader modules on the canonical Vulkan logical device in Release and AddressSanitizer-with-driver-leak-detection-disabled. Render pass, graphics pipeline, image target, readback, surface, and presentation remain open.
- [x] Add an engine-owned `FarmRenderAdapter` that maps real Farm tile state to software-rendered geometry and produces a deterministic non-empty Farm PPM frame; Release/AddressSanitizer `farm_render_smoke` pass. Camera/UI/animation/texture presentation and GPU output remain open.
- [x] Add CMake-owned GLSL-to-SPIR-V generation so the Vulkan shader smoke depends on current shader sources rather than manually retained binary artifacts.
- [x] Integrate owned `SoftwareRenderer` initialization, `RenderFarm`, and teardown into `NeoRuntime`; Release/AddressSanitizer lifecycle smoke now validates a real Farm frame hash through the canonical runtime. GPU renderer attachment remains open.
- [x] Build and link the canonical shared `neo_core` library successfully after adding scene, renderer, asset, input, audio, Farm render, Vulkan bootstrap, and shader modules.
- [x] Add a bounded Vulkan offscreen triangle path using a shared RAII device context, color image, render pass, framebuffer, graphics pipeline, host-visible readback buffer, fence, and deterministic pixel hash. Release and AddressSanitizer-with-driver-leak-detection-disabled `vulkan_offscreen_smoke` pass twice per execution with 512 non-clear pixels and hash `17894681420588429683`; this is GPU offscreen evidence only, not a surface, swapchain, window, or presentation implementation.
- [x] Define a bounded canonical RPG sandbox contract for a 1024×1024 world grid, four cities, 10,000 player/NPC population, 10,000 quests, level cap 1,000, inventory economy, and explicit capacity/error semantics.
- [x] Implement deterministic dungeon population rules: 100 monsters per level per dungeon, five-second respawn after a death, and a strict no-over-capacity invariant.
- [x] Implement typed item grades and rules: Common/Uncommon/Rare drop grades, Epic/Legendary/Mythic bundle-only grades, white-to-green-to-blue-to-yellow-to-orange-to-red upgrade costs, and probability-gated enhancement that becomes harder at higher tiers.
- [x] Add a deterministic 1024×1024 sandbox stress scenario with four cities, 10,000 players, 10,000 NPCs, 10,000 quests, 1,000 levels, 100,000 monster deaths/respawns, explicit item-economy assertions, and Release/AddressSanitizer validation.
- [ ] Keep APK, AAB, website, dashboard, and control-surface work deferred until the RPG sandbox evidence passes the declared capacity and invariant tests.
- [x] Define the canonical Farm production-tool contract: bounded world/scene dimensions, building types and placement validation, character/NPC capacity, government authority boundaries, save/version behavior, and explicit fail-closed errors.
- [x] Implement a canonical Farm world tool with terrain cells, scene anchors, player character state, building definitions, deterministic placement/removal, and bounded versioned world serialization.
- [x] Implement Farm NPC tooling with typed roles, deterministic behavior selection, movement goals, quest issuance/completion, and no authority over economy, bans, or external agents.
- [x] Implement a bounded Farm government tool with policies, observation-only treasury/accounting events, permits, and typed governance decisions that cannot mint player currency or bypass TrustSafety.
- [x] Integrate the new Farm world tool with existing FarmSystem economy, anti-inject TrustSafety, persistent telemetry outbox, and versioned persistence without storing credentials in saves.
- [x] Build and validate a deterministic Farm sandbox scenario covering construction, scenes, characters, NPC behavior, quest issuance/completion, government policy, Farm economy/trust, telemetry, serialization, Release, and AddressSanitizer.
- [x] Connect validated Farm world/scene state to NeoRuntime and renderer-owned headless test paths; separate frontend, website, dashboard, APK, and AAB work remains deferred until the production-backend readiness gate passes.
- [ ] Audit the user-provided game, multiplayer, anti-cheat, economy, and release documents into traceable release gates; do not mark any game 100% ready until every gate has executable evidence.
- [ ] Research current high-potential lightweight mobile game categories using credible public market sources; record uncertainty and avoid fabricated installs, revenue, ratings, reviews, or user behavior data.
- [ ] Build a shared server-authoritative multiplayer contract with replay-safe commands, state snapshots, reconciliation, rate limits, idempotency, anti-cheat evidence, ban/appeal boundaries, and load-test acceptance criteria.
- [ ] Extend the Farm release path from local tool sandbox to an authoritative multiplayer vertical slice with real persistence, abuse simulations, data retention, recovery, telemetry forwarding, and operational failure tests.
- [ ] Build the required renderer, asset, input, audio, client, Android, packaging, accessibility, privacy, and security evidence before claiming that Farm or any other game is ready to ship.
- [ ] Define and implement a reusable production-tool template for each priority lightweight game genre; only count a template as release-ready after its own gameplay, server, anti-cheat, client, package, and operation gates pass.
- [x] Add and validate an in-process shared AuthoritativeCommandGate plus FarmAuthoritativeService: typed bounded commands, session binding and safe session rotation, strict sequence ordering, duplicate-command idempotency, client-tick skew rejection, rate limits, TrustSafety ban enforcement, revision-tagged Farm snapshots, and NeoRuntime ownership. This is a local authority foundation only, not a network service, durable ledger, multiplayer simulation, or release-ready anti-cheat system.
- [x] Stress the in-process authority core at its explicit 10,000-player bound: one accepted command plus one idempotent replay per player, with exactly 10,000 handler executions and revisions in Release and AddressSanitizer. This does not prove 10,000 network connections or production service capacity.
- [x] Add a bounded CPU texture staging store that consumes imported PPM assets, verifies kind/source metadata, enforces decoded-memory limits, and exposes renderer-ready RGBA resources; Release and AddressSanitizer smoke pass. GPU upload and sampling remain separate gates.
- [x] Add a bounded Vulkan RGBA texture-upload/readback path that consumes staged CPU textures, validates image/memory/transfer ownership, and compares readback hashes; Release and AddressSanitizer-with-driver-leak-detection-disabled smoke pass. Material binding, shader sampling, and presentation remain separate gates.
- [x] Add a bounded Vulkan textured offscreen draw with image view, sampler, descriptor set, shader sampling, and pixel readback; Release and AddressSanitizer-with-driver-leak-detection-disabled smoke pass. Mesh import, camera/light/material authoring, surface, and presentation remain separate gates.
- [x] Add an engine-owned SDL/Vulkan surface and swapchain present proof under a virtual display, with explicit unsupported/fail-closed reporting; Release and AddressSanitizer-with-driver-leak-detection-disabled smoke pass on Xvfb. User-facing windows, gameplay UI, Android, and shipping presentation remain separate gates.
- [x] Render a staged texture through a Vulkan descriptor/pipeline into an SDL swapchain image and present it under Xvfb; `vulkan_textured_present_smoke` passes twice in Release under Xvfb with stable texture hash `1142008413756203960`. Game scene integration, player UI, input/audio platform, Android, and device evidence remain separate gates.
- [x] Add an SDL event bridge that maps real platform keyboard input to bounded InputState events and validates quit/window boundaries without creating a user-facing UI; Release and AddressSanitizer smoke pass on Xvfb. Touch, controller, mobile lifecycle, and player UI remain separate gates.
- [x] Add an SDL audio-device bridge that drains bounded AudioMixer PCM output through a platform callback and validates device lifecycle without creating a user-facing client; Release and AddressSanitizer smoke pass with SDL dummy audio. Android audio, decoder formats, streaming, interruption handling, and player-facing controls remain separate gates.
- [x] Add a bounded loopback TCP authority transport that frames AuthorityWireProtocol commands, delegates only to AuthoritativeCommandGate, and returns revision snapshots; Release and AddressSanitizer smoke pass. Production authentication, TLS, distributed scaling, persistence, and internet deployment remain separate gates.
- [x] Route one Farm gameplay command through the bounded loopback TCP transport into FarmAuthoritativeService and return a serialized Farm snapshot; Release and AddressSanitizer smoke pass. It remains a local proof, not multiplayer service deployment.
- [x] Harden the loopback authority transport lifecycle against an idle connected client and concurrent stop by tracking sockets atomically, shutting down active client I/O, and asserting prompt teardown; Release and AddressSanitizer smoke pass.
- [x] Add a bounded versioned Farm authority checkpoint that combines world state with replay/sequence ledger for restart-local reconciliation, explicit session rebind, idempotent replay, sequence continuation, and rollback assertions for corrupt/truncated input; Release and AddressSanitizer smoke pass. Durable database, encryption, backup, and cross-process production recovery remain separate gates.
- [x] Extend the local loopback authority proof to process a bounded ordered sequence of Farm commands on one connection and reconcile each returned authoritative snapshot; three commands plus an idempotent replay pass in Release and AddressSanitizer. This remains a one-client localhost test, not production multiplayer.
- [ ] Select and implement a real authority persistence stack from the documented transaction/recovery design: authenticated sessions, database migrations, exactly-once command records, atomic world/ledger/outbox writes, backup/restore, and fault-injection evidence. No sandbox process or local checkpoint may be represented as that service.
- [ ] Audit the canonical engine and existing agent contracts for real prompt-to-game capability, renderer/world/NPC integration, unsafe stubs, and missing release evidence; record only executable proof as ready.
- [ ] Define and implement a bounded document-ingest contract for user-provided DOCX, PDF, and archive inputs, including type/size/entry limits, extraction quarantine, provenance, and prompt-context limits; no untrusted archive content may execute.
- [x] Define and validate a typed, allowlisted AI tool-command graph with at most 16 dependency-ordered nodes; Coba/Aries plans are dry-run by default, require typed build/test evidence before plan issuance, and cannot obtain direct economy/ban/runtime authority. Release and AddressSanitizer smoke pass. A sandboxed executor, operation budgets, and model-invocation evidence remain separate gates.
- [x] Provision and verify Android SDK command-line tools, JDK 17, platform-tools, API 36 platform, build-tools 36.0.0, NDK r29 (`29.0.14206865`), and CMake 3.22.1 in the sandbox. Canonical preflight passes toolchain checks and blocks packaging due to legacy native engine root; debug APK, signed AAB, device evidence, and Play Store release remain separate gates.
- [ ] Migrate Android JNI/CMake wrapper from the legacy `engine/Source/NeoEngine` path to an explicit canonical `Source/NeoEngine` source contract, pin API 36/NDK r29, and prove native debug build plus emulator and physical-device lifecycle/render/input/audio smokes before considering any APK.
- [ ] Complete Android release gates only after the engine/backend gates: reproducible debug and release artifacts, secure signing key handling, AAB verification, privacy/data safety/policy review, store metadata, Play Console test tracks, pre-launch/device evidence, Android vitals/rollback operations, and explicit upload confirmation.
- [ ] Keep application, website, dashboard, and graphical engine-control work blocked until every release gate has executable evidence; do not treat AI planning, document ingestion, or platform preflight as release readiness.
- [x] Bind Farm character/building/NPC entities to SceneWorld and synchronize transforms after deterministic world changes, with bounded lifecycle and Release/AddressSanitizer smoke evidence. This is not yet a complete renderer scene graph or player client.
- [x] Audit and classify canonical support for skeletal rigs, characters, buildings, scenes, items, narrative, monsters, NPC behavior/movement, collision materials, and authoring serialization; legacy headers/catalog labels remain excluded from active capability claims.
- [x] Implement a bounded, versioned `AuthoringCatalog` for skeleton definitions, characters, buildings, items, narrative entries, NPC/monster actors, materials, and scenes with deterministic validation, atomic deserialize, Release/AddressSanitizer smoke, and no generated player/customer content.
- [x] Implement bounded deterministic NPC/monster movement and behavior state using typed behavior plus material contact properties; Release/AddressSanitizer demonstrates SceneWorld transform propagation and fail-closed invalid data handling. Contact material is not yet wired into XPBD collision solving.
- [x] Define and test self-repair, self-learning, and self-deploy boundaries: diagnosis/plan/learning evaluation are dry-run only; regression requires verified evidence and approval; deployment, source/runtime/authority/economy/ban/credential mutation are rejected. A sandboxed executor remains a separate gate.
- [ ] Retain application, website, dashboard, APK/AAB, and publication blocks until all authoring, gameplay, authority, security, platform, and launch gates have executable production evidence.
- [x] Replace bounded actor drift with deterministic grid navigation for character, NPC, and monster: obstacles, goals, route reconstruction, blocked/unreachable handling, scene propagation, and Release/AddressSanitizer evidence.
- [x] Implement bounded deterministic world authoring with terrain biome cells, forest/tree placement, runtime building placement constraints, occupancy, serialization, and SceneWorld bindings; no procedural output bypasses capacity or seed validation. This remains non-rendered authoring data, not an open-world game.
- [x] Integrate a non-UI game vertical-slice world that combines world generation, navigation, authored characters/NPC/monsters/buildings/items/narrative, authority-safe action boundaries, and renderer-facing scene state; Release/AddressSanitizer pass. This is not a released game client.
- [x] Add an explicit-command manual-repair protocol for Coba/Aries that accepts only a typed repair request, returns an audited diagnosis and constrained patch/test proposal, and never applies code, deploys, or changes runtime/economy/ban/credential authority itself.
- [ ] Publish and maintain a canonical Unity/Godot-style readiness matrix for every checklist domain, mapping each item to active source and executable evidence; no catalog/header/scaffold may be marked ready without both.
- [ ] Implement priority game-light foundations after audit. Clock/pause/timer, multi-device action input/rebinding, generational object pooling, event/signal bus, and local/world dirty transform hierarchy now pass Release/AddressSanitizer; data-driven definitions, generic save/settings path, debug draw, real-device input, and integration contracts remain required.
- [ ] Implement renderer/gameplay gaps only after contracts are defined: sprite/mesh/camera/material/light/text paths, asset import/pak lifecycle, physics 2D collider/trigger/raycast/collision matrix, animation/tween state, and UI/mobile input. Do not claim 2D/3D engine parity until real presentation and device evidence pass.
- [ ] Expand prompt execution AI from plan issuance to a staged manual-approved operation protocol. The current runner proves prompt-ID confirmation, execution budgets, allowlisted inspect/build/test, and typed receipts while source mutation/deploy remain disabled; isolated workspaces, cancellation, diff review, rollback evidence, and an independently proven mutation executor are still required.
- [ ] Implement production platform/game gates separately: cloud multiplayer persistence/auth/TLS, secure telemetry/LiveOps, Android canonical bridge/device/debug APK/signed AAB, privacy/security, Play Console testing, and launch operations.
- [ ] Phase 1 runtime: extend tested RuntimeClock with bounded timers; extend input to mouse/touch/gamepad/action map/rebinding; add usable object pooling; add event/signal bus; and complete transform local/world/parent/dirty behavior with Release/AddressSanitizer evidence.
- [ ] Phase 2 renderer: build testable 2D/3D presentation primitives—camera, sprite sorting, mesh draw, texture/material/light/text contracts—and a graphical Farm vertical slice only after each renderer path has executable evidence.
- [ ] Phase 3 game interaction: build UI, tween, basic sprite/skeletal animation, Collider2D/Rigidbody2D/raycast/trigger/layer matrix, and a bounded 3D physics interface; no application or packaging claim before player-facing evidence exists.
- [ ] Phase 4 simulation: implement generic asset/save/settings paths, then extend Farm economic data toward 50+ commodity definitions, progressive land, bank tenor rules, NPC payroll, and government policy through authoritative, bounded, deterministic, anti-inject contracts. Advertising/IAP/bundle execution remains separate policy, provider, and receipt-authority work.
- [ ] Phase 5/6: prove a limited 3D farm vertical slice, then only pursue Android/debug APK/AAB/Play readiness after renderer, gameplay, authority, privacy/security, and operational gates have separate executable evidence.
- [x] Add explicit caller-triggered `Refresh` to texture, mesh, and material staging stores. It decodes/imports replacement registry bytes into temporary data, validates retained aggregate capacity, then replaces only an existing deque-backed resource's contents while preserving that resource object's address. Texture retains P6 PPM or BI_RGB BMP decoder provenance; mesh retains its original flat-normal choice; material retains its named-MTL selection. Release/AddressSanitizer smoke proves success after `ReplaceBytes`, pointer stability, accounting preservation, and atomic retention of old staged data for invalid PPM, invalid OBJ, or a missing named MTL material. Refresh does not traverse dependencies, rebind `SceneMeshAdapter` copies, invalidate caches, schedule work, or touch GPU resources.
- [x] Add explicit caller-triggered `SceneMeshAdapter::RefreshStaged` for an existing copy-on-register CPU instance. It constructs and validates a candidate instance before replacement, requires the same staged mesh asset identity and, for paired resources, the same material asset/name identity, then atomically updates CPU mesh/material copies and source hashes. Release/AddressSanitizer smoke proves a copied pre-refresh frame remains unchanged until rebind, successful green-to-red material rebind after mesh/material staging refresh, and unchanged refreshed frame after wrong mesh or wrong material identity is rejected. It does not observe registry changes itself, traverse dependencies, mutate other scene copies, schedule updates, upload GPU data, or provide background hot reload.
- [x] Add read-only `AssetRefreshDiagnostics`. For a changed registry asset it combines the deterministic transitive dependency plan with stale texture/mesh/material staging and stale SceneMeshAdapter source hashes into bounded caller-action entries (`RefreshTexture`, `RefreshMesh`, `RefreshMaterial`, `RebindSceneInstance`). It keeps the prior plan on a missing asset and never invokes refresh/rebind or mutates registry, staging, or scene state. Release/AddressSanitizer smoke proves an initially empty plan, then exactly `RefreshMesh` plus `RebindSceneInstance` after mesh-byte replacement, exactly `RefreshMaterial` plus `RebindSceneInstance` after material-byte replacement, and exactly `RefreshTexture` after standalone texture-byte replacement, with unchanged staged hashes and fail-closed missing-asset handling. Texture does not create a scene rebind action because `SceneMeshInstance` has no texture provenance. It is not a reload executor, file watcher, task scheduler, cache manager, GPU uploader, or automatic hot reload mechanism.
- [x] Upgrade `SceneMeshAdapter` texture handling to copy-on-register. A scene instance now owns a validated RGBA texture copy, its material pointer is rebound to that owned copy, and it records source texture asset/hash in append-stable deque storage. Direct staged mesh refresh validates texture identity before replacing the instance, while `AssetRefreshDiagnostics` reports a scene rebind when an instance's source texture becomes stale. Release/AddressSanitizer smoke proves a red texture remains rendered after staging refresh until explicit rebind, then becomes blue after rebind; a wrong texture identity is rejected while the blue frame remains. This still has no automatic refresh, file watching, dependency executor, material texture-map import, cache invalidation, GPU upload, or background hot reload.
- [x] Add `AssetRefreshExecutor`, a caller-invoked CPU action runner for a previously built refresh plan. It executes actions in supplied order, writes one bounded receipt per attempted action, and stops at the first failure; already completed earlier actions are retained and the executor never builds a plan, watches files, schedules work, or runs itself. Rebind requires staged sources to be current and validates scene provenance. Release/AddressSanitizer smoke proves explicit texture refresh followed by scene rebind changes a copied raster from red to blue with two successful receipts; a malformed replacement fails on the first texture action, produces one failed receipt, preserves the existing blue scene copy, and does not attempt rebind. No transaction/rollback across multiple actions, automatic invocation, cache manager, GPU upload, or hot reload loop exists.
- [x] Add structural read-only `AssetRefreshExecutor::Preflight`. It walks a supplied plan in order, emits bounded preflight receipts, and verifies staged resource presence/staleness plus whether earlier listed refresh actions make a later scene rebind structurally eligible. It does not decode/import replacement bytes, invoke refresh/rebind, mutate resource hashes, or promise that the later executor will succeed. Release/AddressSanitizer smoke proves a two-action texture refresh/rebind plan preflights successfully without changing the red staged hash, malformed replacement still structurally preflights but later fails during execution, and a missing texture action fails preflight with no mutation. It is not transactional validation, byte-format validation, a watcher, scheduler, automatic executor, or hot reload loop.
- [x] Add read-only `CanRefresh` probes for texture, mesh, and material staging. Each uses the retained decoder/import option and named-material selection to validate the current ready replacement bytes plus retained capacity without updating source hashes, payloads, or accounting. Executor preflight now invokes these probes, rejecting malformed payloads before execution with `ProbeFailed`. Release/AddressSanitizer smoke proves valid OBJ and named-MTL replacements probe true before refresh, malformed OBJ and missing named-MTL probe false while preserving staged state, and malformed PPM fails executor preflight before any receipt-bearing execute action. This validates individual candidates only; no cross-action atomic transaction, rollback, watcher, scheduler, cache management, GPU update, or background hot reload exists.
- [x] Bind every `AssetRefreshDiagnostics` action to the ready `AssetRegistry` content hash observed while building the plan. `AssetRefreshExecutor` preflight and execution both reject a zero, missing, unready, or mismatched expected hash as `PlanStale` before action mutation. Release/AddressSanitizer smoke proves a texture plan built for green bytes is rejected after the registry changes again to yellow, leaving the prior blue staged resource and scene copy untouched. This reduces plan-versus-registry time-of-check drift, but it is not locking, versioned registry transactions, rollback, concurrent synchronization, watcher-driven regeneration, GPU upload, or background hot reload.
- [x] Reject duplicate refresh-plan action targets as `PlanInvalid` before preflight or execution starts. Texture/mesh/material actions are keyed by action plus asset/material name; scene rebinds are keyed by entity. Release/AddressSanitizer smoke proves a duplicated texture-refresh action fails with no receipts and no mutation of the existing blue staged resource or scene copy. This guards malformed caller-built plans only; it is not broad dependency conflict analysis, ordering optimization, transaction rollback, concurrent synchronization, watcher-driven regeneration, GPU upload, or automatic hot reload.
- [x] Make `AssetRefreshExecutor::Execute` run the same preflight gate internally before any action loop. A malformed payload, stale hash, invalid plan, or failed structural/probe validation now returns the preflight error with no execution receipts and no resource/scene mutation even when the caller never called `Preflight` separately. Release/AddressSanitizer smoke proves malformed PPM is rejected by direct `Execute` as `ProbeFailed`, with an empty execution receipt list and preserved blue staged/scene state. This is an execution-entry gate, not a cross-action transaction, rollback system, lock, concurrent synchronization primitive, watcher, scheduler, cache manager, GPU uploader, or hot reload loop.
- [x] Add read-only direct and paired `SceneMeshAdapter::CanRefreshStaged` probes. They validate the same source identities and construct the same CPU candidate—including texture copy-on-register—that `RefreshStaged` would use, then discard it without replacing the instance. Executor preflight now invokes the appropriate probe before accepting a rebind action. Release/AddressSanitizer smoke proves a valid direct texture rebind probes true, a wrong texture identity probes false while the blue frame remains intact, and all 75 non-Vulkan smokes pass. It remains a per-instance candidate probe, not a cross-action transaction, rollback system, cache manager, GPU upload, watcher, or hot reload loop.
- [x] Replace the legacy CPU `Skinning` stub with bounded four-influence linear-blend position skinning. It accepts xyz-only CPU positions, up to 4096 vertices and 128 supplied bone matrices, validates finite matrices/positions, non-negative finite weights, valid bone indices, and per-vertex weight sum before constructing a candidate output; invalid input leaves the original positions intact. Release/AddressSanitizer smoke proves a two-bone weighted translation blend and failure atomicity for invalid index, weight sum, matrix, and weight count. This is not skeleton hierarchy solving, bind-pose construction, keyframe pose sampling, normal/tangent skinning, mesh-renderer integration, GPU skinning, animation blending, or production character animation.
- [x] Replace the legacy `Skeleton` vector container with a bounded 64-bone single-root hierarchy contract aligned to `AuthoringCatalog::kMaxBonesPerSkeleton`. `TryAddBone` validates name, uniqueness, root placement, and parent topological order before appending; all failures preserve the existing hierarchy and surface a typed `SkeletonError`. Release/AddressSanitizer smoke proves root→spine→head hierarchy, duplicate/second-root/forward-parent rejection, empty-hierarchy root enforcement, capacity, and atomic preservation. This is still not global-pose evaluation, bind/inverse-bind derivation, clip sampling, retargeting, blending, renderer integration, or GPU skinning.
- [x] Add bounded atomic `Skeleton::EvaluateGlobalBindPose`. It composes each finite local bind matrix from root through the already validated parent hierarchy into a temporary global-matrix vector, then swaps output only when all results are finite. Release/AddressSanitizer smoke proves root/spine/head translations compose to `(1,2,3)` and an invalid NaN bind matrix or empty skeleton preserves the caller's prior output. This is bind-pose evaluation only: no animated local poses, inverse-bind derivation, animation clips, blending, retargeting, mesh palette, renderer bridge, or GPU upload exists.
- [x] Add bounded atomic affine `Skeleton::DeriveInverseBindPose`. It derives global bind matrices, rejects non-affine, non-finite, or singular transforms before writing any bone, then atomically stores inverse bind matrices for all bones. Release/AddressSanitizer smoke proves a head global bind translation `(1,2,3)` derives inverse translation `(-1,-2,-3)` and multiplies back to identity; singular bind matrices fail while the existing inverse remains untouched. This is static inverse-bind derivation only: no animated pose palette, mesh renderer integration, normal/tangent skinning, GPU palette/upload, clip playback, or character animation exists.
- [x] Add bounded atomic `Skeleton::EvaluateSkinningPalette` from caller-supplied local poses and derived inverse binds. It accepts exactly one finite affine local pose per existing bone, composes a parent-first global pose, computes `globalPose × inverseBind` into a candidate palette, and replaces caller output only after all bones succeed. Release/AddressSanitizer smoke proves bind poses yield identity, a head local z change yields a one-unit skinning translation, and pose-count mismatch, non-affine pose, or unavailable inverse-bind state preserve prior output. It does not sample clips, blend, retarget, apply root motion, bind a mesh, skin normals/tangents, render, or upload to GPU.
- [x] Prove the bounded canonical local-pose-to-position CPU path in `skeleton_smoke`: the head local z change produces a palette translation of one, then the existing four-influence `Skinning::ApplySkinning` moves a 100%-head-influenced vertex from `(1,2,3)` to `(1,2,4)`. An invalid skinning bone index preserves the already-skinned vertex output. This smoke evidence is not a mesh component, clip player, blend/retarget/root-motion system, normal/tangent path, renderer binding, or GPU skinning implementation.
- [x] Add bounded atomic `Skinning::ApplySkinningWithNormals` for separate xyz position and normal buffers. It uses the same four influences but requires finite affine matrices with invertible linear 3×3 components, applies inverse-transpose normal transforms, and normalizes the blended normal before swapping both candidate buffers. Release/AddressSanitizer smoke proves a non-uniform x2 scale plus x3 translation transforms position `(1,1,0)` to `(5,1,0)` and normal `(1,1,0)` to approximately `(0.447214,0.894427,0)`; malformed normal count, zero normal, singular, and projective matrices preserve prior output. Tangents, interleaved mesh vertices, clip/blend pose input, renderer binding, and GPU skinning remain open.
- [x] Add bounded caller-invoked `SkeletalPoseClip`: at most 64 complete bone tracks and 128 finite TRS keyframes per track, time-zero start required, linear translation/scale interpolation, normalized quaternion interpolation, and atomic local-affine-pose output. Release/AddressSanitizer `skeletal_pose_clip_smoke` proves a half-time root translation plus 90-degree head rotation, negative-time/incomplete-clip preservation, and a full clip-to-bind/inverse/palette/100%-head CPU-position path from `(0,2,3)` to `(1,2,3)`. There is no clip loop/playback clock, blend/additive layer, retargeting, root motion, mesh component, renderer binding, or GPU implementation.
- [x] Add caller-invoked `SkeletalPoseClip::SampleLooped`: it requires a complete clip with a positive maximum track duration, wraps time by that duration, and delegates to the existing atomic sample path. Release/AddressSanitizer smoke proves `t=1.5` wraps to the same root translation and head rotation as `t=0.5`; static zero-duration clips reject loop sampling while retaining caller output. There is still no playback clock/state, automatic ticking, blend/additive layer, retargeting, root motion, mesh component, renderer binding, or GPU implementation.
- [x] Replace `SkeletalPosePlayer` non-owning clip reference with an owned complete `SkeletalPoseClip` snapshot at bind. Bind validates the candidate snapshot before replacing player state, so invalid/static-loop binding preserves the prior player; source clip destruction or mutation cannot dangle the player. Release/AddressSanitizer smoke scopes an ephemeral source clip, then advances the player snapshot to prove `(0→4)` source translation still samples x=2 at 0.5 seconds after source destruction. No NeoRuntime tick ownership, autoplay, blend/additive layer, retargeting, root motion, renderer binding, or GPU implementation exists.
- [x] Extend `skeletal_pose_clip_smoke` through `Skinning::ApplySkinningWithNormals`: the same half-time z-rotation palette which moves a 100%-head vertex `(0,2,3)` to `(1,2,3)` also rotates its normal `(1,0,0)` to `(0,1,0)`. A zero normal fails closed and preserves the prior position/normal outputs. This is CPU xyz normal evidence only; tangents, mesh binding, renderer/GPU skinning, blend/retarget/root motion, and runtime tick ownership remain open.
- [x] Add bounded atomic `SkeletalPoseClip::SampleBlended` for two compatible complete clips. It samples each clip TRS at caller times, linearly blends translation/scale, normalized-lerps rotation, then swaps all local matrices only on full success. Release/AddressSanitizer smoke proves 50% root x blend `(1,4)→2.5`, head rotation `(90°,0°)→45°`, and output preservation for factor outside `[0,1]` or incompatible bone count. This is one two-clip primitive—not a parameterized blend tree, state machine, transition system, additive layer, retargeting, root motion, renderer, or GPU animation path.
- [x] Add canonical `SkeletalAnimationController`, a snapshot-owned caller-invoked bridge from `SkeletalPosePlayer` to copied `Skeleton` inverse-bind palette output. It validates matching clip/bone count and derives inverse bind before replacing controller configuration; `Advance` copies player state, samples local pose, evaluates palette candidate, then atomically commits player/palette only on success. Release/AddressSanitizer smoke proves a three-bone root translation yields head palette x=1 at 0.5 seconds and x=2 at terminal, pause and speed-two behavior, invalid-delta preservation, and failed two-track reinitialization retaining prior controller state. It has no NeoRuntime tick ownership, blend-tree parameter wiring, retargeting, root motion, mesh/renderer binding, or GPU path.
- [x] Add `SkeletalAnimationController::AdvanceAndSkin`, which uses candidate player/local-pose/palette/position/normal buffers and commits them together only after `ApplySkinningWithNormals` succeeds. Release/AddressSanitizer smoke proves a 100%-head vertex moves `(0,2,3)→(1,2,3)` at `t=0.5` through controller palette and that an invalid bone index preserves controller time plus position/normal buffers. This is not a mesh component, tangent path, renderer integration, GPU skinning, or NeoRuntime-owned animation system.
- [x] Add clamp-only `SkeletalAnimationController::AdvanceWithRootMotion` translation extraction. It retains the prior root local translation, advances player/local pose/palette as candidates, derives the candidate root delta, and commits controller time/palette/root state only on success. Release/AddressSanitizer smoke proves x deltas `1,0,1,0` across half-time, pause, speed-two-to-terminal, and terminal clamp, preserves state on invalid delta, and explicitly rejects loop root motion. Root rotation, loop accumulation, navigation/physics integration, entity transforms, NeoRuntime tick ownership, renderer, and GPU remain open.
- [x] Add clamp-only `SkeletalAnimationController::AdvanceApplyRootMotion` to apply root translation into a SceneWorld entity local transform through a full candidate world snapshot. It commits world, player time, root state, and palette together only when entity lookup and SceneWorld transform validation succeed. Release/AddressSanitizer smoke proves actor local x moves `10→11` at 0.5 seconds, while an invalid entity preserves the existing world transform and controller time. Navigation, collision/physics authority, root rotation/loop accumulation, automatic tick, mesh rendering, and GPU remain open.
- [x] Extend `SkeletalAnimationController::AdvanceWithRootMotion` with bounded loop accumulation. Initialization captures root start/end translations and cycle duration; a loop advance decomposes delta into prior-to-end, up to 127 full cycles, and start-to-current segments before atomic commit. Release/AddressSanitizer smoke proves one wrap produces x=3 and a multi-wrap produces x=6 while preserving wrapped player time. Root rotation, SceneWorld application of looped root motion, navigation/physics authority, automatic tick, mesh rendering, and GPU remain open.
- [x] Extend `SkeletalAnimationController::AdvanceApplyRootMotion` to apply the same bounded loop delta through a candidate SceneWorld. Release/AddressSanitizer smoke proves an actor local x moves `10→13` after one wrapped advance and `13→19` after multi-wrap, while player time remains wrapped at 0.5 seconds. Root rotation, navigation/physics authority, automatic tick, mesh rendering, and GPU remain open.
- [x] Correct `AdvanceApplyRootMotion` palette semantics to be in-place: before deriving its candidate palette, it subtracts the animated root translation relative to the clip start from local root pose, so the SceneWorld entity owns the root displacement and skinning does not double-count it. Release/AddressSanitizer smoke proves palette head x remains zero while clamp actor moves `10→11` and loop actor moves `10→13→19`. Root rotation, navigation/physics authority, automatic tick, mesh rendering, and GPU remain open.
- [ ] Define and validate one transform-authority contract per SceneWorld entity before combining `GridRouteFollower` with `SkeletalAnimationController` root motion. The current follower writes world transforms through `KinematicMotionController`, while root motion also writes local transforms; direct composition would double-write and drift. A future adapter must make route navigation intent-only or otherwise provide explicit ownership, replan/conflict behavior, and Release/AddressSanitizer smoke coverage before NPC movement integration can be claimed.
- [x] Make `GridRouteFollower::Step` atomic across follower state, SceneWorld transform, and KinematicMotionController by executing a complete candidate copy before commit. Release/AddressSanitizer smoke proves a route whose later target is blocked leaves position at start, retains replan eligibility only after blocked-target failure, and proceeds to exact goal after unblocking; `runtime_route_motion_smoke` still rejects replan before a route starts and passes its manual-replan path. This does not resolve transform authority between route motion and skeletal root motion, nor add avoidance, collision, steering, NPC behavior, prediction, or multiplayer authority.
- [x] Add bounded per-frame `MovementAuthorityGate` and guarded overloads for `GridRouteFollower` and `SkeletalAnimationController` root-motion application. A guarded route claim rejects a competing skeletal claim before world/player mutation; same-authority access remains valid and `BeginFrame` permits the other authority next frame. Release/AddressSanitizer `movement_authority_smoke` proves invalid entity rejection, route success, same-frame route-versus-skeletal conflict with time/transform preservation, frame reset, and opposite conflict. NeoRuntime orchestration, route-to-animation adaptation, NPC behavior, steering, avoidance, collision/physics, prediction, and multiplayer authority remain open.
- [x] Bind `MovementAuthorityGate` to `NeoRuntime` lifecycle for its existing opt-in route motion: the runtime owns the gate, calls `BeginFrame` on each unpaused tick before movement, exposes it for explicit coordination, and routes `GridRouteFollower` through `StepGuarded`. Release/AddressSanitizer `runtime_route_motion_smoke` proves active route ticks reject a competing skeletal claim, while the following goal-stable tick resets the claim and permits it. No runtime-owned skeletal controller, animation-route adapter, NPC behavior, steering, avoidance, collision/physics, prediction, or multiplayer authority exists.
- [x] Specify the route-intent / single-transform-writer seam in `route_intent_single_writer_contract.md` before any route-to-skeletal integration. The contract forbids direct kinematic/root-motion delta blending, preserves fail-closed candidate commit semantics, limits the first stage to planar translation, and defines active route, reached-goal, blocked/replan, pause, stale-intent, clip-mismatch, and authority-conflict smoke requirements. This is design only; no C++ adapter or runtime integration was added.
- [x] Implement the bounded value-based `RouteIntent`/receipt seam in `GridRouteFollower`: `PeekIntent` validates and returns one next-cell snapshot without world/cursor/controller mutation; `CommitIntent` validates a post-writer receipt, route revision/cursor/entity/target/obstacle, and current segment position before advancing at most one cell only on arrival. `grid_route_intent_smoke` passes in Release/AddressSanitizer for initial no-write, invalid receipt, partial progress, blocked target with caller-output preservation, stale/tampered intent rejection, and exact arrival. No skeletal adapter, transform writer, or NeoRuntime path was added.
- [x] Add bounded `RouteRootMotionAdapter` as the sole transform writer for a valid planar `RouteIntent`. It copies follower/controller/world/authority/palette candidates, invokes only guarded skeletal root motion, rejects non-planar or wrong-axis/direction candidate deltas, then commits the route receipt only after actual arrival validation. Release/AddressSanitizer `route_root_motion_adapter_smoke` proves partial and exact route progress with skeletal-only writes, direction-mismatch and competing `KinematicRoute` conflict preservation, and blocked-target rejection. It has no root rotation, kinematic fallback/delta blending, automatic replanning, NPC behavior, collision/physics, prediction, multiplayer, or NeoRuntime ownership.
- [x] Specify opt-in `NeoRuntime` skeletal-route lifecycle in `route_root_motion_runtime_lifecycle_contract.md`: kinematic and skeletal static-route modes are exclusive; skeleton/clip data must be snapshot-owned and validated; the first runtime mode is one clamp, one straight segment, and an asserted clip direction; paused/goal/replan/failure/shutdown state requirements and a dedicated smoke matrix are recorded. This is documentation only and does not alter runtime source.
- [x] Add an opt-in `NeoRuntime` lifecycle for a single straight clamp `RouteRootMotionAdapter` route. Skeletal and kinematic route config are exclusive; runtime snapshots/validates the skeleton and clip, ticks root motion only while unpaused, resets the authority gate before work, preserves pause, keeps goal ticks stable, rejects skeletal replan explicitly, and clears the controller accessor at shutdown. Release/AddressSanitizer `runtime_route_root_motion_smoke` proves config rejection, partial/arrival progress, pause, same-frame conflict then next goal-tick reset, replan rejection, shutdown, direction mismatch, and next-target block preservation. This does not replace static kinematic route motion or create actor/NPC automatic locomotion.
- [ ] Extend skeletal route runtime beyond one straight clamp segment only after bounded clip-direction metadata, loop/cycle-distance policy, multi-segment transition behavior, invalid clip/skeleton/playback smoke coverage, and a replan contract that preserves controller/palette/world/cursor atomically are separately specified and tested. Root rotation, kinematic fallback/delta blending, NPC behavior, collision/physics, prediction, multiplayer, and renderer binding remain out of scope.
- [x] Specify `skeletal_locomotion_metadata_contract.md` for future cardinal multi-segment skeletal routes. It requires at most four snapshot-owned clips, exact zero-start/one-cell root displacement validation per cardinal direction, no root rotation or correction, candidate-only transition at arrived cells, and preflight of all replacement-route directions. The document defines Release/AddressSanitizer smoke cases; no metadata type, multi-segment runtime code, or replan expansion was added.
- [x] Add isolated `SkeletalLocomotionMetadata::ValidateCardinalOneCell`, which samples a copied complete clip at zero and its bounded positive duration, then validates zero root start plus an exact one-cell planar cardinal terminal without mutating caller clip state. Release/AddressSanitizer smoke accepts +X and rejects wrong requested direction, nonzero start, diagonal terminal, and duration above one second. It is a validator only: no metadata registry, multi-segment controller transition, runtime config expansion, replan expansion, root rotation, or transform writer was added.
- [x] Add isolated `SkeletalLocomotionRegistry` with at most four snapshot cardinal clips. Configuration preflights every clip with `ValidateCardinalOneCell`, rejects empty/oversized/invalid/duplicate direction input, and preserves the prior registry on rejection; Release/AddressSanitizer smoke covers a valid lookup and duplicate rejection. It does not select a runtime controller, transition route segments, or alter the one-segment runtime mode.
- [x] Freeze feature scope at the validated one-segment skeletal-route baseline; defer the existing multi-segment runtime item and add no new locomotion, rendering, physics, networking, AI, or deployment feature until explicitly authorized.
- [x] Consolidate canonical contracts, readiness/audit documents, TODO status, and a repository build/test guide so the canonical C++ project can be stored in GitHub with reproducible Release and AddressSanitizer smoke commands. `README.md`, `docs/README.md`, and `GITHUB_PREPARATION.md` now state the canonical source/test paths, explicit readiness boundary, direct-smoke commands, and staging whitelist.
- [x] Re-run the complete non-Vulkan smoke suite in Release and AddressSanitizer with `detect_leaks=1`, then perform scoped Git hygiene checks before GitHub storage preparation. The stabilization baseline passed 85/85 eligible smokes in both configurations.
- [x] Audit the user's GitHub repositories and the full FauzanEngine workspace, recommend an existing versus new target repository, then prepare an explicit reviewed staging manifest. The materially outdated Devin-based repository was rejected and the user confirmed the new public target and initial commit/push operation.
- [x] Compare the canonical workspace with the existing `Simulasi-Ekonomi/FauzanEngine` repository and its default branch, inventory all project material requested for storage, document generated/private exclusions, and prepare a reviewed branch/commit plan before any remote write. The comparison confirmed that the old repository could not serve as this complete-workspace archive.
- [ ] Use `GITHUB_FULL_PROJECT_MANIFEST.md` to stage all reviewable FauzanEngine code, tests, contracts, docs, tools, and legacy source to a new backup branch from `origin/main`, while retaining generated/private/local exclusions. Obtain explicit confirmation for the exact branch, staged diff, commit, and public remote push first.
- [x] Replace the outdated Devin-repository target with the new public `Simulasi-Ekonomi/FauzanEngine2-` repository. The full-project manifest and GitHub preparation guide describe a clean initial `main` branch, and the user explicitly confirmed the commit/push operation.
- [x] Verify the user-created empty public `Simulasi-Ekonomi/FauzanEngine2-` target, then prepare its reviewed `main` initial commit from `GITHUB_FULL_PROJECT_MANIFEST.md`. The reviewed staging tree contained 2,320 files before commit.
- [x] Verify the actual user-created empty public `Simulasi-Ekonomi/FauzanEngine2-` target shown in the GitHub screenshot, then update the manifest and initial-commit plan to that exact repository name before any remote operation.
- [x] Exclude all nested `.env` files and resolve the token-pattern match detected in the full-project staging candidate before public GitHub commit/push. The filename-only token scan passed and no credential-bearing file was staged.
- [x] Push the clean full-workspace initial archive commit `9a39c9424c3cc79b350b0eb5b1e0cb89efe4288d` to `https://github.com/Simulasi-Ekonomi/FauzanEngine2-.git` on `main`; read-only `ls-remote` verification matched local and remote SHA and the staging worktree was clean.
- [x] Perform an evidence-based FauzanEngine readiness audit across active CMake source, smoke targets, runtime contracts, renderer/presentation, assets, platform paths, authority/networking, and legacy/stub boundaries; publish a traceable readiness matrix and prioritized no-feature-expansion backlog before selecting the next implementation phase. The audit is recorded in `docs/READINESS_AUDIT_2026-08-24.md`; current Release and ASAN broad non-Vulkan smoke evidence is 85/85 in both configurations.
- [ ] P0 audit follow-up: establish one explicit active-source manifest, classify every C++ file outside canonical CMake as migrate/deprecate/exclude, remove the duplicate `Runtime/AssetRegistry.cpp` CMake entry, and reconcile conflicting/historical readiness documents without changing engine behavior.
- [ ] P1 audit follow-up: define and smoke-test a single `NeoRuntime` frame-composition contract for Farm from input snapshot through simulation, SceneWorld snapshot, render command list, audio/event output, and explicit error/ownership boundaries before integrating more isolated subsystems.
- [ ] P2 audit follow-up: build one bounded runtime-owned Farm asset-to-scene path with manifest/hash validation and explicit resource ownership, then connect it to a selected presentation backend only after the P1 contract is approved.
- [x] Verify the local full FauzanEngine workspace and the GitHub staging manifest against the Hermes report of the original mobile-local project, including editor/UI/backend/Three.js source and XPBD benchmark provenance; determine whether a re-upload is actually required without overwriting the verified baseline. The original `/home/ubuntu/upload/FauzanEngine.rar` remains present; 1,443 of 1,444 reviewable source files are present in the workspace, while the 32 staging omissions are generated CMake build artifacts excluded intentionally. No re-upload is required.
- [x] Create and execute a source-consolidation manifest that preserves the archived editor, backend, agent, Android, legacy engine, and validated C++ runtime source; classify every candidate integration as retain, adapt, replace-stub, or exclude, then establish a tested editor-to-runtime scene/asset contract without bulk overwrite. `docs/SOURCE_CONSOLIDATION_MANIFEST.md` and `docs/SCENE_DOCUMENT_V1.md` define the boundary; `EditorSceneDocumentAdapter`, versioned backend SceneDocument API, and editor client sync were added. Backend tests, editor production build, and 86/86 Release plus 86/86 ASAN non-Vulkan smoke suites passed.
- [x] P0 consolidation follow-up: replace the in-memory backend SceneDocument store with a selected durable authoring repository that preserves revision/checksum semantics, supports migration, and remains separate from player/game authority. The backend now uses a JSON store selected by `NEOENGINE_AUTHORING_STORE_PATH`, atomically replaces its file, validates checksum/revision on reload, and is covered by backend unit/HTTP tests.
- [x] P1 consolidation follow-up: implement an explicit local-only, approval-gated runtime bridge that passes a validated SceneDocument to `EditorSceneDocumentAdapter` and returns structured candidate-load receipts; do not expose the bridge as a public game-control endpoint. `LocalAuthoringBridge` validates the bounded `NAB1` handoff, approval, asset registry references, hierarchy, and atomic scene load; backend serializer and C++ smoke share the checked digest `5832217868230341815`.
- [ ] P2 consolidation follow-up: bind supported SceneDocument asset references to the canonical `AssetRegistry`/`SceneMeshAdapter`, then capability-gate editor Build/Publish UI until a real packaging pipeline has evidence.
- [x] Implement a local-only, approval-gated SceneDocument bridge that reads the bounded authoring handoff, validates the declared AssetRegistry references, invokes the canonical `EditorSceneDocumentAdapter` against a candidate `SceneWorld`, and returns a structured receipt without exposing player/game-control authority. Bridge Release/ASAN smoke passed; full broad suites are now 87/87 in each configuration and backend serializer tests passed.
- [x] Freeze all feature development and reconcile the complete original local FauzanEngine source tree against `Simulasi-Ekonomi/FauzanEngine2-`; archive every reviewable original editor, backend, engine, test, Android/JNI, asset, agent, tool, and configuration source file that is missing from GitHub, while excluding only generated/dependency/private/credential/cache/binary artifacts under a documented manifest. Commit `147e533832b3dc7324e9e874af8d1a2b9df6770a` pushed the final source/configuration gaps; the staged comparison reports zero unexpected reviewable gaps and records the 59 documented artifact/legacy residuals.
- [x] Reconcile the Hermes scan that compared an outdated `1281664` clone with the current GitHub `main`; verify every claimed missing editor/backend/agent/Android/source path against the current remote tree and archive any genuine original-source omission without adding new engine features. The scan clone was behind current `main`; direct `origin/main` checks found the claimed source paths present, with no genuine original-source omission. Reconciliation is published in commit `a0cdb646f26d1f7d85db030dce04bcda1f0a9ddc`.
- [x] Audit whether Coba_Memory/Coba_Obsidian_Vault are required by source or APK paths, define a safe reproducible data boundary, and publish a verifiable readiness assessment for FauzanEngine2 against a personal Unreal-like engine target without creating new features. Source scan found no vault dependency; Android preflight is blocked by unavailable SDK/JDK/NDK/CMake in this sandbox; the 12 mandatory release gates remain 0/12 passed. See `docs/VAULT_APK_ENGINE_READINESS_ASSESSMENT_2026-08-24.md`.
- [x] Phase 1.1: Produce one canonical active-source and legacy/stub inventory covering C++, editor, backend, Android/JNI, agent code, asset/content tooling, and all smoke/build targets; map every advertised engine capability to executable evidence or an explicit gap. See `docs/PHASE_1_ENGINE_CODEBASE_AUDIT.md`; baseline revalidation is 87/87 Release and 87/87 ASAN non-Vulkan.
- [x] Phase 1.2: Reconcile duplicate, disconnected, placeholder, and generated source paths into retain/adapt/replace/exclude decisions without mass-overwriting reviewed source. The audit classifies canonical/legacy/editor/backend/Android/tool/vault roots and records the 207 differing canonical-versus-legacy source paths.
- [x] Phase 1.3: Establish a versioned engine capability contract and acceptance matrix for 2D, 3D, sprites, skeletal assets, scenes, buildings, items, unit attributes, NPCs, monsters, physics, rendering, AI, packaging, and release operations. Initial acceptance contract is recorded in `docs/PHASE_1_ENGINE_CODEBASE_AUDIT.md`; none of the release gates are thereby passed.
- [ ] Phase 2.1: Define one canonical content schema for projects, scenes, prefabs, tilemaps, sprites, meshes, skeletons, materials, buildings, items, units, quests, NPCs, monsters, and game rules with migrations and deterministic validation.
- [ ] Phase 2.2: Complete a bounded asset pipeline from imported source bytes through content hash, dependency validation, registry/staging, scene references, reload diagnostics, and renderer-ready resources for supported 2D/3D asset types.
- [ ] Phase 2.3: Connect editor authoring data to the canonical runtime scene and asset contracts with import/export receipts, rollback-safe candidate loads, and end-to-end smoke coverage.
- [x] Phase 2.3a: Bind validated SceneDocument mesh actors to canonical AssetRegistry, mesh/material staging, SceneWorld entities, and SceneMeshAdapter candidate instances. `EditorSceneMeshBinder` and its smoke prove explicit material mapping, staged render output, rejection preservation, 88/88 Release, and 88/88 ASAN broad suites.
- [ ] Phase 2.3b: Evolve SceneDocument/editor/backend schema with versioned mesh-material-texture bindings, migrations, import/export receipts, and explicit refresh/rebind semantics so caller-supplied mapping is no longer the only bridge.
- [x] Phase 2.3b.1: Evolve canonical SceneDocument, backend persistence, local serializer/bridge, and editor serializer to v2 mesh-material-texture fields while preserving NAB1/v1 compatibility. `EditorSceneMeshBinder::BindDocumentAssets` stages bounded OBJ/MTL/PPM/BMP references; backend 9/9 and broad 88/88 Release plus 88/88 ASAN evidence passed.
- [ ] Phase 2.3b.2: Revalidate editor TypeScript production build after resolving the reviewed `esbuild` package-manager approval policy, then add editor save/import/export migration and receipt UI coverage for SceneDocument v2.
- [ ] Phase 2.3b.3: Add explicit texture/material refresh-rebind receipts and migration tests for supported asset formats; do not silently reload live instances or accept unsupported asset formats.
- [x] Phase 2.1a: Bind canonical AuthoringCatalog building and actor placements to validated mesh/material/texture assets in SceneMeshAdapter so authored gameplay definitions can become visible runtime entities without duplicating scene ownership. `AuthoringCatalogVisualBinder` preserves AuthoringCatalog scene ownership, stages bounded OBJ/MTL/PPM/BMP resources atomically, and is covered by 89/89 Release plus 89/89 ASAN broad suites.
- [x] Phase 2.2a: Bind SceneDocument v3 sprite actors to ready AssetRegistry texture references and canonical SpriteBatch with bounded size/layer/order/RGBA metadata, `NAB3` approval-gated local handoff, target-adapter atomic rejection, and dedicated smoke coverage. The narrow 2D textured-quad path is validated by 90/90 Release and 90/90 ASAN non-Vulkan smokes plus 11/11 backend tests; tilemaps, atlases, animation, GPU, alpha blending, WYSIWYG editor texture preview, and playable-game claims remain open.
- [x] Phase 3.1a: Add a bounded Farm sprite presentation adapter that stages explicitly registered texture assets for tile/building/NPC/player categories and renders a FarmWorld snapshot through SpriteBatch without replacing the existing deterministic colored FarmRenderAdapter. The adapter preserves the prior frame and caller staging store when asset validation/staging rejects; `farm_sprite_render_smoke` uses real FarmSystem/FarmWorldTool actions and proves deterministic rendering, missing-config rejection, stale/decode rejection, and preservation. Broad suites now pass 91/91 Release and 91/91 ASAN non-Vulkan; this remains a read-only presentation slice, not a playable Farm release.
- [x] Phase 3.2a: Add a bounded Farm player input bridge that reads named InputState actions, moves only through FarmWorldTool character state, and triggers exactly one selected till/plant/water/harvest action through existing FarmWorldTool authority APIs with smoke evidence. `farm_player_input_bridge_smoke` proves movement, three crop actions, conflicting input, boundary, and missing-action rejection without exposing economy/permit/top-up/ban/agent authority; broad suites pass 92/92 Release and 92/92 ASAN non-Vulkan. This is still local input proof, not an end-user playable loop.
- [x] Phase 3.3a: Add a bounded Farm runtime session that orchestrates existing input bridge, FarmWorldTool tick, and staged sprite rendering in one explicit frame lifecycle while preserving independent authority ownership. `farm_runtime_session_smoke` proves input→world→render ordering, tile state presentation change, zero-tick and missing-input rejection with frame/count preservation; broad suites pass 93/93 Release and 93/93 ASAN non-Vulkan. Render failure after a successful world tick does not claim game-state rollback, and this is not a player-facing executable.
- [x] Phase A.0: Audit the active canonical renderer path, runtime dependencies, current smoke evidence, and window/surface capability boundaries before selecting the next renderer increment. Finding: NeoRuntime was CPU-headless; Vulkan Present is a separate hidden clear-frame probe; the reusable Vulkan path remains offscreen; the existing SDL host bridge made an explicit CPU-surface seam the bounded next step.
- [x] Phase A.1: Strengthen a bounded active renderer seam selected by the audit, with explicit failure behavior and dedicated Release/ASAN smoke coverage. Implemented optional CPU `SoftwareRenderer` → SDL surface upload/present in NeoRuntime; default remains headless and requested presentation fails closed.
- [x] Phase A.2: Demonstrate the strengthened renderer in one graphical Farm or simple-scene vertical slice that can be inspected locally; do not claim end-user or production readiness without a validated present path. A finite native Farm surface demo now drives canonical NeoRuntime, presents to SDL, and writes an inspectable PPM artifact; it is not interactive or release-ready.
- [x] Phase A.2a: Build a bounded native Farm surface demo that initializes NeoRuntime with visible-or-hidden SDL presentation, advances a fixed number of Farm frames, emits deterministic frame evidence, exits cleanly, and has Release/ASAN smoke coverage; no persistent desktop loop or player-release claim. `farm_surface_demo_smoke` plus executable Release/ASAN evidence pass; broad suite is 95/95 Release and 95/95 ASAN non-Vulkan.
- [x] Phase A.3a: Audit and strengthen one bounded camera seam shared by orthographic sprite/Farm and perspective mesh rendering, with explicit projection validity and no gameplay transform writer changes. RenderCamera now has normalized forward/up orientation, rejects degenerate poses, and preserves orthographic/perspective clip contracts; `render_camera_smoke` plus full 95/95 Release and 95/95 ASAN non-Vulkan evidence pass.
- [x] Phase A.4a: Audit and strengthen one active SpriteBatch seam beyond existing layer/order sorting, with deterministic behavior, explicit rejection, and Release/ASAN smoke evidence while retaining bounded software-renderer semantics. Flat and textured sprites now compose RGBA source-over with tint; existing ordering/capacity/texture validation remains intact. `sprite_batch_smoke`, `software_renderer_smoke`, and broad suites pass 95/95 Release and 95/95 ASAN non-Vulkan.
- [x] Phase A.5a: Audit and strengthen one bounded MeshRenderer seam for material/texture/light output, retaining existing staged-resource validation and providing Release/ASAN smoke evidence before any 3D production claim. Near clipping and perspective reciprocal depth now use RenderCamera camera-space, so staged texture/material/light output remains correct for an oriented 3D camera; `mesh_renderer_smoke` plus 95/95 Release and 95/95 ASAN non-Vulkan evidence pass.
- [x] Phase A.6a: Strengthen one bounded basic-lighting seam in MeshRenderer, retaining deterministic CPU Lambert behavior and staged texture/material validation while adding explicit Release/ASAN evidence. Directional intensity `[0,8]` and material RGBA texture tint are now validated; mesh/editor/authoring visual smoke coverage plus full suites pass 95/95 Release and 95/95 ASAN non-Vulkan.
- [x] Phase A.7a: Make SpriteBatch flush fail atomically by preserving the caller framebuffer on projection/raster failure, with targeted Release/ASAN proof before claiming render recovery behavior. Candidate framebuffer commit is now all-or-nothing; `sprite_batch_smoke` and full suites pass 95/95 Release and 95/95 ASAN non-Vulkan.
- [x] Phase A.8a: Make MeshRenderer draw fail atomically by preserving the caller framebuffer on projection/raster failure, with targeted Release/ASAN proof and no changes to scene or asset authority. Candidate framebuffer commit is now all-or-nothing; `mesh_renderer_smoke` and full suites pass 95/95 Release and 95/95 ASAN non-Vulkan.
- [x] Phase A.9: Consolidate the renderer baseline, evidence, remaining gates, and explicitly unfulfilled capability boundaries before expanding renderer scope or advancing to another phase. Baseline document records 95/95 Release and 95/95 ASAN non-Vulkan evidence, current renderer seams, and open window/GPU/2D/3D/release gates.
- [x] Phase A.10a: Add a bounded SDL surface lifecycle API that pumps events, exposes close-request state, and keeps present failure explicit; prove hidden-surface behavior in Release/ASAN without claiming a complete desktop host. `software_surface_presenter_smoke` and full suites pass 95/95 Release and 95/95 ASAN non-Vulkan.
- [x] Phase A.11a: Build a finite native 3D mesh surface demo that uses canonical perspective camera, MeshRenderer texture/material/light, SDL surface lifecycle, and PPM artifact output; require Release/ASAN smoke evidence while excluding interactive-game and release claims. `mesh_surface_demo_smoke` validates invalid config rejection, hidden four-frame presentation receipt, non-background pixels/hash, and P6 artifact magic. Targeted Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 96/96 Release and 96/96 ASAN. The demo is finite-pose proof only: it is not an animation system, a persistent desktop host, playable 3D game, GPU path, APK, or release evidence.
- [x] Phase A.12a: Extend bounded MeshRenderer local camera-space clipping from near-only to all six frustum planes, treating fully outside triangles as successful culling while retaining atomic framebuffer rejection for genuine raster failure. `mesh_renderer_smoke` proves right/far culling, side-plane clipping, preserved near behavior, and a true `RasterFailed` atomic rejection; the finite mesh surface demo now exercises bounded per-frame pose changes. Targeted Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 96/96 Release and 96/96 ASAN. This is not scene-wide culling, animation system, GPU rendering, desktop/game/release proof.
- [x] Phase A.13a: Extend SpriteBatch local camera-space clipping from fail-on-any-outside-corner to bounded six-plane quad clipping, treating fully outside sprites as successful culling while retaining atomic framebuffer rejection for genuine raster failure. `sprite_batch_smoke` proves off-frustum culling, side crossing raster, alpha/tint/sorting preservation, and a true `DrawFailed` atomic rejection. Targeted Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 96/96 Release and 96/96 ASAN. This is not atlas/flipbook, animation, rotation, GPU batching, UI framework, desktop/game/release proof.
- [x] Phase A.14a: Add read-only SDL resize-event observation to SoftwareSurfacePresenter while keeping source/texture dimensions fixed and mismatch presentation fail-closed. `software_surface_presenter_smoke` proves injected size observation, subsequent present, close behavior, reset, and NeoRuntime hook; targeted Release/ASAN `detect_leaks=1` plus broad non-Vulkan suites pass 96/96 Release and 96/96 ASAN. This is not framebuffer resize, DPI/fullscreen/vsync, persistent desktop host, game, APK, or release proof.
- [x] Phase A.15a: Strengthen scene-wide 3D mesh culling with conservative bounding-sphere tests against the oriented camera frustum, preserving SceneMeshAdapter validation, MeshRenderer atomic draw, and existing authority boundaries. `scene_mesh_adapter_smoke` proves +X-camera visible/behind culling; Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 96/96. This is not occlusion culling, GPU rendering, or production readiness.
- [x] Phase A.16a: Expose a bounded reusable RenderCamera sphere-frustum query so oriented SceneMeshAdapter culling uses the same validated camera-space contract without duplicating plane equations; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase A.16a: Expose a bounded reusable RenderCamera sphere-frustum query so oriented SceneMeshAdapter culling uses the same validated camera-space contract without duplicating plane equations. `render_camera_smoke` and `scene_mesh_adapter_smoke` pass Release/ASAN `detect_leaks=1`; broad non-Vulkan suites pass 96/96. This is not hierarchy, occlusion, GPU, or production readiness.
- [x] Phase A.17a: Add bounded finite sprite quad rotation while preserving camera-space clipping, alpha/tint, stable ordering, and atomic flush semantics; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase A.17a: Add bounded finite sprite quad rotation while preserving camera-space clipping, alpha/tint, stable ordering, and atomic flush semantics. `sprite_batch_smoke` proves π/2 output change; Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 96/96. This is not animation, atlas, GPU batching, or production readiness.
- [x] Phase A.18a: Add camera-oriented sprite billboard geometry using validated RenderCamera basis while preserving local quad rotation, clipping, tint, alpha, stable ordering, and atomic flush semantics; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase A.18a: Add camera-oriented sprite billboard geometry using validated RenderCamera basis while preserving local quad rotation, clipping, tint, alpha, stable ordering, and atomic flush semantics. `sprite_batch_smoke` proves +X-camera billboard raster; Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 96/96. This is not animation, GPU batching, or production readiness.
- [x] Phase A.19a: Formalize existing default double-sided and opt-in `cullBackFaces` mesh material behavior with explicit smoke and documentation while preserving frustum clipping, depth, lighting, tint, texture, and atomic draw contracts. `mesh_renderer_smoke` proves default backward winding raster and opt-in backface culling; Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 96/96. This is not PBR, GPU rendering, or production readiness.
- [x] Phase A.20a: Build a finite native hybrid surface demo that combines a textured 3D mesh, camera billboard sprite, SoftwareSurfacePresenter lifecycle, and explicit PPM artifact; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase A.20a: Build a finite native hybrid surface demo that combines a textured 3D mesh, camera billboard sprite, SoftwareSurfacePresenter lifecycle, and explicit PPM artifact. `hybrid_surface_demo_smoke` rejects invalid frames and proves hidden three-frame receipt plus P6 artifact; targeted Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 97/97. This is not gameplay, editor, GPU, APK, or release readiness.
- [x] Phase A.21a: Add explicit SpriteBatch alpha depth-write policy so translucent sprite composition can preserve or avoid depth updates deliberately while retaining clipping, tint, sorting, and atomic flush; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase A.21a: Add explicit SpriteBatch alpha depth-write policy so translucent sprite composition can preserve or avoid depth updates deliberately while retaining clipping, tint, sorting, and atomic flush. `sprite_batch_smoke` proves transparent no-depth-write versus default depth-write behavior; Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 97/97. This is not general transparency sorting, GPU, or production readiness.
- [x] Phase A.22a: Audit and strengthen runtime scene composition so canonical staged mesh and sprite adapters can be rendered together through one bounded scene entry point, with deterministic ordering, camera validation, Release/ASAN smoke, and broad non-Vulkan evidence; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase A.22a: Audit and strengthen runtime scene composition so canonical staged mesh and sprite adapters can be rendered together through one bounded scene entry point, with deterministic mesh-first ordering, camera validation, Release/ASAN smoke, and broad non-Vulkan evidence. `scene_render_adapter_smoke` proves mesh-plus-sprite rendering and atomic preservation after sprite queue failure; broad suites pass 98/98 Release and 98/98 ASAN. This is not editor, gameplay, GPU, APK, or release readiness.
- [x] Phase A.23a: Extend staged scene sprite instances to carry local rotation, camera-billboard opt-in, and alpha depth-write policy into SpriteBatch so SceneRenderAdapter uses canonical sprite capabilities instead of silently dropping them; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase A.23a: Extend staged scene sprite instances to carry local rotation, camera-billboard opt-in, and alpha depth-write policy into SpriteBatch so SceneRenderAdapter uses canonical sprite capabilities instead of silently dropping them. `scene_render_adapter_smoke` proves +X-camera staged billboard; Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 98/98. This is not full scene/material or production readiness.
- [x] Phase B.1a: Audit and build a bounded editor-session foundation over SceneDocument that exposes deterministic hierarchy and inspector snapshots, fail-closed load/save, and an explicit viewport render seam using canonical scene adapters; completed with the evidence recorded in the duplicate historical entry below.
- [x] Phase B.1a: Audit and build a bounded editor-session foundation over SceneDocument that exposes deterministic hierarchy and inspector snapshots, fail-closed load/save, and an explicit viewport render seam using canonical scene adapters. `editor_scene_session_smoke` proves hierarchy, inspector, snapshot save, viewport, and atomic failed open; Release/ASAN `detect_leaks=1` and broad non-Vulkan suites pass 99/99. This is not a desktop editor, gameplay, APK, or release readiness.
- [x] Phase B.2a: Add transactional editor-session transform editing that validates a candidate SceneDocument through canonical load/bind paths, increments revision only on success, and preserves hierarchy/viewport state on failure. `editor_scene_session_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 99/99 Release serta 99/99 ASAN. Ini bukan desktop editor, undo/redo, collaboration, atau release readiness.
- [x] Phase B.2b: Add transactional editor-session reparenting through the canonical candidate document/load/bind path, increment revision only on success, and preserve the active hierarchy and viewport on missing/cyclic parent failure. `editor_scene_session_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 99/99 Release serta 99/99 ASAN. Ini bukan desktop editor, undo/redo, collaboration, atau release readiness.
- [x] Phase B.2c: Add transactional editor-session actor add/delete through the canonical candidate document/load/bind path, reject duplicate/invalid new actors and deletion of an actor with children, increment revision only on success, preserve active hierarchy/viewport on failure. `editor_scene_session_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 99/99 Release serta 99/99 ASAN. Ini bukan desktop editor, undo/redo, collaboration, atau release readiness.
- [x] Phase C.1a: Add a bounded scalar animation state machine over canonical AnimationTimeline with explicit states/transitions, deterministic linear blend, finite time validation, and no SceneWorld/movement-authority writes. `animation_state_machine_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 100/100 Release serta 100/100 ASAN. Ini bukan skeletal animation, root motion, NPC behavior, atau release readiness.
- [x] Phase C.1b: Add a bounded locomotion animation bridge that translates validated planar kinematic input into explicit idle/locomotion state-machine triggers without owning SceneWorld, RouteIntent, MovementAuthorityGate, or any transform write. `animation_locomotion_bridge_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 101/101 Release serta 101/101 ASAN. Ini bukan skeletal animation, root motion, NPC behavior, atau release readiness.
- [x] Phase D.1a: Add a bounded read-only gameplay raycast wrapper over active XPBD data that validates finite rays/masks, maps flat solver hit indices to EntityID, and preserves caller hit data on miss/invalid input. `gameplay_physics_query_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 102/102 Release serta 102/102 ASAN. Ini bukan 3D physics, full collider/rigidbody API, triggers, atau release readiness.
- [x] Phase D.1b: Add a bounded read-only gameplay overlap-circle wrapper over active XPBD data that validates finite shapes/masks, maps flat solver results to EntityID, bounds result count, and preserves caller output on invalid/mapping failure. `gameplay_physics_query_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 102/102 Release serta 102/102 ASAN. Ini bukan 3D physics, full collider/rigidbody API, triggers, atau release readiness.
- [x] Phase D.2a: Add a bounded gameplay circle-body builder that validates static/dynamic mass, finite pose/velocity, and positive radius before creating canonical ECS physics components, with explicit solver ownership remaining external. `gameplay_physics_body_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 103/103 Release serta 103/103 ASAN. Ini bukan full Rigidbody API, triggers, 3D physics, atau release readiness.
- [x] Phase E.1a: Add an atomic in-memory texture import transaction that validates candidate PPM/BMP bytes through canonical AssetRegistry readiness and TextureStagingStore before committing both registry and staging stores. `texture_import_pipeline_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 104/104 Release serta 104/104 ASAN. Ini bukan filesystem import, hot reload, GPU upload, general formats, atau release readiness.
- [x] Phase E.1b: Add an atomic in-memory OBJ mesh import transaction that validates candidate bytes through canonical AssetRegistry readiness and MeshStagingStore before committing both registry and staging stores. `mesh_import_pipeline_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 105/105 Release serta 105/105 ASAN. Ini bukan filesystem import, MTL/material import, hot reload, GPU upload, general formats, atau release readiness.
- [x] Vertical Slice E.2a: Add a finite asset-import-to-render surface demo that imports PPM/OBJ through canonical candidate pipelines, rasterizes staged resources through MeshRenderer, presents through the existing software surface seam, and writes an explicit PPM artifact. `asset_import_surface_demo_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 106/106 Release serta 106/106 ASAN. Ini bukan gameplay, persistent host, GPU runtime, atau release readiness.
- [x] Vertical Slice R.1a: Add a finite runtime slice that uses KinematicMotionController as the only transform writer, applies AnimationLocomotionBridge as read-only state triggering, queues a staged sprite from the canonical SceneWorld, and writes an explicit PPM artifact. `motion_animation_surface_demo_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 107/107 Release serta 107/107 ASAN. Ini bukan gameplay, NPC intelligence, persistent host, multiplayer, atau release readiness.
- [x] Vertical Slice R.1b: Add a bounded collision preflight that uses a read-only XPBD raycast to block/pass a planned kinematic step while only KinematicMotionController writes transforms. `kinematic_collision_preflight_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 108/108 Release serta 108/108 ASAN. Ini bukan collision response, body sync, 3D physics, navigation, multiplayer, atau release readiness.
- [x] Phase D.3a: Add a bounded read-only circle-trigger tracker over XPBD overlap queries that emits deterministic enter/exit EntityID deltas only after a successful query and preserves the prior active set on invalid/query failure. `gameplay_trigger_tracker_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 109/109 Release serta 109/109 ASAN. Ini bukan trigger callbacks, solver mutation, body sync, 3D physics, atau release readiness.
- [x] Vertical Slice R.2a: Add a bounded SceneWorld-to-ECS pose sync with validated unique bindings and candidate validation before writing canonical physics position components. `scene_physics_pose_sync_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 110/110 Release serta 110/110 ASAN. Ini bukan bidirectional sync, physics authority over SceneWorld, interpolation, 3D body sync, atau release readiness.
- [x] Vertical Slice R.2b: Add a finite integrated runtime demo that claims kinematic authority, moves a SceneWorld actor, mirrors pose to a canonical ECS circle body, steps XPBD externally, derives trigger enter/exit delta read-only, renders a staged sprite, and writes a PPM artifact. `trigger_runtime_surface_demo_smoke` lulus Release/ASAN `detect_leaks=1`, dan broad non-Vulkan lulus 111/111 Release serta 111/111 ASAN. Ini bukan gameplay, collision response, persistent host, multiplayer, atau release readiness.
- [x] Phase B.3a: Add a bounded deterministic in-memory SceneDocument codec that preserves version, scene ID, revision, actors, transforms, mesh/sprite asset fields, and sprite render fields; decode into a candidate document and preserve caller output on malformed/capacity data. `editor_scene_document_codec_smoke` passes Release and ASAN `detect_leaks=1` with deterministic 206-byte v3 round-trip plus rejection of truncation, magic/version/length/kind/non-finite/capacity/trailing data; final broad non-Vulkan evidence is 112/112 Release and 112/112 ASAN. It remains in-memory only: no filesystem projects, editor UI, asset readiness/import, hot reload, collaboration, or release readiness is claimed.
- [x] Phase B.3b: Add bounded EditorSceneSession byte open/save handoff through the canonical SceneDocument codec. `OpenBytes` decodes a local candidate then delegates to existing candidate `Open`; `SaveBytes` serializes only an opened in-memory snapshot. `editor_scene_session_codec_smoke` passes Release and ASAN `detect_leaks=1`, proving 191-byte sprite-session round trip, malformed-byte session/frame preservation, missing-asset `Open` rollback, and unopened SaveBytes output preservation; final broad non-Vulkan evidence is 113/113 Release and 113/113 ASAN. No filesystem persistence, undo/redo, editor UI, collaboration, asset import/hot reload, or release readiness is claimed.
- [x] Phase B.4a: Add bounded EditorSceneSession dirty-state tracking that distinguishes successfully opened/saved in-memory snapshots from successful transactional editor mutations. `editor_scene_session_dirty_smoke` passes Release and ASAN `detect_leaks=1`, proving clean open, dirty successful mutation, clean Save/SaveBytes, and dirty-state preservation across invalid transform, malformed codec bytes, and duplicate-actor Open failure; final broad non-Vulkan evidence is 114/114 Release and 114/114 ASAN. No filesystem persistence, undo/redo, desktop UI, collaboration, asset reload, or release readiness is claimed.
- [x] Phase B.4b: Add bounded EditorSceneSession revert-to-saved-snapshot that restores the last successfully opened/saved in-memory document through candidate Open and leaves the current dirty session unchanged when reversion fails. `editor_scene_session_revert_smoke` passes Release and ASAN `detect_leaks=1`, proving open-snapshot restore, Save-updated snapshot restore, invalidated-texture reversion failure with dirty transform preservation, and empty-session rejection; final broad non-Vulkan evidence is 115/115 Release and 115/115 ASAN. No undo/redo history, filesystem persistence, desktop UI, collaboration, asset hot reload, or release readiness is claimed.
- [x] Phase B.5a: Add bounded EditorSceneSession undo/redo history for successful transactional transform/reparent/add/delete mutations, restoring snapshots only through candidate Open. `editor_scene_session_history_smoke` passes Release and ASAN `detect_leaks=1`, proving undo/redo state, redo invalidation on a new mutation branch, asset-bind candidate failure preserving current session/history, and empty-history rejection; final broad non-Vulkan evidence is 116/116 Release and 116/116 ASAN. No filesystem persistence, desktop UI, collaboration, runtime authority, asset hot reload, or release readiness is claimed.
- [x] Phase C.3a: Add bounded frame-local animation scalar-to-sprite tint binding and connect it to the finite motion animation surface slice, proving visual animation output without SceneWorld, RouteIntent, MovementAuthorityGate, or transform writes. `animation_sprite_tint_binding_smoke` and the extended `motion_animation_surface_demo_smoke` pass Release and ASAN `detect_leaks=1`; the four-frame demo proves two locomotion tint frames while preserving kinematic sole transform write. Final broad non-Vulkan evidence is 117/117 Release and 117/117 ASAN. No GPU/skeletal/flipbook/production readiness claim is made.
- [x] Phase E.4a: Add a bounded candidate/commit in-memory MTL material import pipeline through AssetRegistry ready state into MaterialStagingStore, with full output/store/registry preservation on malformed material or stage failure. `material_import_pipeline_smoke` passes Release and ASAN `detect_leaks=1`, proving ready registry/staged RGBA/hash receipt linkage plus malformed-stage, duplicate, and invalid-request rollback; final broad non-Vulkan evidence is 118/118 Release and 118/118 ASAN. No filesystem, hot reload, GPU, or production pipeline claim is made.
- [x] Phase E.4b: Add a finite PPM/OBJ/MTL import-to-staged-mesh-plus-material-to-software-surface proof that uses the three candidate/commit import pipelines and existing staged scene adapter. `material_import_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving four rendered/presented frames, PPM artifact, non-background pixels, nonzero resource hashes, and staged MTL RGBA `FF66CC99`; final broad non-Vulkan evidence is 119/119 Release and 119/119 ASAN. No filesystem, live reload, GPU, or production readiness claim is made.
- [x] Phase D.5a: Make bounded GameplayPhysicsQuery overlap-circle output canonical EntityID-sorted after atomic index mapping, preserving output on mapping/shape/mask/capacity failures. The existing `gameplay_physics_query_smoke` passes Release and ASAN `detect_leaks=1`, now proving two-entity ascending overlap output alongside prior raycast and rollback boundaries; broad non-Vulkan remains 119/119 Release and 119/119 ASAN because no new root smoke was added. No solver, collision response, Rigidbody, or gameplay-transform claim is made.
- [x] Phase R.3a: Add a finite movement-authority → ScenePhysicsPoseSync → external XPBD trigger → trigger-only animation state/tint sprite surface proof, retaining KinematicMotionController as sole SceneWorld transform writer. `trigger_animation_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving one enter/exit, two active tint frames, final x=0.750, final idle sample, and PPM output; final broad non-Vulkan evidence is 120/120 Release and 120/120 ASAN. No multiplayer, collision response, GPU, or release readiness claim is made.
- [x] Phase R.4a: Add a bounded FarmRuntimeSession committed-frame receipt that records frame number and software framebuffer hash only after input/world/render success, preserving the prior receipt on rejected frames and without adding simulation, asset, authority, persistence, or host ownership. The extended `farm_runtime_session_smoke` passes Release and ASAN `detect_leaks=1`, proving committed frame/hash receipt plus invalid-tick and rejected-input preservation; broad non-Vulkan remains 128/128 Release and 128/128 ASAN because no root smoke was added. No persistent host, networking, save system, APK, or release claim is made.
- [x] Phase E.5a: Add bounded in-memory MaterialImportPipeline replacement/refresh transaction that candidate-replaces existing MTL bytes, validates/stages the named material, and commits registry/store/receipt together only on success. The existing `material_import_pipeline_smoke` passes Release and ASAN `detect_leaks=1`, proving changed hash/RGBA receipt after replacement plus malformed/missing replacement rollback; broad non-Vulkan remains 120/120 Release and 120/120 ASAN because no new root smoke was added. No filesystem watching, live scene rebinding, GPU hot reload, or production readiness claim is made.
- [x] Phase E.5b: Add a finite explicit material replacement → SceneMeshAdapter staged refresh → software surface proof that renders pre/post material hashes using caller-invoked refresh only. `material_refresh_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving 2 frames, pre/post framebuffer and material hash changes, RGBA `FF33CC33 → FFCC1A33`, and PPM output; final broad non-Vulkan evidence is 121/121 Release and 121/121 ASAN. No watcher, automatic live reload, GPU, or production readiness claim is made.
- [x] Phase D.6a: Add a finite KinematicCollisionPreflight surface proof that uses an XPBD snapshot raycast to block a requested move while retaining KinematicMotionController as sole SceneWorld transform writer. `kinematic_preflight_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving 3 frames, two delegated motions, one blocked request, final x=-0.250, and PPM output; final broad non-Vulkan evidence is 122/122 Release and 122/122 ASAN. No collision response, Rigidbody, navigation, GPU, or release readiness claim is made.
- [x] Phase D.6b: Add a bounded read-only KinematicCollisionPreflight probe that reports a clear or blocked requested planar move and returns the blocker hit without calling motion or mutating SceneWorld; retain caller output atomically on invalid/query failure. The extended `kinematic_collision_preflight_smoke` passes Release and ASAN `detect_leaks=1`, proving blocker receipt, no transform change during probe, invalid-input output preservation, blocked Step behavior, and later delegated clear motion; broad non-Vulkan remains 128/128 Release and 128/128 ASAN because no root smoke was added. No collision response, Rigidbody, navigation, GPU, or production physics claim is made.
- [x] Phase A.10a: Add bounded source-rectangle support to staged sprite rendering for CPU texture atlases, preserving default full-texture behavior and atomic SpriteBatch flush semantics. `sprite_atlas_source_rect_smoke` passes Release and ASAN `detect_leaks=1`, proving red/blue atlas frame sampling and invalid-rect rejection; final broad non-Vulkan evidence is 123/123 Release and 123/123 ASAN. No GPU renderer, editor UI, flipbook timeline, or production readiness claim is made.
- [x] Phase C.4a: Add a bounded deterministic flipbook frame selector over CPU atlas rectangles and prove frame progression output without SceneWorld, RouteIntent, MovementAuthorityGate, or transform writes. `flipbook_frame_selector_smoke` passes Release and ASAN `detect_leaks=1`, proving 2×2 endpoint/midpoint selection, invalid config, and NaN output preservation; final broad non-Vulkan evidence is 124/124 Release and 124/124 ASAN. No skeletal/GPU/production animation claim is made.
- [x] Phase C.4b: Connect bounded flipbook frame selection to staged sprite atlas output in a finite surface proof, preserving SceneWorld read-only rendering and all movement/authority boundaries. The extended `sprite_atlas_source_rect_smoke` passes Release and ASAN `detect_leaks=1`, proving selector-to-QueueFrame staged blue output and preserved transform; broad non-Vulkan remains 124/124 Release and 124/124 ASAN because no new root smoke was added. No skeletal/GPU/production animation claim is made.
- [x] Phase C.4c: Add a bounded finite flipbook surface sequence proof that selects multiple staged atlas frames deterministically across samples, renders explicit frame evidence, and proves SceneWorld transforms remain read-only. `flipbook_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving four rendered/presented/selected frames, visible final yellow frame, P6 artifact, sequence hash `7898515516691923083`, final hash `11835874581093820931`, and full transform preservation; final broad non-Vulkan evidence is 125/125 Release and 125/125 ASAN. No skeletal/GPU/production animation claim is made.
- [x] Phase C.5a: Add a bounded caller-driven flipbook playback clock that advances/loops a normalized frame-local sample with atomic invalid-delta rejection, then prove its sequence through the existing selector without SceneWorld, RouteIntent, MovementAuthorityGate, or transform writes. `flipbook_playback_smoke` passes Release and ASAN `detect_leaks=1`, proving quarter-step frame selection, loop wrap, invalid-NaN preservation, and clamp terminal; final broad non-Vulkan evidence is 126/126 Release and 126/126 ASAN. No skeletal/GPU/production animation claim is made.
- [x] Phase C.5b: Connect bounded FlipbookPlayback samples to the existing finite flipbook surface sequence proof so frame progression is clock-driven rather than manually sampled, while preserving SceneWorld read-only rendering and all movement/authority boundaries. The extended `flipbook_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, retaining 4 rendered/presented/selected frames, visible final output, P6 artifact, sequence hash `7898515516691923083`, final hash `11835874581093820931`, and full transform preservation; broad non-Vulkan remains 126/126 Release and 126/126 ASAN because no new root smoke was added. No skeletal/GPU/production animation claim is made.
- [x] Phase C.5c: Add a bounded caller-driven flipbook render bridge that advances playback, selects a frame rectangle, and queues the staged sprite through one atomic candidate step with output preservation on selector/playback/queue failure; it does not mutate SceneWorld, RouteIntent, MovementAuthorityGate, or transforms. `flipbook_render_bridge_smoke` passes Release and ASAN `detect_leaks=1`, proving half-step frame selection, invalid-delta rollback, missing-transform queue rollback, batch/time/rect preservation, and unchanged actor transform; final broad non-Vulkan evidence is 127/127 Release and 127/127 ASAN. No skeletal/GPU/production animation claim is made.
- [x] Phase C.5d: Add bounded caller-driven pause/resume to FlipbookPlayback, preserving elapsed time while paused and retaining invalid-delta atomicity; prove selector output remains stable during pause without SceneWorld, RouteIntent, MovementAuthorityGate, transform, or runtime ownership. `flipbook_playback_smoke` passes Release and ASAN `detect_leaks=1`, proving paused sample/time stability, selector stability, loop wrap, invalid-NaN rollback, and terminal clamp; broad non-Vulkan evidence remains 127/127 Release and 127/127 ASAN because no root smoke was added. No skeletal/GPU/production animation claim is made.
- [x] Phase C.5e: Prove the atomic FlipbookRenderBridge integrates paused playback by queueing a stable frame-local rect without advancing playback time, while preserving read-only SceneWorld and candidate rollback boundaries. The extended `flipbook_render_bridge_smoke` passes Release and ASAN `detect_leaks=1`, proving paused stable-frame queue, batch growth without playback-time advance, existing rollback boundaries, and unchanged actor transform; broad non-Vulkan remains 127/127 Release and 127/127 ASAN because no root smoke was added. No skeletal/GPU/production animation claim is made.
- [x] Phase C.6a: Add a bounded animation-state-to-flipbook playback control binding that maps an explicit active state to pause/resume only, rejects invalid state control atomically, and remains independent of SceneWorld, RouteIntent, MovementAuthorityGate, transforms, and runtime ownership. `animation_playback_control_binding_smoke` passes Release and ASAN `detect_leaks=1`, proving idle pause, paused sample stability, unmapped-state preservation, and move resume to sample 0.50; final broad non-Vulkan evidence is 128/128 Release and 128/128 ASAN. No skeletal/GPU/production animation claim is made.
- [x] Phase A.1a: Add an explicit SoftwareRenderer-to-SDL surface presenter with a hidden-surface smoke and optional NeoRuntime Farm present hook; retain the default headless behavior and report failure rather than silently falling back when presentation was requested. `software_surface_presenter_smoke` and `renderer_capability_smoke` pass Release/ASAN; full non-Vulkan suites pass 94/94 Release and 94/94 ASAN.
- [ ] Phase 3.1: Complete a playable 2D runtime slice with camera, sprite/tile rendering, input, collision/physics boundaries, UI, audio output, save/load, and deterministic test scene evidence.
- [ ] Phase 3.2: Complete a bounded 3D runtime slice with mesh/material/light rendering, camera and presentation surface, skeletal mesh playback, collision/physics integration, and deterministic scene evidence.
- [ ] Phase 3.3: Integrate character, building, item, unit-attribute, monster, NPC, quest, navigation, combat, and behavior systems into canonical runtime ownership rather than isolated sandboxes.
- [ ] Phase 3.4: Preserve XPBD performance/determinism evidence while coupling supported physics bodies to scene/gameplay lifecycle only through validated contracts and regression benchmarks.
- [ ] Phase 4.1: Define the game-creation assistant contract as dialogue/clarification, structured project plan, dry-run tool graph, human approval, bounded executor, receipts, rollback, and audit log.
- [ ] Phase 4.2: Reconcile existing Coba/Aries source with the canonical AgentCommandGateway and PromptToolGraph; retain useful planning/audit behavior while removing any untyped external-token or placeholder execution path from engine authority.
- [ ] Phase 4.3: Implement a provider-neutral server-side LLM adapter with structured output validation, model/provider configuration, secret isolation, rate limits, failure handling, and a deterministic offline/mock test mode.
- [ ] Phase 4.4: Select and configure any third-party AI/API connector only after provider, capability, cost, privacy, and credential ownership are explicitly approved; never embed API keys in source, game assets, saves, or client APKs.
- [ ] Phase 4.5: Build an allowlisted tool registry for project authoring, asset generation/import, validation, build/test, and report tasks; prohibit agents from direct runtime/economy/ban/credential/deploy authority.
- [ ] Phase 5.1: Prove a complete Farm game vertical slice through the canonical runtime, including authoring, assets, scene, gameplay loop, NPCs, buildings, items, persistence, player UI, and test evidence.
- [ ] Phase 5.2: Prove at least one additional lightweight game template (for example tower defense or puzzle) through the same contracts to demonstrate reusable engine tooling rather than Farm-only code.
- [ ] Phase 5.3: Restore and validate the Android toolchain, build a reproducible debug APK from the canonical source subset, then obtain separate signed AAB, emulator, and physical-device evidence before any release claim.
- [ ] Phase 6.1: Build server-authoritative multiplayer/session, persistence/recovery, economy/receipt, anti-cheat, ban/appeal, telemetry, privacy, and operations contracts with adversarial and load evidence.
- [ ] Phase 6.2: Design post-release agent operations as observation, triage, proposed plans, and approval-gated tools; require trusted-server authority and auditable receipts for every external effect.
- [ ] Phase 6.3: Complete security/dependency review, secret management, consent/privacy, rollback/incident procedures, support workflow, release checklist, and final owner sign-off gates.
- [ ] Phase 7: Require Release, ASAN, integration, performance, editor/backend, Android, device, security, and template evidence before changing any readiness status or GitHub release documentation.
- [ ] Audit and repair the reproducible editor build environment: resolve the documented package-manager approval policy for `esbuild` using a reviewed lockfile/dependency workflow, then capture a clean production editor build as evidence without committing `node_modules` or generated output.
- [x] Phase B.6a: Add bounded editor-session actor selection that accepts only live document actors, preserves selection atomically on invalid selection, clears selection when the selected actor is deleted or a document is reopened without it, and does not mutate SceneDocument, SceneWorld transforms, history, filesystem, desktop UI, or gameplay authority. The extended `editor_scene_session_smoke` passes Release and ASAN `detect_leaks=1`, proving valid selection, invalid-selection preservation, selected deletion clear, failed-open preservation, successful-open clear, and unchanged viewport hash; broad non-Vulkan remains 128/128 Release and 128/128 ASAN because no root smoke was added. No desktop editor, collaboration, filesystem, or production claim is made.
- [x] Phase E.6a: Add bounded candidate/commit MTL multi-material import that stages an explicit unique set of named materials from one in-memory asset and commits registry, staging store, and ordered receipts only after every requested material validates; preserve all caller state on duplicate, missing, or capacity failure. The extended `material_import_pipeline_smoke` passes Release and ASAN `detect_leaks=1`, proving ordered bark/leaf commit, two rollback classes, 2 assets, 3 staged materials, and receipt preservation; broad non-Vulkan remains 128/128 Release and 128/128 ASAN because no root smoke was added. No filesystem, hot reload, GPU, or production pipeline claim is made.
- [x] Phase R.4b: Add a bounded read-only Farm telemetry snapshot to the committed frame receipt, captured only after input/world/render success and preserved with the prior receipt on rejected frames; it adds no simulation, persistence, networking, authority, or host ownership. The extended `farm_runtime_session_smoke` passes Release and ASAN `detect_leaks=1`, proving committed telemetry parity plus rejected-frame receipt preservation; broad non-Vulkan remains 128/128 Release and 128/128 ASAN because no root smoke was added. No persistent host, networking, save system, APK, or release claim is made.
- [x] Phase R.5a: Add a finite read-only Farm HUD software proof that formats committed runtime receipt telemetry into canvas labels and draws it over a software framebuffer, preserving renderer output on invalid receipt or canvas setup failure. `farm_runtime_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving visible receipt HUD output and invalid-receipt framebuffer preservation; final broad non-Vulkan evidence is 129/129 Release and 129/129 ASAN. No input authority, simulation, persistence, networking, host, APK, or production claim is made.
- [x] Phase R.5b: Integrate the read-only Farm HUD with the finite Farm surface demo after canonical tick/render, producing a HUD-overlaid PPM artifact and preserving surface demo receipt output on HUD failure. The extended `farm_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving four presented world frames, HUD overlay, P6 artifact, and distinct world/HUD hashes; broad non-Vulkan remains 129/129 Release and 129/129 ASAN because no root smoke was added. No persistent loop, gameplay authority, networking, save data, APK, or production claim is made.
- [x] Phase R.6a: Add a bounded Farm in-memory save envelope codec that composes canonical `FarmSystem::Serialize`/`Deserialize` with `RuntimeSaveCodec`, validates the exact `farm-world` kind and nonzero revision, and preserves caller Farm state on malformed, wrong-kind, or deserialize failure. `farm_runtime_save_codec_smoke` passes Release and ASAN `detect_leaks=1`, proving Farm round-trip revision 7 plus wrong-kind/checksum atomic preservation; final broad non-Vulkan evidence is 130/130 Release and 130/130 ASAN. No filesystem, networking, auth, cloud, or production save claim is made.
- [x] Phase R.6b: Add one bounded FarmRuntimeSession in-memory checkpoint/restore handoff through the canonical FarmRuntimeSaveCodec, preserving caller Farm state and existing committed receipt/frame count atomically on validation or decode failure. `farm_runtime_session_checkpoint_smoke` passes Release and ASAN `detect_leaks=1`, proving revision-7 Farm restore, wrong-kind/checksum/zero-revision rejection, unchanged committed receipt/frame count, and renderer hash preservation; final broad non-Vulkan evidence is 131/131 Release and 131/131 ASAN. No filesystem, networking, host loop, auth, cloud, or production persistence claim is made.
- [x] Phase R.6c: Add one bounded in-memory FarmRuntimeSession world-checkpoint handoff that envelopes canonical FarmWorldTool serialization, restores the paired Farm/world state only after envelope validation, and preserves session receipt/frame count plus caller Farm/world state on failure. `farm_runtime_session_world_checkpoint_smoke` passes Release and ASAN `detect_leaks=1`, proving revision-11 Farm/world restore plus wrong-kind, checksum, inner-world corruption, and zero-revision atomic preservation; final broad non-Vulkan evidence is 132/132 Release and 132/132 ASAN. No filesystem, networking, host loop, auth, cloud, or production persistence claim is made.
- [x] Phase R.7.1a: Add a bounded FarmRuntimeSession read-only HUD overlay handoff that renders the committed world receipt through FarmRuntimeHud into an explicit HUD receipt, preserves frame/session/world receipt state, and preserves caller renderer/output on rejection. `farm_runtime_session_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving unframed rejection preserves sentinel output/renderer, then a distinct HUD hash with receipt telemetry parity and unchanged session world receipt; final broad non-Vulkan evidence is 133/133 Release and 133/133 ASAN. No interactive UI, input authority, networking, host loop, APK, or production readiness claim is made.
- [x] Phase B.6b: Add bounded EditorSceneSession selected-inspector access that snapshots only the transiently selected live document actor, rejects absence of selection while preserving caller output, and remains outside SceneDocument, transforms, history, viewport, filesystem, desktop UI, collaboration, and gameplay authority. The extended `editor_scene_session_smoke` passes Release and ASAN `detect_leaks=1`, proving no-selection sentinel preservation and selected sprite snapshot; final broad non-Vulkan evidence remains 133/133 Release and 133/133 ASAN. No desktop inspector UI, filesystem, collaboration, asset editing, or gameplay claim is made.
- [x] Phase D.6c: Add a bounded read-only GameplayPhysicsBody snapshot API that exposes validated canonical ECS circle-body state without stepping XPBD or writing SceneWorld, preserves caller output on unknown/invalid body state. The extended `gameplay_physics_body_smoke` passes Release and ASAN `detect_leaks=1`, proving dynamic/static snapshots and unknown-body sentinel preservation; final broad non-Vulkan evidence remains 133/133 Release and 133/133 ASAN. No Rigidbody, collision-response, solver ownership, transform authority, or production physics claim is made.
- [x] Phase D.6d: Add a bounded GameplayPhysicsBody dynamic planar-velocity setter that validates one canonical dynamic ECS circle body before committing both velocity components and one physics-revision signal, rejects static/unknown/non-finite input without mutation, and does not step XPBD or write SceneWorld. The extended `gameplay_physics_body_smoke` passes Release and ASAN `detect_leaks=1`, proving one dynamic velocity/revision commit and static/non-finite/unknown preservation; final broad non-Vulkan evidence remains 133/133 Release and 133/133 ASAN. No Rigidbody, forces, collision response, solver ownership, transform authority, or production physics claim is made.
- [x] Phase R.7.1b: Harden bounded AudioMixer multi-voice PCM accumulation by using a safe wide accumulator before deterministic int16 saturation. `audio_mixer_smoke` passes Release and ASAN `detect_leaks=1`, proving normal mix plus deterministic positive/negative saturation; final broad non-Vulkan evidence remains 133/133 Release and 133/133 ASAN. No runtime audio integration, device delivery, streaming, spatial audio, APK, or production readiness claim is made.
- [x] Phase C.6b: Add a bounded read-only gameplay-physics velocity-to-animation locomotion bridge that snapshots one canonical ECS circle body then delegates planar velocity only into AnimationLocomotionBridge/AnimationStateMachine, preserving animation state on snapshot failure and never stepping XPBD or writing SceneWorld, RouteIntent, MovementAuthorityGate, or transforms. `physics_animation_locomotion_bridge_smoke` passes Release and ASAN `detect_leaks=1`, proving velocity-selected move/idle plus unknown-body atomic preservation; final broad non-Vulkan evidence is 134/134 Release and 134/134 ASAN. No skeletal, Rigidbody, navigation, transform authority, or production animation claim is made.
- [x] Phase C.6c: Add a finite software-surface proof that drives animation tint from read-only canonical ECS body velocity through PhysicsAnimationLocomotionBridge while KinematicMotionController remains the sole SceneWorld transform writer. `physics_animation_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving four rendered/presented frames, P6 artifact, two velocity-sourced tint frames, final idle sample, and positive kinematic X; final broad non-Vulkan evidence is 135/135 Release and 135/135 ASAN. No skeletal, Rigidbody, collision response, navigation, GPU, or production animation claim is made.
- [x] Phase W.1a: Repair bounded WorldAuthoring deserialization so canonical GridNavigation is reconstructed from decoded biome/tree/building state only after full candidate validation, preserving the target world on malformed payload. The extended `world_authoring_smoke` passes Release and ASAN `detect_leaks=1`, proving a valid altered decoded water biome becomes navigation-blocked alongside round-trip/truncation coverage; final broad non-Vulkan evidence remains 135/135 Release and 135/135 ASAN. No runtime navigation agent, path-following, multiplayer, filesystem, or production world-authoring claim is made.
- [x] Phase W.1b: Make bounded GridNavigation FindPath output candidate-commit so caller routes are preserved on not-initialized, invalid endpoint, blocked endpoint, unreachable, or route-capacity failure. The extended `grid_navigation_smoke` passes Release and ASAN `detect_leaks=1`, proving sentinel/valid-detour preservation across not-initialized, out-of-bounds, blocked, and unreachable failures; final broad non-Vulkan evidence remains 135/135 Release and 135/135 ASAN. No runtime path-following, transform authority, navigation agents, multiplayer, or production navigation claim is made.
- [x] Phase W.1c: Preserve single-writer movement authority by removing GridRouteFollower direct SceneWorld transform snaps, using only delegated KinematicMotionController motion while retaining route arrival/replan/atomic failure semantics. `grid_route_follower_smoke` passes Release and ASAN `detect_leaks=1`, preserving arrival, chunked route, obstacle rollback, replan, and deterministic endpoint evidence; final broad non-Vulkan evidence remains 135/135 Release and 135/135 ASAN. No autonomous navigation agents, multi-segment skeletal routing, multiplayer, or production navigation claim is made.
- [x] Phase W.1d: Add one finite software-surface NPC route proof that derives ECS velocity from read-only GridRouteFollower intent, advances SceneWorld only through the guarded kinematic route controller, and selects locomotion tint through PhysicsAnimationLocomotionBridge. `npc_route_animation_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving five rendered/presented frames, P6 artifact, four route-tinted frames, final `(1,2)` route goal, and final idle selection; final broad non-Vulkan evidence is 136/136 Release and 136/136 ASAN. No behavior trees, autonomous navigation agents, skeletal animation, multiplayer, or production NPC claim is made.
- [x] Phase R.7.2a: Add an optional bounded NeoRuntime Farm HUD overlay that composes the canonical read-only FarmRuntimeHud after Farm render into a candidate renderer, commits only on full render/HUD success, and validates minimum surface dimensions. `runtime_farm_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving 63×48 rejection, nonzero world/HUD hashes, distinct overlay composition, and repeated HUD render success; final broad non-Vulkan evidence is 137/137 Release and 137/137 ASAN. No interactive UI, input authority, host loop, audio device, networking, APK, or production runtime claim is made.
- [x] Phase R.7.2b: Add a bounded immutable NeoRuntime Farm render receipt that commits world and optional-HUD hashes plus copied Farm telemetry only after complete render/HUD success, preserving the prior receipt on render failure. The extended `runtime_farm_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving world-only hash parity/HUD zero, distinct HUD composition hashes, copied tick-one telemetry, and frame-two advancement; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No telemetry transport, interactive UI, networking, cloud persistence, host loop, APK, or production runtime claim is made.
- [x] Phase E.6b: Add a bounded TextureImportPipeline multi-texture candidate-commit import set that preserves registry, staging, and caller receipts on any request failure. The extended `texture_import_pipeline_smoke` passes Release and ASAN `detect_leaks=1`, proving ordered two-texture commit, malformed second-request rollback, and empty-set receipt preservation; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No filesystem watchers, hot reload, GPU upload, external asset source, or production asset-streaming claim is made.
- [x] Phase E.6c: Add a bounded MeshImportPipeline multi-OBJ candidate-commit import set that preserves registry, mesh staging, and caller receipts on any request failure. The extended `mesh_import_pipeline_smoke` passes Release and ASAN `detect_leaks=1`, proving ordered two-OBJ commit, malformed second-request rollback, empty-set rejection, and 33-request capacity rejection; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No filesystem watchers, MTL parsing, hot reload, GPU upload, external asset source, or production asset-streaming claim is made.
- [x] Phase E.6d: Add a bounded MaterialImportPipeline multi-material refresh transaction that replaces caller-supplied in-memory MTL bytes and refreshes one to sixteen already-staged named materials atomically, preserving registry, staging, and receipts on invalid replacement data. The extended `material_import_pipeline_smoke` passes Release and ASAN `detect_leaks=1`, proving ordered two-material refresh, malformed second-material rollback, and 17-request rejection; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No paths, file watching, live-scene rebinding, GPU upload, or production hot-reload claim is made.
- [x] Phase E.6e: Make SceneMeshAdapter copy/move value-semantics rebind each textured instance to its own embedded CPU texture after transfer. The extended `scene_mesh_adapter_smoke` passes Release and ASAN `detect_leaks=1`, proving moved/move-assigned adapters preserve their blue CPU snapshot after the source refreshes green; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No GPU/lifetime streaming, hot reload, editor UI, or production renderer claim is made.
- [x] Phase E.6f: Add an AssetRefreshExecutor candidate-commit execution path that applies a bounded preflight-valid refresh plan to candidate texture/mesh/material staging and SceneMeshAdapter copies, committing all candidates and receipts only on complete success while preserving caller receipts and live state on candidate failure. The extended `asset_refresh_executor_smoke` passes Release and ASAN `detect_leaks=1`, proving a red-to-blue sequential refresh, blue-to-green atomic commit, stale candidate rejection that preserves green live rendering and prior receipt vectors, and existing invalid/probe boundaries; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No concurrency guarantees, file watching, GPU upload, live game host, or production hot-reload claim is made.
- [x] Phase E.6g: Make EditorSceneMeshBinder candidate-commit mesh/material/texture staging together with its SceneMeshAdapter target, preserving caller staging and target on a later actor's binding failure. The extended `editor_scene_mesh_binder_smoke` passes Release and ASAN `detect_leaks=1`, proving a valid first actor followed by a malformed second MTL leaves all three candidate caller stores empty and preserves the original textured target; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No filesystem import, editor UI, runtime ownership, GPU upload, or production scene-loading claim is made.
- [x] Phase E.6h: Route the finite MaterialRefreshSurfaceDemo material replacement and scene rebind through AssetRefreshExecutor::ExecuteAtomic, preserving its hidden SDL/software proof and before/after receipt semantics. The `material_refresh_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving distinct green-before/red-after frame/material hashes and PPM artifact through the atomic refresh/rebind path; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No filesystem watching, concurrent reload, GPU upload, persistent world, or production hot-reload claim is made.
- [x] Phase C.6d: Add a read-only AnimationStateMachine snapshot exposing active/target state, blend fraction, and scalar clocks with no transform, route, or movement-authority mutation. The extended `animation_state_machine_smoke` passes Release and ASAN `detect_leaks=1`, proving idle start, zero/half/completed blend snapshots, immediate transition, invalid delta preservation, and not-started snapshot rejection; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. The one-segment skeletal-route boundary remains frozen.
- [x] Phase D.6e: Add a bounded GameplayPhysicsBodyBuilder batch dynamic planar-velocity command that prevalidates all 1–64 distinct bodies before one ECS-only commit, preserving prior velocities/revision on invalid, static, unknown, duplicate, or non-finite request. The extended `gameplay_physics_body_smoke` passes Release and ASAN `detect_leaks=1`, proving a two-body batch commits both velocity pairs with one physics revision and static/duplicate/oversized/non-finite requests preserve the existing velocity/revision; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No SceneWorld writes, solver stepping, impulses, collision response, or multiplayer authority claim is made.
- [x] Phase D.6f: Add a bounded GameplayPhysicsQuery batch overlap-circle API that evaluates 1–32 read-only overlap queries into candidate ordered entity sets and commits the complete output only when every query validates/maps within capacity. The extended `gameplay_physics_query_smoke` passes Release and ASAN `detect_leaks=1`, proving a two-zone ordered/empty batch and that malformed, empty, and oversized batches preserve prior caller output; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No solver stepping, ECS/SceneWorld writes, trigger callbacks, collision response, or multiplayer authority claim is made.
- [x] Phase R.7.2c: Make NeoRuntime Farm render receipt/renderer commit only after an optional hidden SoftwareSurfacePresenter has pumped and presented the candidate frame, while retaining the no-presenter path. The extended `runtime_farm_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving no-presenter receipts remain zero and a hidden surface presents one candidate frame before renderer/receipt commit; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No real GPU backend, desktop editor, persistent window/event loop, or production presentation claim is made.
- [x] Phase R.7.2d: Make FarmSurfaceDemo enable NeoRuntime Farm HUD and derive its PPM/world/HUD/presentation receipts directly from the committed runtime frame, so manual post-present HUD composition cannot diverge from the visual surface proof. The `farm_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving four hidden presented frames, distinct committed world/HUD hashes, and a valid PPM from the same runtime frame; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No GPU backend, persistent game loop, input/UI interaction, or production game claim is made.
- [x] Phase B.6c: Make EditorSceneSpriteBinder candidate-commit texture staging together with its SceneSpriteAdapter target, preserving caller texture staging and target on a later sprite's bind failure. The extended `editor_scene_sprite_binder_smoke` passes Release and ASAN `detect_leaks=1`, proving a valid first sprite followed by a ready malformed second texture leaves candidate caller staging empty and preserves the pre-existing sprite target; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No filesystem import, editor UI, runtime ownership, GPU upload, or production scene-loading claim is made.
- [x] Phase B.6d: Extend EditorSceneSession smoke coverage with one SceneDocument containing both a ready staged mesh/material and a staged sprite, proving one CPU viewport composes both adapters and a later malformed sprite asset preserves the committed session viewport. The extended `editor_scene_session_smoke` passes Release and ASAN `detect_leaks=1`, proving mixed mesh-sprite CPU viewport composition and a malformed later sprite yields `SpriteBindFailed` while preserving the committed viewport hash; final broad non-Vulkan evidence remains 137/137 Release and 137/137 ASAN. No editor UI, filesystem project assets, live reloading, GPU viewport, or production editor claim is made.
- [x] Phase B.7a: Add bounded in-memory `EditorScenePrefab` capture/instantiate for one validated actor subtree, remapping caller-supplied unique IDs and reattaching only the prefab root to a caller parent in one candidate SceneDocument commit. `editor_scene_prefab_smoke` passes Release and ASAN `detect_leaks=1`, proving capture, remap, atomic rejection of duplicate/malformed input, and session undo/redo; broad non-Vulkan regressions pass 138/138 in both configurations. No filesystem assets, nested prefab references, editor UI, live runtime instancing, or production content pipeline claim is made.
- [x] Phase B.7b: Add a bounded in-memory `EditorScenePrefabCodec` that frames an existing SceneDocument codec payload with prefab root identity and rejects malformed/trailing/cross-subtree input while preserving caller outputs. `editor_scene_prefab_codec_smoke` passes Release and ASAN `detect_leaks=1`, proving round trip, output preservation, malformed encode rejection, and decoded instantiation; broad non-Vulkan regressions pass 139/139 in both configurations. No filesystem format, asset embedding/lifetime, nested prefab, UI editor, live runtime instancing, or production content pipeline claim is made.
- [x] Phase B.7c: Add bounded `PrefabStagingStore` for ready `AssetRegistry` Prefab bytes decoded through `EditorScenePrefabCodec`, preserving staged resources on invalid later input and exposing stale-hash detection; allow EditorSceneSession to instantiate only a current staged prefab in one candidate document commit. `prefab_staging_smoke` passes Release and ASAN `detect_leaks=1`, proving ready staging, stale rejection, atomic preservation, and valid refresh; broad non-Vulkan regressions pass 140/140 in both configurations. No filesystem watchers, nested prefab, live runtime spawning, editor UI, GPU assets, or production content pipeline claim is made.
- [x] Phase E.7a: Add bounded `SceneSpriteAdapter` staged-texture refresh for one existing sprite entity, preserving all display metadata and rejecting malformed replacement/source-rect incompatibility atomically. The extended `editor_scene_sprite_binder_smoke` passes Release and ASAN `detect_leaks=1`, proving refreshed color rendering plus malformed-refresh frame preservation; broad non-Vulkan regressions pass 140/140 in both configurations. No file watching, automatic registry reload, GPU texture rebinding, editor UI, or production hot-reload claim is made.
- [x] Phase E.7b: Add bounded manual `AssetRefreshExecutor::ExecuteSpritesAtomic` for a texture refresh followed by one sprite instance rebind on candidate copies. The extended `asset_refresh_executor_smoke` passes Release and ASAN `detect_leaks=1`, proving texture-plus-sprite candidate commit while retaining existing mesh refresh evidence; broad non-Vulkan regressions pass 140/140 in both configurations. No sprite auto-planning diagnostics, filesystem watching, GPU upload, editor UI, or production hot-reload claim is made.
- [x] Phase E.7c: Add bounded diagnostics auto-planning for stale `SceneSpriteAdapter` bindings through value-owned immutable snapshots and deterministic adapter-insertion order, while retaining the existing mesh-only `BuildPlan` overload. `ExecuteSpritesAtomic` now accepts multiple distinct sprite entities and commits candidate texture/sprite stores plus receipts only after full success. The extended `asset_refresh_executor_smoke` passes Release and ASAN `detect_leaks=1`, proving stable mesh planning, deterministic two-sprite actions, multi-sprite refresh, later source-rect rejection rollback, and receipt preservation; broad non-Vulkan regressions pass 140/140 in both configurations. No file watching, GPU upload, editor UI, automatic project reload, asset streaming, or production hot-reload claim is made.
- [x] Phase E.7d: Add bounded `AssetRefreshExecutor::ExecuteCombinedAtomic` for one diagnostics plan containing existing texture/mesh/material/scene-mesh actions plus `RefreshSpriteInstance`. It groups dependency-aware scene-mesh actions and sprite actions on shared candidate stores, then commits texture, mesh, material, scene-mesh, sprite, and receipt state only after total success while retaining existing executor APIs. The extended `asset_refresh_executor_smoke` passes Release and ASAN `detect_leaks=1`, proving deterministic mixed texture→scene-mesh→two-sprite refresh and a later sprite source-rect failure rolls back all candidate stores and receipts; broad non-Vulkan regressions pass 140/140 in both configurations. No watcher, GPU upload, editor UI, automatic project reload, asset streaming, or production hot-reload claim is made.
- [x] Phase E.8a: Add bounded `RefreshPrefab` diagnostics and `AssetRefreshExecutor::ExecutePrefabsAtomic` for stale `PrefabStagingStore` resources. The candidate executor refreshes staged prefab data and receipts only after full success, retains all existing APIs, and never mutates `EditorSceneSession`; explicit instantiation remains caller-controlled. The extended `prefab_staging_smoke` passes Release and ASAN `detect_leaks=1`, proving valid different-hash prefab refresh, deterministic one-action plan, session preservation before explicit instantiation, malformed replacement rollback, and receipt preservation; broad non-Vulkan regressions pass 140/140 in both configurations. No file watcher, automatic scene re-instantiation, editor UI, filesystem project manager, GPU asset path, or production hot-reload claim is made.
- [x] Phase E.8b: Add one bounded diagnostics overload for sprite plus prefab staging and `AssetRefreshExecutor::ExecuteAllAtomic` for its texture/mesh/material/scene-mesh/sprite/prefab subset. It uses candidate stores, preserves read-only binding/session boundaries, returns plan-order receipts, and commits only after all resource, sprite, and prefab groups succeed. The extended `prefab_staging_smoke` passes Release and ASAN `detect_leaks=1`, proving deterministic dependency texture→sprite→prefab refresh plus malformed later prefab rollback of texture, sprite, prefab, and receipts; broad non-Vulkan regressions pass 140/140 in both configurations. No watcher, automatic scene re-instantiation, editor UI, filesystem project manager, GPU upload, or production hot-reload claim is made.
- [x] Phase E.8c: Preserve the diagnostics `expectedHash` in every committed asset-refresh preflight and execution receipt, including the combined texture/sprite/prefab plan order, so callers can audit the exact approved asset version without reading mutable registry state. The extended `prefab_staging_smoke` passes Release and ASAN `detect_leaks=1`, proving single-prefab and plan-order texture/sprite/prefab receipt fidelity; retained receipts remain unchanged after a later malformed prefab rollback. Broad non-Vulkan regressions pass 140/140 in both configurations. This is receipt metadata only: no watcher, automatic session mutation, re-instantiation, GPU upload, or hot-reload behavior.
- [x] Phase UI.1a: Add a bounded retained UI layout resolver for existing canvas widgets, with validated parent-relative anchors, padding, fixed/minimum sizing, and deterministic vertical stacking committed only after the complete candidate layout succeeds. `ui_layout_resolver_smoke` passes Release and ASAN `detect_leaks=1`, proving a four-button TILL/PLANT/WATER/HARVEST panel resolves, hit-tests, renders, remains deterministic, and preserves geometry after an invalid later layout. Existing router/canvas smoke remains green; broad non-Vulkan regressions pass 141/141 in both configurations. No Farm authority, UI-to-world command, persistence, window loop, GPU, monetization, APK, or production UI claim.
- [x] Phase UI.1b: Add a bounded Farm action-panel controller that maps one validated activated UI widget to the existing `FarmPlayerInputBridge::SetSelectedAction` state for Till, PlantWheat, Water, or Harvest, preserving the prior selected action on unknown, duplicate, or invalid panel configuration. `farm_action_panel_controller_smoke` passes Release and ASAN `detect_leaks=1`, proving pointer/keyboard activation, unknown-widget and configuration rejection, unrelated-widget ignore, receipt preservation, and no world mutation. Broad non-Vulkan regressions pass 143/143 in both configurations. No direct `FarmWorldTool` call, session tick, persistence, monetization, APK, or production UI claim.
- [x] Phase UI.1c: Extend the bounded `FarmRuntimeHud`/`FarmRuntimeSession` handoff with retained layout-resolved HUD panels for frame, coins, tick, growing/harvestable tile state, and canonical wheat seed/produce inventory alongside a four-button action panel. Pointer activation routes only through `FarmActionPanelController` into the existing session `FarmPlayerInputBridge` selection. `farm_interactive_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving unframed input rejection/receipt preservation, software overlay, TILL then UI-selected PLANT through later canonical interact frames, and committed growing/inventory receipt changes. Existing Farm HUD/session smoke remains green; broad non-Vulkan regressions pass 143/143 in both configurations. No direct `FarmWorldTool` call from UI, stamina/energy simulation, persistence, networking, monetization, APK, or production claim.
- [x] Phase UI.1d: Add a finite hidden-surface Farm interactive demo that drives the bounded layout-resolved HUD selection plus later canonical interact frames, writes a P6 artifact from the composed software frame, and records frame/action/world/HUD/presentation receipts only after total success. `farm_interactive_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving four presented frames, UI-selected Plant through later canonical interact input, distinct world/HUD hashes, final growing crop/wheat-seed receipt state, P6 artifact, and invalid-config caller-receipt preservation. Broad non-Vulkan regressions pass 144/144 in both configurations. No persistent game loop, direct UI world authority, filesystem project pipeline beyond the caller-supplied PPM artifact, networking, monetization, APK, or production claim.
- [x] Phase UI.1e: Add a bounded `UiCanvasImage` binding for an existing visible UI widget and a current staged CPU texture, with validated source rectangles and candidate canvas draw. The extended `ui_canvas_renderer_smoke` passes Release and ASAN `detect_leaks=1`, proving a red/blue texture-backed icon, duplicate/source-rectangle rejection, stale texture rejection, and renderer-hash preservation. Broad non-Vulkan regressions pass 144/144 in both configurations. No image import/watcher, GPU UI, world authority, persistence, monetization, APK, or production UI claim.
- [x] Phase UI.1f: Extend the finite interactive Farm proof to select all four retained action widgets—Till, PlantWheat, Water, and Harvest—through `FarmRuntimeSession` HUD input and execute each only on later canonical interact frames. `farm_interactive_hud_smoke` and `farm_interactive_surface_demo_smoke` pass Release and ASAN `detect_leaks=1`, proving all selections, ordinary one-tick Farm growth to harvestable, Harvest quest progress, wheat seed `32→31`, wheat produce `0→2`, 17 presented hidden-surface frames, P6 artifact, and four-action receipt mask. Broad non-Vulkan regressions pass 144/144 in both configurations. No direct UI `FarmWorldTool` authority, time acceleration outside canonical tick, persistence, networking, monetization, APK, or production claim.
- [x] Phase UI.1g: Prove the retained Farm HUD keyboard path end-to-end: deterministic focus traversal and `Activate` select a configured action through `FarmRuntimeSession::RouteHudKeyboard`, then only a later canonical interact frame mutates the Farm world. The extended `farm_interactive_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving focus traversal from Till to Plant, Activate selection, invalid-key rejection with selection/receipt preservation, and later canonical Plant execution. Broad non-Vulkan regressions pass 144/144 in both configurations. No direct UI world authority, keyboard remapping/persistence, network, monetization, APK, or production claim.
- [x] Phase UI.1h: Extend the finite interactive Farm surface proof with canonical character movement input before the UI action sequence, then prove the HUD-selected Till/Plant/Water/Harvest actions operate at the moved character tile. `farm_interactive_surface_demo_smoke` passes Release and ASAN `detect_leaks=1`, proving a canonical right move from `(1,1)` to `(2,1)`, 19 presented hidden-surface frames, all four actions at the moved tile, Farm progression/inventory receipt changes, and P6 output. Broad non-Vulkan regressions pass 144/144 in both configurations. No UI movement authority, pathfinding, persistence, networking, monetization, APK, or production claim.
- [x] Phase UI.1i: Bind the enhanced Farm HUD’s retained action-panel background to a current staged CPU texture already used by Farm world rendering, through the `UiCanvasImage` path and session-owned registry/staging references. The extended `farm_interactive_hud_smoke` passes Release and ASAN `detect_leaks=1`, proving enhanced composition with the current staged icon, stale-icon HUD rejection, caller renderer/HUD-receipt preservation, then canonical world-render refresh before action continuation. Legacy HUD/session smoke remains green; broad non-Vulkan regressions pass 144/144 in both configurations. No persistence, image import/watch, GPU UI, world authority, monetization, APK, or production claim.
- [x] Phase UI.1j: Add a bounded immutable Farm player-input receipt for successful canonical movement and action application, carry it into `FarmRuntimeFrameReceipt`/HUD receipt, and render a concise HUD quick-status label from that receipt. `farm_player_input_bridge_smoke`, `farm_runtime_session_smoke`, interactive HUD, and legacy HUD/session smoke pass Release and ASAN `detect_leaks=1`, proving movement/action receipt commit, coordinates, action identity, failed-input receipt preservation, session receipt propagation, and HUD feedback. Broad non-Vulkan regressions pass 144/144 in both configurations. No energy/stamina simulation, UI world authority, persistence, networking, monetization, APK, or production claim.
- [x] Phase UI.1k: Add one retained inventory row to the enhanced Farm HUD using the existing immutable session inventory snapshot, so wheat seeds and wheat produce are presented separately beside frame/coins/tick/action feedback. The enhanced layout fits bitmap labels by candidate resolving seven nine-pixel information rows; interactive and legacy HUD/session smoke pass Release and ASAN `detect_leaks=1`, proving canonical seed depletion `32→31`, produce `0→2`, action flow, and prior failure preservation remain intact. Broad non-Vulkan regressions pass 144/144 in both configurations. No inventory mutation from UI, persistence, networking, monetization, APK, or production claim.
- [x] Phase UI.1l: Add a read-only Farm action-availability snapshot computed from the current character tile and canonical inventory, propagate it through frame/HUD receipts, and make the retained action panel reject unavailable selections while rendering them disabled. Controller, session, and HUD smoke pass Release and ASAN `detect_leaks=1`, proving unavailable direct/pointer/focused-keyboard selection preserves bridge action, caller receipt, capture, and focus; availability derives only from tile state, watering, and seed count. Broad non-Vulkan regressions pass 144/144 in both configurations. No UI tile/inventory mutation, predictive simulation, persistence, networking, monetization, APK, or production claim.
- [ ] Program U60.1: Synchronize the canonical sandbox checkout to the verified remote `main` SHA, record an active-source manifest, and reproduce clean CMake plus targeted and broad Release/ASAN evidence on the same revision before counting any parallel-agent work toward the maturity rubric.
- [ ] Program U60.2: Define and prove one runtime frame-composition contract for Farm: input snapshot → bounded simulation → scene/asset snapshot → CPU render/present → UI/audio/event receipts, with candidate commit and deterministic replay boundaries.
- [ ] Program U60.3: Build one runtime-owned Farm asset-to-scene path using explicit manifest/hash/staging ownership and fail-closed CPU rendering, then extend editor/session proof only after the shared contract is stable.
- [ ] Program U60.4: Complete one player-facing Farm vertical slice with deterministic HUD/action/inventory/energy feedback, character control, save/recovery, audio lifecycle seam, and a reproducible surface evidence path; no production readiness claim without separate platform/service gates.
- [ ] Program U60.5: Harden separated authority, persistence, agent, Android, security, and operations contracts through approval-gated adapters and evidence; never grant direct agent, client, or UI authority over economy, bans, secrets, or deployment.
- [x] Program U60.2a: Add an immutable `NeoRuntimeFrameReceipt` that commits clock, time, actor fixed-tick, Farm snapshot, scene/world revision, event dispatch count, and optional render receipt only after a fully successful `NeoRuntime::Tick`/`RenderFarm` candidate boundary. Preserve the prior receipt after any failed phase; prove deterministic valid and rejected frame behavior. This is an orchestration receipt only, not a second simulation loop, UI authority, network authority, audio device, persistence, monetization, Android, or production claim. `runtime_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, including valid tick/render receipt and actor-tick rejection preservation.
- [x] Program U60.2b: Add bounded immutable provenance summaries for the existing input state, live SceneWorld entity count, and AssetRegistry metadata to `NeoRuntimeFrameReceipt`, committed only at the existing successful Tick boundary. Prove summary determinism and preserve prior receipt on the existing rejected actor-tick path; do not add a simulation loop, input authority, asset mutation, UI/audio device, networking, persistence, Android, monetization, or production claim. `runtime_smoke`, `input_state_smoke`, and `asset_registry_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`.
- [x] Program U60.2c: Add a bounded immutable dispatch summary that records the existing event listener count, dispatched-event count, and deterministic digest in `NeoRuntimeFrameReceipt` only after successful event dispatch. Prove exact receipt linkage and prior-receipt preservation on the existing rejected actor-tick path; do not add event authority, a second simulation loop, networking, persistence, audio device, Android, monetization, or production claim. `runtime_smoke` and `event_signal_bus_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`.
- [x] Program U60.3a: Define a bounded runtime-owned Farm render-asset manifest and validate its ready content-hash identity through AssetRegistry/AssetResourceManager before it can be consumed by CPU Farm rendering. Keep the existing color renderer as the default path; this increment must fail closed on missing, wrong-kind, stale, or mismatched source identity and must not claim GPU rendering, generic import, hot reload, UI authority, or production readiness. `runtime_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving default-color fallback, 16-texture manifest bind, sprite render, duplicate-bind rejection, and malformed/stale source rejection with frame preservation.
- [x] Program U60.3b: Record the bound Farm sprite manifest receipt only after a successful CPU sprite render so callers can correlate the output frame with immutable asset-count and aggregate-hash provenance. Preserve the existing no-manifest color path and stale rejection behavior; do not add generic hot reload, GPU upload, UI authority, networking, persistence, Android, monetization, or production claims. `runtime_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, including provenance receipt and stale-render receipt preservation.
- [x] Audit 2026-08-27 (remote re-scan): Reconciled remote `main` commit `efed0ea04f2e007d409e8507c230708a985ff397`, source/module inventory, active smoke coverage, explicit stub boundaries, and R1–R12 release gates into `docs/REPOSITORY_RESCAN_2026-08-27.md`. The report deliberately makes no Unreal parity, production, Android/Play, monetization, or broad-ASAN claim.
- [x] P0.1: Integrate an opt-in NeoRuntime-owned Farm player input bridge into the existing tick order, with immutable receipt and rejected-input preservation; no second FarmRuntimeSession loop or direct UI world authority. `runtime_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving opt-in canonical movement, later canonical Till, and committed movement/action receipts.
- [x] R1a: Establish the canonical Farm game-tool contract with typed world/rules/content commands, versioned v2 save payloads, v1-to-v2 migration, fail-closed invalid-input validation, and deterministic replay receipts. `farm_canonical_game_tool_smoke` passes in Release and AddressSanitizer with leak detection enabled; this closes R1 for the canonical Farm tool scope only, while other templates still require independent evidence. See `docs/FARM_CANONICAL_GAME_TOOL_R1_EVIDENCE_V1.md`.
- [ ] P0.2: Prove one local Farm player vertical slice through canonical input, action selection, Farm simulation, CPU render/present, HUD feedback, local save/restore, and deterministic failure/recovery receipts.
- [x] P0.2a: Compose a read-only `FarmRuntimeFrameReceipt` from existing NeoRuntime-owned Farm, world, time, input-bridge, and renderer state so the existing Farm HUD can present canonical player feedback without running a nested `FarmRuntimeSession`; preserve candidate renderer/receipt behavior on HUD failure. `runtime_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving canonical movement/Till receipts and a HUD render hash.
- [x] P0.2b: Add a versioned atomic NeoRuntime Farm progress checkpoint that captures Farm through FarmWorld's single embedded serialization, RuntimeTime, and the Farm authority ledger without constructing a FarmRuntimeSession. Decode all state into candidates, rebind the authority session explicitly, and inherit SceneWorld binding only for an identical building/NPC topology with valid existing entities. `neo_runtime_checkpoint_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving round-trip, authority rebind, checksum/wrong-kind/trailing/corrupt-payload rejection, topology-mismatch preservation, and receipt invalidation after successful restore. This is a quiescent topology-preserving local progress checkpoint, not general scene rollback, full runtime persistence, or multiplayer recovery.
- [x] P0.2c: Delegate Farm HUD pointer/keyboard routing from NeoRuntime only to its existing FarmRuntimeHud and FarmPlayerInputBridge, reject unavailable/invalid states without direct FarmWorld mutation, and prove selected action is committed only by the later canonical runtime tick. `runtime_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving unavailable-state rejection, keyboard selection without tile mutation, and later canonical Till execution.
- [x] P0.2d: Add one bounded NeoRuntime-owned Farm vertical-slice smoke that composes HUD selection, input-bridge interact ticks, CPU world/HUD rendering, hidden software presentation, topology-preserving progress checkpoint/restore, receipt invalidation, and corrupt-checkpoint preservation without constructing `FarmRuntimeSession`. `neo_runtime_farm_vertical_slice_smoke` passes in Release and AddressSanitizer with `detect_leaks=1` under the SDL dummy driver, proving Till/Plant/Water/Harvest selection and later canonical interact ticks, HUD/world framebuffer distinction, CPU presentation, inventory change, checkpoint restore/receipt invalidation, and corrupt-checkpoint state preservation. This remains a finite host smoke, not interactive user acceptance, a GPU/Android surface test, persistent save, online multiplayer, payment, anti-cheat, or release claim.
- [x] P0.2e: Surface the authored `AgricultureCurriculum` onboarding/progression receipt through the canonical NeoRuntime frame and Farm HUD, and persist curriculum completion/revision in the existing atomic Farm progress checkpoint. `neo_runtime_farm_vertical_slice_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving completed orientation/land lessons, player-facing lesson token, curriculum checkpoint restore, and corrupt-checkpoint preservation. This strengthens R2 only; energy/economy feedback, complete onboarding UX, executed balance data, persistent production save, accessibility/platform acceptance, and package evidence remain open. See `docs/NEO_RUNTIME_FARM_PROGRESSION_EVIDENCE_V1.md`.
- [x] R2a: Wire a validated authored `FarmBalanceProfile` into canonical FarmSystem/NeoRuntime rules for growth, yield, crop pricing, energy cost, and energy regeneration; add next-step/error onboarding receipts, compact HUD feedback, and `FarmProgressCheckpointFile` production-save path. `neo_runtime_farm_vertical_slice_smoke` and `neo_runtime_farm_progress_file_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving balance outcomes, no-partial-write energy rejection/recovery, sale pricing, player-facing feedback, file restore, and corrupt-payload preservation. Physical platform/accessibility/localization/package acceptance remains open; R2 formal status is not changed by this sub-evidence alone.
- [ ] P0.3: Replace the nonfunctional Vulkan RHI placeholder with one bounded device/resource/present lifecycle and explicit unsupported/device-loss behavior, backed by executable desktop evidence.
- [x] P0.3a: Make the existing bounded Vulkan present probe reproducible on a virtual X11 surface, prove its complete hidden window/surface/device/swapchain/submit/present lifecycle in Release and AddressSanitizer, and preserve the separate VulkanRHI `NOT_IMPLEMENTED` boundary rather than claiming a production renderer. `tools/vulkan_present_xvfb_smoke.sh` passed against `vulkan_present_smoke` in both configurations with `detect_leaks=1` for AddressSanitizer. This is virtual X11 evidence only; it does not close the RHI/game-renderer/device-loss/physical-GPU gaps.
- [x] P0.3c: Add fail-closed classification for Vulkan acquire/submit/present driver outcomes, preserving all stage flags while distinguishing device lost, surface out-of-date, timeout, and other driver rejection from a completed frame. `vulkan_present_status_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, while `vulkan_textured_present_smoke` passes under Xvfb in Release with status `Presented`. The full Xvfb ASAN run currently reports a 224-byte indirect leak from external X11/driver worker libraries after engine teardown and is deliberately not claimed as passing; there is no forced device-loss/resize, recovery, physical-GPU, or production renderer claim.
- [x] P0.3d: Connect a canonical NeoRuntime Farm/HUD framebuffer pixel span to the existing Vulkan textured-present lifecycle with bounded invalid-span rejection. `farm_vulkan_textured_present_smoke` passes in Release and ASAN under Xvfb, proving runtime frame source, RGBA conversion, texture upload, swapchain, submit, and present. This remains virtual-surface integration only; production GPU scene rendering, device-loss/resize recovery, physical-GPU coverage, streaming, performance, and Android GPU delivery remain open. See `docs/FARM_GPU_RENDER_INTEGRATION_EVIDENCE_V1.md`.
- [ ] P0.4: Define a minimal versioned authoritative server command/session boundary with authentication binding, idempotency/replay protection, snapshot/delta receipt, and adversarial integration evidence; do not expose Farm economy/bans to clients.
- [x] P0.4a: Add one bounded in-memory Farm authoritative session host that derives internal authority commands only from a server-authenticated session principal, preserves existing Farm authority/replay/trust gates, and emits versioned snapshot/delta receipts. `farm_authoritative_session_host_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving invalid authentication, unknown/stale session, subject spoofing, duplicate command replay, handler rejection, snapshot/delta, and caller-copy isolation. It remains process-local: no transport, TLS, durable sessions, matchmaking, or multiplayer readiness claim.
- [x] P0.4b: Add a bounded localhost-only Farm session loopback adapter that authenticates a server-held principal once, rejects wire subject/session mismatch before Farm command dispatch, returns authoritative Farm snapshots only for accepted or replayed commands, and closes the connection fail-closed on rejected dispatch or snapshot construction. `farm_authoritative_session_loopback_smoke`, `farm_authoritative_session_host_smoke`, and `farm_authority_loopback_smoke` pass in Release and AddressSanitizer with `detect_leaks=1` on head `4ea96a37ac4e47b8acd1f8ec0aaff39a2b189a7c`, proving one accepted till, replay stability, spoofed-player rejection, wrong-session rejection, no snapshot leak on rejection, and unchanged Farm revision/state. This remains localhost-only and does not claim TLS, tokens, durable sessions, delta replication, public networking, or multiplayer readiness.
- [x] P0.4c: Add opt-in bounded TCP reconnect to the Farm authoritative session loopback while preserving one-connection default transport behavior. `farm_authoritative_session_loopback_smoke` and `authority_loopback_transport_smoke` pass in Release and AddressSanitizer, proving disconnect/reconnect replay through the same authenticated session host, identical authoritative snapshot state, and unchanged raw transport stop behavior. The reconnect window is capped at two sequential connections and does not claim TLS, public networking, durable sessions, multi-client concurrency, load, or production multiplayer readiness. See `docs/FARM_AUTHORITY_RECONNECT_EVIDENCE_V1.md`.
- [x] P0.4d: Add a bounded safe-slot local-file adapter for the existing Farm authority checkpoint, retaining caller world/authority-ledger bytes on missing, invalid-slot, and corrupt-file failures while requiring explicit session rebind after restore. `farm_authority_checkpoint_file_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`; related authority checkpoint/session-host regressions were also green before the final reconciliation. Evidence proves local restore, replay retention after explicit rebind, missing/invalid-slot/corrupt-file rejection, and atomic caller-state preservation. This is not TLS, credential/session-secret persistence, locking, encrypted/durable server storage, replication, or public multiplayer.
- [ ] P0.5: Define an authoritative commerce/entitlement boundary with verified receipt ingestion, duplicate/reversal handling, reconciliation and audit receipts; do not process real money or claim store readiness without provider integration evidence.
- [x] P0.5a: Add one bounded in-memory Farm commerce entitlement ledger that accepts only verifier-approved provider receipts for its configured player, fails closed on duplicate/reversal/mismatch input, applies no real payment, and emits copy-owned audit receipts. `farm_commerce_entitlement_smoke`, `farm_smoke`, `farm_world_tool_smoke`, and `farm_trust_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving accepted receipt, duplicate/reversal rejection, wrong-player/verifier rejection, reconciliation mismatch, and caller-copy isolation. No provider integration, payment processing, durable ledger, or store-readiness claim is made.
- [x] P0.5b: Add a versioned checksummed in-memory commerce-ledger snapshot plus an atomic Farm world-and-commerce checkpoint so accepted provider receipt identities survive a controlled restore and cannot be replayed for a second entitlement. `farm_commerce_checkpoint_smoke`, `farm_commerce_entitlement_smoke`, `farm_world_tool_smoke`, and `farm_trust_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving restored coin/receipt state, duplicate rejection after restore, a distinct subsequent receipt, reconciliation, checksum/trailing rejection, player-bound ledger rejection, and atomic preservation. This is an in-memory serialization boundary only, not provider-backed durable storage, payment processing, or store readiness.
- [x] P0.5c: Add a bounded local-file adapter that persists only a valid Farm-commerce checkpoint through `AtomicSaveFile`, retains the caller’s world/ledger on missing, invalid-slot, or corrupt-file load failures, and preserves duplicate receipt rejection after a successful controlled file restore. `farm_commerce_checkpoint_file_smoke`, `farm_commerce_checkpoint_smoke`, and `farm_commerce_entitlement_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving controlled file restore, replay rejection, missing/invalid-slot/corrupt-file failure, and atomic caller-state preservation. This is local file persistence only; no power-loss fsync, locks, encryption, authorization, provider integration, real payment, or store readiness is claimed.
- [x] R6a: Promote canonical commerce fraud signals for duplicate provider receipts and reconciliation mismatch into the bound `TrustSafetySystem`, while preserving non-fraud verifier/wrong-player/reversal rejection boundaries. `farm_commerce_entitlement_smoke`, `farm_commerce_checkpoint_smoke`, and `farm_authoritative_service_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving score escalation to ban and authoritative rejection without extra coin mutation. This is in-process evidence only; provider-backed detection, rate limiting, tamper-resistant storage, appeal governance, identity controls, and adversarial load remain open. See `docs/FARM_COMMERCE_FRAUD_TRUST_EVIDENCE_V1.md`.
- [x] R7a: Add a bounded caller-owned export of versioned Farm commerce audit receipts, preserving sequence/order and internal-state isolation. `farm_commerce_entitlement_smoke`, `farm_commerce_checkpoint_smoke`, and `farm_authoritative_service_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving seven ordered audit outcomes, export-copy mutation isolation, commerce checkpoint behavior, and authoritative fraud-ban rejection. Provider/payment integration, durable audit storage, refund operations, access policy, and operational review remain open. See `docs/FARM_COMMERCE_AUDIT_EXPORT_EVIDENCE_V1.md`.
- [x] R8a: Extend canonical `AtomicSaveFile` with bounded temporary-file backup and restore semantics; missing backup must fail without changing the primary slot. `atomic_save_file_smoke`, `runtime_persistence_smoke`, `neo_runtime_checkpoint_smoke`, and `farm_commerce_checkpoint_file_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving backup/restore, checksum/schema rejection, and caller-state preservation. Authoritative durable storage, locking/fsync, retention/privacy policy, migration service, rotation, and disaster-recovery evidence remain open. See `docs/R8_PERSISTENCE_RECOVERY_EVIDENCE_V1.md`.
- [x] R9a: Add a bounded `FarmTelemetryPolicy` with schema versioning, privacy-minimized default redaction of player ID/direct balances/event values, opt-in diagnostics, and a maximum event count. `farm_telemetry_smoke` and `telemetry_outbox_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving default redaction, opt-in diagnostics, invalid-cap rejection, and outbox regression. Trusted authenticated ingest, consent/retention enforcement, durable sink, dashboards, alerting, rollback, SLO, and incident operations remain open. See `docs/R9_LIVE_OPERATIONS_TELEMETRY_EVIDENCE_V1.md`.
- [x] R4a: Consolidate canonical SDL/InputState/UI routing and PCM/SDL audio reset lifecycle into a Release/ASAN workflow using dummy devices. `sdl_audio_bridge_smoke`, `sdl_input_bridge_smoke`, `input_state_smoke`, and `ui_input_router_smoke` prove bounded lifecycle/routing behavior; physical output, Android audio focus, touch/controller devices, localization/IME, and accessibility acceptance remain open. See `docs/R4_INPUT_AUDIO_LIFECYCLE_EVIDENCE_V1.md`.
- [x] R10a: Document and smoke a bounded security/privacy boundary covering credential-marker rejection, safe slot/path validation, privacy-minimized telemetry, and authority/trust rejection. `runtime_persistence_smoke`, `atomic_save_file_smoke`, `farm_telemetry_smoke`, `authoritative_command_gate_smoke`, and `trust_safety_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`; no credentials or fabricated SBOM/penetration evidence are introduced. Deployment IAM, encrypted storage, vulnerability review, consent/age governance, and adversarial testing remain open. See `docs/R10_SECURITY_PRIVACY_BOUNDARY_EVIDENCE_V1.md`.
- [x] R11a: Add a source-level Android release signing guard smoke that verifies all four required environment keys and fail-closed Gradle exception wiring without reading or creating secret values. `tools/android_release_signing_guard_smoke.sh` passes with `secrets_read=0`; signed AAB/APK, device/emulator, crash/ANR, Play review, and store evidence remain open. See `docs/R11_ANDROID_SIGNING_GUARD_EVIDENCE_V1.md`.
- [x] R12a: Add a fail-closed launch-readiness guard that validates all 12 mandatory matrix rows, confirms 11 blockers remain `Not passed`, and preserves R1 scope limitation. `tools/launch_readiness_guard_smoke.sh` passes with `gates=12 blocked=11 release_ready=0 owner_signoff=missing`; this is not soft-launch, capacity, SLO, support, rollback, or owner-sign-off evidence. See `docs/R12_LAUNCH_READINESS_GUARD_EVIDENCE_V1.md`.
- [ ] P0.6: Complete one Android delivery evidence path from canonical native lifecycle through signed package, emulator/device smoke, crash/ANR evidence and Play policy review; remain Not passed until each artifact exists.
- [x] P0.6a: Repair the canonical debug-APK build path after actual Gradle evidence; retain release signing fail-closed and validate native source alignment/toolchain preflight before treating a debug artifact as package-only evidence. Toolchain preflight, `:app:assembleDebug`, artifact metadata/ABI inspection, and v2 debug-signature verification passed; host `android_lifecycle_gate_smoke` passed in Release and AddressSanitizer with `detect_leaks=1`. LiteRT prompt execution is explicitly unavailable in this package due incompatible dependency metadata. No device/emulator, release signing, AAB, crash/ANR, or Play-readiness claim is made.
- [x] P0.6b: Bind the Android Activity’s create/resume/pause/destroy lifecycle explicitly to the canonical JNI lifecycle gate with native-load failure handling, while preserving the package’s LiteRT fail-closed status and removing inactive direct world-streaming controls from the activity. `android_lifecycle_bridge_source_smoke.sh`, `:app:assembleDebug` with Java 17/SDK 36, and `android_lifecycle_gate_smoke` in Release and AddressSanitizer with `detect_leaks=1` pass. This compiles lifecycle wiring into the debug artifact only; no Android device/emulator, surface/input/audio, release signing, AAB, crash/ANR, LiteRT inference, or Play readiness claim is made.
- [ ] P1.1: Extend the bounded asset path into deterministic import/cooking, dependency-aware cache, explicit refresh, memory budgeting, and GPU resource binding without unbounded watcher behavior.
- [ ] P1.2: Add gameplay-facing physics primitives—collider, trigger, query, rigidbody-style velocity/force contract—with an explicit XPBD/transform authority boundary and deterministic evidence.
- [x] P1.2a: Add bounded single and batch planar impulse commands to `GameplayPhysicsBodyBuilder`; validate complete candidates before writing ECS velocity and reject static, duplicate, unknown, non-finite, or overflowed commands without mutating transforms or partial body state. `gameplay_physics_body_smoke` and `gameplay_physics_query_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving inverse-mass impulse scaling, batch atomicity, static/duplicate/non-finite/overflow rejection, velocity preservation, and unchanged transform authority. This does not provide force accumulation, angular/torque dynamics, complete contact authoring, transform writes, prediction, or production physics integration.
- [x] P1.2b: Harden direct and batch planar velocity setters to reject malformed existing ECS velocity state before any write, preserving physics revision and caller-observable state on failure. `gameplay_physics_body_smoke` and `gameplay_physics_query_smoke` pass in Release and AddressSanitizer with `detect_leaks=1`, proving NaN-state rejection, batch no-partial-write behavior, physics-revision preservation, unaffected peer velocity, and query regression. This does not add force integration, angular dynamics, transform authority, persistence, prediction, or complete rigidbody semantics.
- [ ] P1.3: Integrate animation state/transition/blend and CPU skinning with character/NPC runtime ownership and rendering, while preserving the frozen single-segment route contract.
- [ ] P1.4: Replace AI/RL placeholder paths with approval-gated, bounded, testable agent planning/execution contracts; no autonomous deployment or secret/economy/ban authority.
- [ ] P1.5: Replace streaming and memory placeholders with bounded ownership, budgets, cancellation, eviction, and failure recovery evidence.
- [x] P1.5a: Add a bounded deterministic eviction-plan/commit API over the active AssetResourceManager so callers can inspect exact unleased victims, reject a stale plan before mutation, and preserve active state when a budget cannot be met. `asset_resource_manager_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving deterministic victim selection, altered-plan rejection, stale-plan rejection after a lease mutation, budget-blocked plan preservation, and exact fresh-plan commit. This is single-process CPU resource ownership only; it does not activate the legacy stream manager or provide asynchronous streaming, I/O cancellation, GPU allocation, or production memory-management guarantees.
- [x] P1.5b: Add bounded candidate-plan and commit APIs for hot reload so a caller can inspect affected unleased resources, reject forged/stale plans before mutation, and refresh a dependency closure only from current ready registry definitions. `asset_resource_manager_smoke` passes in Release and AddressSanitizer with `detect_leaks=1`, proving two-target texture/dependent-root planning, forged-plan receipt preservation, stale-plan rejection after lease activity, fresh commit, and invalid-input plan preservation. This is CPU resource metadata only, not an async file watcher/import/cancellation path, GPU-object refresh, renderer notification, or production hot reload.
- [ ] P1.6: Add platform input/audio/accessibility lifecycle evidence, including touch/controller routes, audio output recovery, localization/text scaling, offline/error UX, and acceptance tests.
- [x] P1.6a: Make SdlAudioBridge reset/reinitialize semantics explicit: stop the callback safely, clear queued voices, preserve fail-closed errors, and prove a fresh device lifecycle starts from zero frames without replaying pre-reset audio. `audio_mixer_smoke` and `sdl_audio_bridge_smoke` pass in Release and AddressSanitizer with `detect_leaks=1` under the SDL dummy driver, proving queued voice clear, callback-safe reset, reset/reinitialize frame reset, ID reuse, and preservation of invalid-init/not-initialized errors. This is local dummy-device lifecycle only, not physical output, hot-plug/device-loss, mobile audio focus, accessibility, or production platform readiness.
- [ ] P1.7: Establish production-oriented persistence/security/operations contracts: durable backend storage, migrations/backup/recovery, threat model, secret/SBOM review, consent/privacy controls, observability, alerts, rollback, and incident tests.
- [ ] P2.1: Classify legacy/experimental modules versus canonical runtime paths and either retire, isolate, or test remaining placeholder code so it cannot be mistaken for capability.
- [x] P2.1a: Add a fail-closed canonical runtime scope manifest and verifier that enumerates every current NeoEngine placeholder-marker file, explicitly distinguishes the active fail-closed legacy `EngineLoop` from non-active legacy/experimental files, and rejects any unclassified marker or accidental legacy source admission to `XPBD_RUNTIME_SOURCES`. The verifier and adversarial smoke pass with 150 active sources, 29 tracked marker paths, and 2 approved active markers; `runtime_smoke` passes in Release and AddressSanitizer with `detect_leaks=1` on head `31ca16e1abf61b168fa857f5ae57e0be3b9a06a0`. This classifies the current marker inventory only and does not close P2.1 or certify unmarked code, renderer, network, Android, payments, or release readiness.
- [ ] P2.2: Reproduce broad non-Vulkan Release and ASAN `detect_leaks=1` suites on a single tip revision under a storage-aware build strategy before making cross-engine readiness claims.
- [x] P2.2a: Execute a bounded representative non-Vulkan smoke matrix on one canonical tip in Release and AddressSanitizer with leak detection, recording exact target coverage and any external-driver exceptions without claiming whole-engine coverage; do not expand to every duplicate-heavy executable target. `docs/BROAD_NON_VULKAN_SMOKE_EVIDENCE_V1.md` records the exact 35-target set; all 35/35 pass in Release and ASAN `detect_leaks=1`. The run is headless with dummy SDL drivers; Vulkan `glslc` discovery is an external configuration exception and Vulkan presentation is not counted.
