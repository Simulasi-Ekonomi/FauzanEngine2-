# FauzanEngine Release Readiness Matrix

## Status rule

No FauzanEngine game, game template, client, APK, AAB, website, or control-plane surface may be called **release-ready**, **100% ready**, or **ready to ship** unless every mandatory gate in this matrix has passed with reproducible evidence. A passing unit test, sandbox, CPU frame, or in-process load simulation proves only the specific gate it covers.

The present engine status is **not release-ready**. Existing Farm and RPG sandboxes are backend validation assets. This matrix is a production contract, not a claim that the capabilities below already exist.

## Mandatory release gates

| ID | Gate | Mandatory production evidence | Current evidence | Status |
|---|---|---|---|---|
| R1 | Canonical game tool | A typed, versioned game-tool contract for world, rules, content, save migration, invalid input, and deterministic replay. | Farm canonical tool contract now covers typed FarmWorldTool binding, versioned rules/content, v1→v2 save migration, fail-closed invalid input, and deterministic replay; Release/ASAN evidence is recorded in `docs/FARM_CANONICAL_GAME_TOOL_R1_EVIDENCE_V1.md`. This pass is Farm-specific; other templates still require independent evidence. | Passed for Farm canonical tool scope |
| R2 | Complete game loop | A playable vertical slice implementing onboarding, core loop, progression, failure/recovery, balancing data, content authoring, and player-facing UX. | NeoRuntime Farm consumes a validated authored `FarmBalanceProfile` for growth/yield/price/energy rules, exposes energy regeneration and fail-closed energy/inventory outcomes, emits onboarding next-step/error receipts, and persists production progress through `FarmProgressCheckpointFile`; Release/ASAN evidence is recorded in `docs/NEO_RUNTIME_FARM_PROGRESSION_EVIDENCE_V1.md`, `docs/R2_STRICT_GAP_AUDIT_V1.md`, and `.github/workflows/r2-farm-loop.yml`. Remote run `33136163089` on `f42d903` passed Release and ASAN. This is a bounded canonical Farm vertical-slice pass; physical platform/accessibility/localization acceptance and broader product/package sign-off remain outside this scoped pass. | Passed for canonical Farm vertical-slice scope |
| R3 | Asset and renderer path | Production decoders; content limits; texture/mesh/material uploads; camera, lighting, animation; surface/swapchain presentation; resource-loss handling; device evidence. | Asset/texture/mesh/material bounded proofs, Farm render manifest, and Vulkan textured present now connect a canonical NeoRuntime Farm/HUD framebuffer to RGBA upload and swapchain presentation; Release and ASAN/Xvfb evidence is recorded in `docs/FARM_GPU_RENDER_INTEGRATION_EVIDENCE_V1.md`. Remote run `33137066510` on `a865577` passed Release and ASAN. This is a bounded canonical Farm renderer pass; physical-GPU/device-loss/resize, streaming, performance budget, and Android GPU delivery remain outside this scoped pass. | Passed for canonical Farm scope |
| R4 | Input, audio, accessibility | Platform touch/controller input, audio output, localization/text scaling, focus handling, offline/error UX, and accessibility acceptance tests. | Canonical InputState/SDL input, deterministic UI routing/focus, bounded PCM mixer, and SDL audio reset/reinitialize lifecycle are covered by Release/ASAN smokes in `.github/workflows/r4-input-audio.yml`; evidence is documented in `docs/R4_INPUT_AUDIO_LIFECYCLE_EVIDENCE_V1.md`. Remote run `33137030685` on `f80898e` passed Release and ASAN. This is a bounded canonical input/audio lifecycle pass; physical output, Android audio focus, touch/controller devices, localization, and accessibility remain outside this scoped pass. | Passed for canonical input/audio lifecycle scope |
| R5 | Authoritative multiplayer | Versioned client commands, authentication/session binding, authoritative simulation, snapshot/delta protocol, idempotency, replay protection, reconciliation, reconnect, and integration/load evidence. | Farm authoritative host now has bounded localhost TCP session loopback with subject/session rejection, replay stability, and opt-in two-connection reconnect; generic transport/prediction/replication primitives also have Release-safe smokes. Evidence is recorded in `docs/FARM_AUTHORITY_RECONNECT_EVIDENCE_V1.md`. TLS, durable session storage, public networking, multi-client/load evidence, and production deployment remain incomplete. | Not passed |
| R6 | Anti-cheat and fraud | Server-side validation of every privileged command, rate limits, tamper-resistant evidence records, receipt verification, ban lifecycle, appeal governance, and adversarial replay/load tests. | TrustSafety now receives strong commerce fraud signals for duplicate receipt and reconciliation mismatch, and authoritative gate rejects the resulting banned player; Release/ASAN evidence is recorded in `docs/FARM_COMMERCE_FRAUD_TRUST_EVIDENCE_V1.md` and `.github/workflows/r6-fraud-trust.yml`. Provider-backed detection, rate-limit operations, tamper-resistant storage, ban appeal governance, identity controls, and adversarial load remain incomplete. | Not passed |
| R7 | Economy and commerce | Ledger invariants, product catalog/receipt authority, refund/reversal handling, duplicate prevention, entitlement reconciliation, audit export, and operational review. | Farm commerce ledger now provides verified receipt/idempotency/reversal/reconciliation behavior, caller-owned versioned audit export, checkpoint file coverage, and fraud-linked TrustSafety receipts; Release/ASAN evidence is recorded in `docs/FARM_COMMERCE_AUDIT_EXPORT_EVIDENCE_V1.md`. Provider/payment integration, refund provider reconciliation, durable ledger, access control, and operational review remain incomplete. | Not passed |
| R8 | Persistence and recovery | Authoritative durable store, schema migrations, backups/restores, corruption handling, retention/deletion, privacy boundary, disaster-recovery test, and no credentials in saves. | Canonical versioned/checksummed runtime codecs reject malformed, trailing, unsupported, and sensitive payloads while preserving prior state; `AtomicSaveFile` now has bounded atomic slot backup/restore and missing-backup preservation, with Release/ASAN evidence in `docs/R8_PERSISTENCE_RECOVERY_EVIDENCE_V1.md` and `.github/workflows/r8-persistence-recovery.yml`. Authoritative durable store, locking/fsync, retention/privacy controls, migration service, rotation, and disaster-recovery evidence remain incomplete. | Not passed |
| R9 | Live operations | Token-holding trusted ingest host, real telemetry schema, privacy-minimized event policy, alerting, feature/config rollback, incident runbook, and no fabricated player data. | `FarmTelemetryAdapter` now emits schema-versioned telemetry with default redaction of player ID, direct balances, and event values, bounded event count, opt-in diagnostics, and Release/ASAN evidence in `docs/R9_LIVE_OPERATIONS_TELEMETRY_EVIDENCE_V1.md`; `TelemetryOutbox` regression remains green. Trusted authenticated ingest, durable sink, consent/retention enforcement, dashboards/alerting, rollback, SLO, and incident operations remain incomplete. | Not passed |
| R10 | Security and privacy | Threat model, secret management, abuse controls, dependency/SBOM review, vulnerability response, data-access policy, consent/age handling where required, and penetration/adversarial testing. | Bounded threat/asset table, credential-marker rejection in runtime persistence, privacy-minimized telemetry defaults, safe-slot/path validation, and authority/trust rejection are covered by Release/ASAN boundary smokes; evidence is documented in `docs/R10_SECURITY_PRIVACY_BOUNDARY_EVIDENCE_V1.md` and `.github/workflows/r10-security-privacy.yml`. SBOM/dependency vulnerability review, encrypted deployment, IAM/access policy, consent/age governance, incident response, and penetration/adversarial testing remain incomplete. | Not passed |
| R11 | Android delivery | Canonical Android source subset builds on pinned SDK/NDK/Java; debug APK, signed release AAB, emulator/device smoke, crash/ANR evidence, and Play policy/store asset review. | Canonical Android debug/native lifecycle evidence exists, and `tools/android_release_signing_guard_smoke.sh` proves four-key release/bundle signing is fail-closed without reading secrets; evidence is documented in `docs/R11_ANDROID_SIGNING_GUARD_EVIDENCE_V1.md`. Signed AAB/APK, device/emulator, crash/ANR, Play review, and store assets remain incomplete. | Not passed |
| R12 | Launch operations | Soft-launch plan, real consented observability, support/appeal workflow, rollback, capacity plan, service SLOs, release checklist, and final owner sign-off. | No soft-launch or operational evidence. | Not passed |

## Template-specific requirement

Each advertised game template must pass R1–R12 independently. Shared engine tooling can satisfy portions of a gate only if the particular game proves its integration. A template catalog entry, executable smoke, or game-state sandbox cannot be promoted to a production template by association with another game.

## Farm-specific release additions

Farm requires authoritative validation of crop/animal timers, building placement, inventories, market prices, top-up entitlements, player-to-player or co-op rules if enabled, and government policy boundaries. NPC and government tools are advisory or simulation components; they must never mint currency, accept receipts, modify bans, or bypass the authoritative service.

## Evidence package requirement

Every final release claim must include the exact build identifier, source revision, dependency lockfile/SBOM, test reports, performance/load reports, supported-device list, package hashes, signing provenance without secret disclosure, operations runbook, and documented known issues. Any missing item leaves the release status as **not passed**.
