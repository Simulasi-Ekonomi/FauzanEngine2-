#include "ECSCore.h"

// Registry implementation is header-only for templates.
// This file keeps linkable object for non-template functions if needed.
namespace NeoEngine {
    // (future: non-template Registry methods)
}
